---
name: verify
description: Комплексная проверка что ничего не сломалось после изменений
---

# Верификация изменений

## Задача

Провести комплексную проверку что все изменения корректны и ничего не сломано.

## Чеклист проверки

### 1. Синтаксис Python

Проверь что все файлы имеют корректный синтаксис:
```bash
echo "🔍 Проверка синтаксиса Python..."
find docprep -name "*.py" -exec python -m py_compile {} \; 2>&1
if [ $? -eq 0 ]; then
    echo "✅ Синтаксис OK"
else
    echo "❌ ОШИБКИ СИНТАКСИСА!"
fi
```

### 2. Импорты работают

```bash
echo "🔍 Проверка импортов..."
python -c "
from docprep.core import *
from docprep.engine import *
print('✅ Импорты OK')
" 2>&1
```

### 3. Тесты проходят

```bash
echo "🔍 Запуск тестов..."
pytest docprep/tests/ -v --tb=short 2>&1
```

### 4. Опечатки исправлены

```bash
echo "🔍 Проверка опечаток..."
TYPOS=$(grep -rn "ErExtact\|ErNormalaze" docprep/ 2>/dev/null | wc -l)
if [ "$TYPOS" -eq 0 ]; then
    echo "✅ Опечатки исправлены"
else
    echo "❌ Найдены опечатки:"
    grep -rn "ErExtact\|ErNormalaze" docprep/
fi
```

### 5. CLI работает

```bash
echo "🔍 Проверка CLI..."
python -m docprep.cli.main --help > /dev/null 2>&1
if [ $? -eq 0 ]; then
    echo "✅ CLI работает"
else
    echo "❌ CLI не запускается!"
fi
```

### 6. Неиспользуемые импорты (опционально)

```bash
echo "🔍 Проверка неиспользуемых импортов..."
ruff check --select=F401 docprep/ 2>/dev/null || echo "⚠️ ruff не установлен, пропускаем"
```

## Финальный отчёт

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 ОТЧЁТ ВЕРИФИКАЦИИ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[✅/❌] Синтаксис Python
[✅/❌] Импорты
[✅/❌] Тесты (X passed, Y failed)
[✅/❌] Опечатки
[✅/❌] CLI

Общий статус: [ГОТОВО К ПРОДОЛЖЕНИЮ / ТРЕБУЮТСЯ ИСПРАВЛЕНИЯ]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Если что-то не прошло — СТОП! Сообщи через HITL и дождись указаний.
