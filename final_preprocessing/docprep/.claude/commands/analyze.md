---
name: analyze
description: Глубокий анализ текущего состояния кодовой базы перед рефакторингом
---

# Задача: Глубокий анализ кодовой базы DocPrep

Проведи комплексный анализ текущего состояния проекта ПЕРЕД любыми изменениями.

## Шаг 1: Структурный анализ

Изучи структуру проекта:
```bash
tree -I '__pycache__|*.pyc|.git' docprep/
```

## Шаг 2: Анализ зависимостей

Проверь импорты и зависимости между модулями:
- Какие модули импортируют config.py?
- Какие модули используют State Machine?
- Есть ли циклические зависимости?

## Шаг 3: Поиск проблемных мест

Найди все упоминания опечаток:
```bash
grep -rn "ErExtact\|ErNormalaze" docprep/
```

Найди magic numbers:
```bash
grep -rn "if.*> 3\|> 1000\|== 3" docprep/
```

## Шаг 4: Анализ тестового покрытия

Проверь какие тесты существуют:
```bash
find docprep/tests -name "*.py" -exec basename {} \;
```

Запусти существующие тесты:
```bash
pytest docprep/tests/ -v --tb=short 2>&1 | head -50
```

## Шаг 5: Составление отчёта

Подготовь структурированный отчёт:

```
📊 ОТЧЁТ АНАЛИЗА КОДОВОЙ БАЗЫ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. СТРУКТУРА
   - Количество модулей: X
   - Ключевые компоненты: [список]

2. ПРОБЛЕМЫ (найденные)
   - Опечатки: [где и сколько]
   - Magic numbers: [где]
   - Дублирование кода: [где]

3. ТЕСТЫ
   - Существующие тесты: [список]
   - Покрытие: X%
   - Проходят/Падают: X/Y

4. РИСКИ РЕФАКТОРИНГА
   - Критические файлы: [список]
   - Зависимости которые могут сломаться: [список]

5. РЕКОМЕНДАЦИИ
   - Порядок изменений: [последовательность]
   - Что требует HITL: [список]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

После анализа ОБЯЗАТЕЛЬНО покажи отчёт и дождись подтверждения перед продолжением.
