# 🚀 Стартовый промпт для безопасного рефакторинга DocPrep

## Как использовать

1. Скопируйте файлы конфигурации в ваш проект:
   - `.claude/CLAUDE.md`
   - `.claude/settings.json`
   - `.claude/commands/*.md`
   - `.claude/agents/*.md`
   - `.claude/DOCPREP_REFACTORING_PLAN.md` (ваш план)

2. Откройте терминал в директории проекта и запустите:
   ```bash
   cd path/to/docprep
   claude
   ```

3. Вставьте стартовый промпт ниже и следуйте инструкциям.

---

## 📋 СТАРТОВЫЙ ПРОМПТ (скопируйте целиком)

```
Привет! Мы начинаем безопасный рефакторинг проекта DocPrep.

ВАЖНЫЕ ПРИНЦИПЫ РАБОТЫ:

1. HITL (Human in the Loop) — я хочу контролировать все важные решения. 
   Перед любым рискованным действием (удаление файлов, изменение API, 
   спорные решения) — спрашивай моё подтверждение в формате:
   
   🔔 HITL CHECKPOINT
   ━━━━━━━━━━━━━━━━━━
   Действие: [что планируешь]
   Риск: [что может сломаться]
   ━━━━━━━━━━━━━━━━━━
   Продолжить? (да/нет/обсудить)

2. TDD — сначала тесты, потом код. Для каждого изменения:
   - Напиши тест (red)
   - Убедись что падает
   - Сделай изменение
   - Убедись что проходит (green)

3. Инкрементальность — маленькие шаги с проверкой после каждого.

4. Не сломать — главный приоритет. Если сомневаешься — спроси.

ПЛАН РАБОТЫ:

Начнём с глубокого анализа. Выполни команду /project:analyze чтобы:
- Изучить текущее состояние кодовой базы
- Найти все проблемные места
- Понять зависимости между модулями
- Запустить существующие тесты

После анализа покажи мне отчёт и мы вместе решим как продолжать.

Поехали!
```

---

## 📋 ПРОМПТЫ ДЛЯ КАЖДОЙ ФАЗЫ

### После анализа — Фаза 1 (Опечатки)

```
Отлично, теперь выполни ФАЗУ 1 — исправление опечаток.

Но сначала напиши тесты! Используй /project:write-tests для core/config.py

Тесты должны проверять:
- Что EXCEPTION_SUBDIRS содержит "ErExtract" (не "ErExtact")
- Что EXCEPTION_SUBDIRS содержит "ErNormalize" (не "ErNormalaze")
- Что директории создаются с правильными именами

После тестов — исправь опечатки через /project:refactor-safe 1

В конце — /project:verify чтобы убедиться что всё работает.
```

### Фаза 2 (Архитектура)

```
Переходим к ФАЗЕ 2 — улучшение архитектуры.

Начнём с State Machine. Сначала напиши тесты:
/project:write-tests для core/state_machine.py

Тесты:
- test_valid_transition — валидные переходы работают
- test_invalid_transition_raises — невалидные выбрасывают исключение
- test_transition_with_metadata — метаданные сохраняются
- test_state_trace_history — история записывается
- test_is_terminal — терминальные состояния определяются

Затем реализуй StateTransition dataclass и обнови UnitStateMachine.

ВАЖНО: Сохрани обратную совместимость с существующими manifest.json!
Если это требует изменения формата — HITL checkpoint.
```

### Фаза 2 продолжение (Логирование)

```
Теперь создай централизованное логирование.

Создай файл docprep/core/logging_config.py с:
- setup_logging(verbose, log_file) — настройка
- get_logger(name) — получение логгера

Это новый файл, тесты опциональны. Но проверь что импорт работает.

Затем интегрируй в cli/main.py.

После — /project:verify
```

### Фаза 3 (Очистка)

```
ФАЗА 3 — очистка кода.

🔔 ВНИМАНИЕ: Эта фаза включает УДАЛЕНИЕ файлов. 
Перед каждым удалением — HITL checkpoint!

1. Проверь cli/chunked_classifier.py — действительно дублирует?
   Покажи мне анализ перед удалением.

2. Проверь tests/test_cli.py — если только TODO, можно удалить.
   Покажи содержимое.

3. Создай .gitignore для Python артефактов.

После каждого удаления — /project:verify
```

### Фаза 4 (Тесты)

```
Финальная ФАЗА 4 — дополнительные тесты.

Напиши недостающие тесты:
- tests/test_state_machine.py (если не готов)
- tests/test_merger.py — интеграционные тесты

Используй /project:write-tests для каждого модуля.

В конце — полная проверка покрытия:
pytest --cov=docprep --cov-report=term-missing tests/
```

---

## 📋 ПРОМПТ ДЛЯ ЗАВЕРШЕНИЯ

```
Рефакторинг завершён! Давай сделаем финальную проверку:

1. /project:verify — полная верификация

2. Проверь покрытие тестами:
   pytest --cov=docprep tests/

3. Проверь форматирование:
   black docprep/ --check
   isort docprep/ --check

4. Подготовь финальный отчёт:
   - Что изменилось
   - Какие тесты добавлены
   - Результаты тестирования

5. Если всё OK — создай коммит:
   git add -A
   git commit -m "Refactoring: [краткое описание всех изменений]"

Покажи мне финальный отчёт!
```

---

## 🆘 ПРОМПТ ДЛЯ ОТКАТА (если что-то сломалось)

```
СТОП! Что-то сломалось.

1. Покажи текущие ошибки

2. Покажи последние изменённые файлы:
   git diff --name-only

3. НЕ делай новых изменений пока не разберёмся

4. Варианты:
   - Откатить последнее изменение: git checkout -- <файл>
   - Откатить всё: git checkout .
   - Разобраться в причине

Давай обсудим что делать.
```
