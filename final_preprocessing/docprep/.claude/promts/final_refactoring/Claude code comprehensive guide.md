# 📚 Исчерпывающее руководство по Claude Code
## Инструкции и справочник для ведения разработки

**Версия:** 2.1 (Январь 2026)  
**Актуальность:** Claude Code 2.1.x

---

## 📑 Содержание

1. [Введение и архитектура](#1-введение-и-архитектура)
2. [Способы установки](#2-способы-установки)
3. [Режимы выполнения](#3-режимы-выполнения)
4. [Конфигурация проекта](#4-конфигурация-проекта)
5. [CLAUDE.md — память проекта](#5-claudemd--память-проекта)
6. [Skills — навыки агента](#6-skills--навыки-агента)
7. [Hooks — автоматизация](#7-hooks--автоматизация)
8. [MCP — внешние интеграции](#8-mcp--внешние-интеграции)
9. [Subagents — подагенты](#9-subagents--подагенты)
10. [Slash Commands — команды](#10-slash-commands--команды)
11. [Permissions — разрешения](#11-permissions--разрешения)
12. [Практические сценарии](#12-практические-сценарии)
13. [Выполнение плана рефакторинга](#13-выполнение-плана-рефакторинга)
14. [Лучшие практики](#14-лучшие-практики)

---

## 1. Введение и архитектура

### 1.1 Что такое Claude Code?

Claude Code — это **агентный инструмент для кодирования**, который работает в терминале, понимает вашу кодовую базу и помогает писать код быстрее через команды на естественном языке. Это не просто автодополнение — это полноценный фреймворк для оркестрации AI-агентов.

### 1.2 Архитектура Claude Code

Claude Code имеет многослойную архитектуру, где каждый слой добавляет возможности:

```
┌─────────────────────────────────────────────────────────────────┐
│                    СЛОЙ РАСШИРЕНИЙ (Extension Layer)            │
│  MCP серверы → Hooks → Skills → Plugins                         │
│  (Внешние сервисы, автоматизация, знания, пакеты)              │
├─────────────────────────────────────────────────────────────────┤
│                    СЛОЙ ДЕЛЕГИРОВАНИЯ (Delegation Layer)        │
│  Subagents → Task Tool → Explore/Plan agents                    │
│  (Параллельная работа, изолированные контексты)                │
├─────────────────────────────────────────────────────────────────┤
│                    ЯДРО (Core Layer)                            │
│  Read → Write → Edit → Bash → Grep → Glob                       │
│  (Базовые инструменты работы с файлами и командами)            │
├─────────────────────────────────────────────────────────────────┤
│                    МОДЕЛЬ (Model Layer)                         │
│  Opus 4.5 (мощный) ↔ Sonnet 4.5 (быстрый) ↔ Haiku (дешёвый)   │
│  (Переключение моделей для оптимизации стоимости/качества)     │
└─────────────────────────────────────────────────────────────────┘
```

**Ключевой инсайт:** Большинство пользователей работают только с ядром. Опытные пользователи используют делегирование для параллельной работы и расширения для автоматизации, а ядро — только для оркестрации.

### 1.3 Структура файлов конфигурации

```
your-project/
├── CLAUDE.md                    # Память проекта (альтернативное расположение)
├── .mcp.json                    # MCP серверы (коммитится в git)
├── .claude/
│   ├── CLAUDE.md               # Память проекта (основное место)
│   ├── settings.json           # Hooks, переменные окружения, разрешения
│   ├── settings.local.json     # Личные настройки (в .gitignore)
│   ├── commands/               # Пользовательские slash-команды
│   │   ├── fix-issue.md
│   │   └── review.md
│   ├── skills/                 # Навыки проекта
│   │   └── deployment/
│   │       └── SKILL.md
│   └── agents/                 # Кастомные подагенты
│       └── code-reviewer.md
└── ~/.claude/                   # Глобальные настройки пользователя
    ├── CLAUDE.md               # Глобальная память
    ├── settings.json           # Глобальные настройки
    ├── commands/               # Личные команды
    └── skills/                 # Личные навыки
```

---

## 2. Способы установки

### 2.1 Рекомендуемые методы

**macOS/Linux (curl):**
```bash
# Самый простой способ
curl -fsSL https://claude.ai/install.sh | sh
```

**macOS (Homebrew):**
```bash
brew install anthropic/tap/claude-code
```

**Windows (PowerShell):**
```powershell
irm https://claude.ai/install.ps1 | iex
```

**npm (глобально):**
```bash
npm install -g @anthropic-ai/claude-code
```

### 2.2 Первый запуск

```bash
# Переход в директорию проекта
cd your-project

# Запуск Claude Code
claude

# Первый запуск попросит авторизацию через браузер
```

### 2.3 Проверка установки

```bash
# Версия
claude --version

# Статус (модель, контекст, стоимость)
/status

# Помощь
claude --help
```

---

## 3. Режимы выполнения

### 3.1 Обзор режимов

| Режим | Описание | Команда | Безопасность |
|-------|----------|---------|--------------|
| **Интерактивный** | Стандартный режим с подтверждениями | `claude` | ✅ Максимальная |
| **Auto-Accept** | Автопринятие с визуальным контролем | `Shift+Tab` | ⚠️ Средняя |
| **YOLO Mode** | Полностью автономный без подтверждений | `--dangerously-skip-permissions` | ❌ Минимальная |
| **Headless** | Для CI/CD и автоматизации | `-p "prompt"` | ⚙️ Настраиваемая |

### 3.2 Интерактивный режим (по умолчанию)

Это стандартный режим, где Claude запрашивает подтверждение для каждой операции:

```bash
claude
```

**Плюсы:** Полный контроль, безопасность, понимание происходящего.  
**Минусы:** Много прерываний при длинных задачах.

### 3.3 Auto-Accept режим

Включается во время сессии нажатием `Shift+Tab`. Показывает "auto-accept edit on" и продолжает без пауз, но вы видите происходящее и можете вмешаться.

**Когда использовать:**
- Задачи средней сложности, которые вы понимаете
- Когда хотите наблюдать, но не прерывать

### 3.4 YOLO Mode (dangerously-skip-permissions)

**⚠️ ВНИМАНИЕ: Это опасный режим! Используйте с осторожностью!**

```bash
# Полный автономный режим
claude --dangerously-skip-permissions

# Создание алиаса для удобства
alias clauded="claude --dangerously-skip-permissions"
```

**Что происходит:** Claude выполняет ВСЕ операции без подтверждений — создание, изменение, удаление файлов, выполнение команд.

**Риски (реальная статистика):**
- 32% разработчиков сталкивались с непреднамеренными изменениями файлов
- 9% сообщали о потере данных или повреждении
- Возможен "scope creep" — изменение файлов вне области задачи

**Когда МОЖНО использовать:**
- Массовые исправления lint-ошибок
- Генерация boilerplate-кода
- В изолированном Docker-контейнере
- В проектах под контролем версий с возможностью отката

**Когда НЕЛЬЗЯ использовать:**
- В директориях с важными данными
- На рабочих машинах без изоляции
- При работе с конфиденциальной информацией
- В production-окружении

### 3.5 Headless режим (для CI/CD)

Предназначен для автоматизации в скриптах и CI/CD пайплайнах:

```bash
# Базовый headless запуск
claude -p "Fix all TypeScript errors" --output-format json

# С таймаутом и лимитом итераций
claude -p "Analyze code quality" \
  --output-format stream-json \
  --max-turns 10 \
  --timeout-minutes 30

# Для CI с полной автономией (в контейнере!)
claude -p "Run tests and fix failures" \
  --dangerously-skip-permissions \
  --output-format json
```

**Форматы вывода:**
- `text` — простой текст (по умолчанию)
- `json` — структурированный JSON
- `stream-json` — потоковый JSON (для длинных задач)

### 3.6 Безопасный YOLO в Docker

Рекомендуемый способ использования автономного режима:

```dockerfile
# Dockerfile для безопасного YOLO
FROM ubuntu:22.04

# Установка зависимостей
RUN apt-get update && apt-get install -y \
    nodejs npm git curl \
    && rm -rf /var/lib/apt/lists/*

# Установка Claude Code
RUN curl -fsSL https://claude.ai/install.sh | sh

# Создание рабочей директории
WORKDIR /workspace

# Копирование проекта
COPY . .

# Запуск без сети для безопасности
# docker run --network none ...
```

```bash
# Запуск в изолированном контейнере
docker run --rm -it \
  --network none \  # Без интернета для безопасности
  -v $(pwd):/workspace \
  claude-safe-yolo \
  claude --dangerously-skip-permissions -p "Fix all lint errors"
```

---

## 4. Конфигурация проекта

### 4.1 settings.json — основная конфигурация

Расположение: `.claude/settings.json`

```json
{
  "permissions": {
    "allow": [
      "Bash(git status)",
      "Bash(git diff:*)",
      "Bash(npm test)",
      "Bash(python -m pytest:*)",
      "Read",
      "Write(*.md)",
      "Edit"
    ],
    "deny": [
      "Bash(rm -rf:*)",
      "Read(.env*)",
      "Write(.env*)"
    ]
  },
  "env": {
    "PYTHONPATH": "./src",
    "NODE_ENV": "development"
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "prettier --write \"${TOOL_INPUT_FILE_PATH}\""
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "typescript-lsp@claude-plugins-official": true,
    "pyright-lsp@claude-plugins-official": true
  }
}
```

### 4.2 Иерархия конфигурации

Конфигурации объединяются в порядке приоритета (более высокий переопределяет):

1. **Enterprise** — корпоративные настройки (если есть)
2. **User** (`~/.claude/`) — глобальные настройки пользователя
3. **Project** (`.claude/`) — настройки проекта
4. **Local** (`.claude/settings.local.json`) — личные настройки (в .gitignore)

### 4.3 settings.local.json — личные настройки

```json
{
  "env": {
    "ANTHROPIC_API_KEY": "sk-...",
    "DATABASE_URL": "postgresql://localhost:5432/mydb"
  },
  "permissions": {
    "allow": [
      "Bash(docker compose:*)"
    ]
  }
}
```

**Важно:** Добавьте в `.gitignore`:
```gitignore
.claude/settings.local.json
```

---

## 5. CLAUDE.md — память проекта

### 5.1 Назначение

CLAUDE.md — это специальный файл, который Claude автоматически загружает при старте сессии. Он даёт Claude "память" о вашем проекте: архитектуру, конвенции, особенности.

### 5.2 Расположение (по приоритету)

1. `.claude/CLAUDE.md` — основное место
2. `CLAUDE.md` — корень проекта (альтернатива)
3. `~/.claude/CLAUDE.md` — глобальные инструкции

### 5.3 Рекомендуемая структура

```markdown
# Project: DocPrep Backend

## Обзор
DocPrep — CLI система для preprocessing документов с 3-цикловой обработкой.

## Технологический стек
- Python 3.11+
- Typer (CLI)
- Pydantic (валидация)
- LibreOffice (конвертация) — обязательная зависимость

## Архитектура
```
docprep/
├── core/          # Ядро: state machine, manifest, config
├── engine/        # Движки: classifier, converter, extractor
├── cli/           # CLI команды
└── utils/         # Утилиты
```

## Конвенции кода
- Типизация: использовать type hints везде
- Docstrings: Google-style
- Форматирование: black + isort
- Линтинг: ruff
- Тесты: pytest

## Важные паттерны
- State Machine для управления состояниями UNIT
- Manifest v2 для метаданных
- ErrorPolicy для обработки ошибок

## Особенности проекта
- Директории исключений: ErConvert, ErExtract, ErNormalize
- Максимум 3 цикла обработки (MAX_CYCLES = 3)

## Команды разработки
```bash
# Тесты
pytest tests/ -v

# Форматирование
black docprep/ && isort docprep/

# Линтинг
ruff check docprep/

# CLI
python -m docprep.cli.main --help
```

## Что НЕ делать
- Не добавлять fallback для LibreOffice
- Не трогать Web UI в этой итерации
- Не коммитить Data/ директорию
```

### 5.4 Включение файлов через @include

CLAUDE.md поддерживает директиву `@include` для модульности:

```markdown
# Project Memory

@include docs/ARCHITECTURE.md
@include docs/CONVENTIONS.md
@include .claude/steering/tech.md
```

**Важно:** Избегайте включения бинарных файлов (изображения, PDF).

---

## 6. Skills — навыки агента

### 6.1 Что такое Skills?

**Skills** (навыки) — это модульные инструкции, которые Claude автоматически активирует на основе контекста задачи. В отличие от slash-команд (вызываются пользователем), Skills вызываются моделью.

**Ключевое отличие от MCP:**
- **MCP** предоставляет инструменты (доступ к API, базам данных)
- **Skills** учат Claude КАК использовать эти инструменты

### 6.2 Структура навыка

```
.claude/skills/
└── deployment/
    ├── SKILL.md           # Обязательный файл с инструкциями
    ├── examples/          # Примеры (опционально)
    │   └── deploy.sh
    └── resources/         # Ресурсы (опционально)
        └── checklist.md
```

### 6.3 Формат SKILL.md

```markdown
---
# YAML frontmatter (метаданные)
name: deployment
description: Инструкции по деплою приложения на Railway
version: 1.0.0
allowed-tools:
  - Bash
  - Read
  - Write
user-invocable: true  # Показывать в меню /
---

# Deployment Skill

## Когда использовать
Активируй этот навык когда пользователь просит:
- Задеплоить приложение
- Настроить CI/CD
- Проверить статус деплоя

## Процедура деплоя

### 1. Проверка готовности
```bash
# Запусти это для проверки
./scripts/pre-deploy-check.sh
```

### 2. Деплой
```bash
railway up --environment production
```

### 3. Верификация
- Проверь health endpoint
- Проверь логи на ошибки

## Rollback
Если что-то пошло не так:
```bash
railway rollback
```
```

### 6.4 Progressive Disclosure (прогрессивная загрузка)

Skills используют трёхуровневую систему загрузки для экономии контекста:

1. **Frontmatter** — загружается всегда (name + description)
2. **SKILL.md body** — загружается при активации навыка
3. **Linked files** — загружаются по запросу

Это позволяет иметь неограниченный объём документации без переполнения контекста.

### 6.5 Вызов навыков

```bash
# Автоматически (Claude сам активирует по контексту)
> Deploy the application to production

# Явно через меню
/project:deployment

# Через Skill tool (программно)
> Use the deployment skill to deploy
```

---

## 7. Hooks — автоматизация

### 7.1 Что такое Hooks?

Hooks — это автоматические триггеры, которые выполняют shell-команды в определённые моменты жизненного цикла Claude Code. Они обеспечивают **детерминированное** поведение (в отличие от инструкций в CLAUDE.md).

### 7.2 Типы событий

| Событие | Когда срабатывает | Применение |
|---------|-------------------|------------|
| `PreToolUse` | Перед вызовом инструмента | Валидация, блокировка опасных команд |
| `PostToolUse` | После вызова инструмента | Форматирование, линтинг, логирование |
| `SessionStart` | При старте сессии | Инициализация окружения |
| `SessionStop` | При завершении сессии | Очистка, сохранение логов |
| `Stop` | Когда Claude завершает ответ | Логирование, уведомления |

### 7.3 Конфигурация hooks

```json
// .claude/settings.json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "./scripts/validate-bash.sh",
            "timeout": 5000
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "prettier --write \"${TOOL_INPUT_FILE_PATH}\"",
            "timeout": 10000
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command", 
            "command": "npm run typecheck -- \"${TOOL_INPUT_FILE_PATH}\""
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo \"Session started at $(date)\" >> ~/.claude/activity.log"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$(date): ${CONVERSATION_SUMMARY}\" >> ~/.claude/activity.log"
          }
        ]
      }
    ]
  }
}
```

### 7.4 Matchers (паттерны соответствия)

Matchers определяют, для каких инструментов срабатывает hook:

```json
{
  "matcher": "Bash",           // Точное совпадение
  "matcher": "Write|Edit",     // Regex: Write ИЛИ Edit
  "matcher": "Bash(git:*)",    // Bash команды начинающиеся с git
  "matcher": ".*"              // Все инструменты
}
```

### 7.5 Возвращаемые значения PreToolUse

Hook может вернуть JSON для управления поведением:

```json
{
  "block": true,                    // Заблокировать операцию
  "message": "Reason for blocking", // Сообщение пользователю
  "feedback": "Info for Claude",    // Обратная связь модели
  "suppressOutput": true,           // Скрыть вывод команды
  "continue": false                 // Продолжать ли выполнение
}
```

### 7.6 Переменные окружения в hooks

```bash
${TOOL_INPUT_FILE_PATH}   # Путь к файлу (для Write/Edit)
${TOOL_NAME}              # Имя инструмента
${CONVERSATION_SUMMARY}   # Сводка разговора (для Stop)
${CLAUDE_PLUGIN_ROOT}     # Корень плагина
```

### 7.7 Примеры полезных hooks

**Автоформатирование Python:**
```json
{
  "PostToolUse": [
    {
      "matcher": "Write|Edit",
      "hooks": [
        {
          "type": "command",
          "command": "if [[ \"${TOOL_INPUT_FILE_PATH}\" == *.py ]]; then black \"${TOOL_INPUT_FILE_PATH}\" && isort \"${TOOL_INPUT_FILE_PATH}\"; fi"
        }
      ]
    }
  ]
}
```

**Блокировка опасных команд:**
```json
{
  "PreToolUse": [
    {
      "matcher": "Bash",
      "hooks": [
        {
          "type": "command",
          "command": "if echo \"$TOOL_INPUT\" | grep -qE 'rm -rf|sudo|DROP TABLE'; then echo '{\"block\": true, \"message\": \"Dangerous command blocked\"}'; fi"
        }
      ]
    }
  ]
}
```

---

## 8. MCP — внешние интеграции

### 8.1 Что такое MCP?

**Model Context Protocol (MCP)** — это протокол для подключения Claude к внешним инструментам, API и сервисам. Это "универсальный адаптер" (как USB-C для AI).

**Экосистема:** 300+ интеграций, от GitHub и Slack до PostgreSQL и Sentry.

### 8.2 Конфигурация MCP

Расположение: `.mcp.json` (коммитится в git для команды)

```json
{
  "mcpServers": {
    "github": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-github"],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    },
    "postgres": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-postgres"],
      "env": {
        "DATABASE_URL": "${DATABASE_URL}"
      }
    },
    "jira": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-jira"],
      "env": {
        "JIRA_HOST": "${JIRA_HOST}",
        "JIRA_EMAIL": "${JIRA_EMAIL}",
        "JIRA_API_TOKEN": "${JIRA_API_TOKEN}"
      }
    },
    "sentry": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-sentry"],
      "env": {
        "SENTRY_AUTH_TOKEN": "${SENTRY_AUTH_TOKEN}"
      }
    }
  }
}
```

### 8.3 Управление MCP серверами

```bash
# Добавить сервер
claude mcp add github npx -y @anthropic/mcp-github

# Добавить с переменными окружения
claude mcp add postgres npx -y @anthropic/mcp-postgres \
  --env DATABASE_URL=postgresql://localhost:5432/db

# Список серверов
claude mcp list

# Удалить сервер
claude mcp remove github

# Отладка MCP
claude --mcp-debug
```

### 8.4 Популярные MCP серверы

| Сервер | Назначение | Команда установки |
|--------|-----------|-------------------|
| **GitHub** | PRs, Issues, код | `@anthropic/mcp-github` |
| **PostgreSQL** | База данных | `@anthropic/mcp-postgres` |
| **Slack** | Сообщения, каналы | `@anthropic/mcp-slack` |
| **JIRA** | Тикеты, проекты | `@anthropic/mcp-jira` |
| **Sentry** | Ошибки, мониторинг | `@anthropic/mcp-sentry` |
| **Puppeteer** | Браузерная автоматизация | `@playwright/mcp` |
| **Notion** | Документы, базы знаний | `@anthropic/mcp-notion` |
| **Airtable** | Таблицы, данные | `airtable-mcp-server` |

### 8.5 MCP + Skills = Мощные workflow

MCP предоставляет доступ, Skills учат использовать:

```markdown
# .claude/skills/jira-workflow/SKILL.md
---
name: jira-workflow
description: Работа с JIRA тикетами по нашим процессам
---

# JIRA Workflow

Когда пользователь говорит "работай с тикетом PROJ-123":

1. Получи тикет: `mcp__jira__get_issue PROJ-123`
2. Прочитай acceptance criteria
3. Найди связанные файлы в кодовой базе
4. Реализуй изменения
5. Обнови статус тикета: `mcp__jira__update_issue`
6. Создай PR с линком на тикет
```

---

## 9. Subagents — подагенты

### 9.1 Что такое Subagents?

Subagents — это специализированные "помощники" с отдельным контекстом, своими инструментами и специализацией. Они позволяют делегировать задачи и работать параллельно.

### 9.2 Встроенные подагенты

- **Explore** — исследование кодовой базы
- **Plan** — планирование задач
- **Task** — выполнение подзадач

### 9.3 Кастомные подагенты

Расположение: `.claude/agents/code-reviewer.md`

```markdown
---
name: code-reviewer
description: Ревью кода на качество и безопасность
model: sonnet
tools:
  - Read
  - Grep
  - Glob
  - Bash
skills:
  - security-check
  - code-style
---

Ты — Senior Code Reviewer. Твоя задача:

1. Анализировать изменения на:
   - Качество кода
   - Безопасность
   - Производительность
   - Соответствие конвенциям

2. Выдавать структурированный отчёт:
   - 🔴 Критические проблемы
   - 🟡 Предупреждения
   - 🟢 Предложения по улучшению

3. НЕ вносить изменения, только анализировать.
```

### 9.4 Запуск через CLI

```bash
# Определение подагентов при запуске
claude --agents '{
  "code-reviewer": {
    "description": "Expert code reviewer",
    "prompt": "You are a senior code reviewer...",
    "tools": ["Read", "Grep", "Glob"],
    "model": "sonnet"
  },
  "debugger": {
    "description": "Debugging specialist",
    "prompt": "You are an expert debugger...",
    "tools": ["Read", "Bash", "Grep"]
  }
}'
```

### 9.5 Паттерн: Writer + Reviewer

Мощный паттерн — один агент пишет код, другой ревьюит:

```
[Main Agent]
    ↓ "Implement feature X"
[Writer Subagent] → код
    ↓ результат
[Reviewer Subagent] → ревью
    ↓ фидбек
[Main Agent] → итерация
```

---

## 10. Slash Commands — команды

### 10.1 Встроенные команды

| Команда | Описание |
|---------|----------|
| `/help` | Справка по командам |
| `/status` | Модель, контекст, стоимость |
| `/context` | Детали использования контекста |
| `/clear` | Очистить контекст |
| `/compact` | Сжать контекст |
| `/config` | Открыть конфигурацию |
| `/hooks` | Управление hooks |
| `/mcp` | Управление MCP серверами |
| `/bug` | Отправить баг-репорт |
| `/install-github-app` | Установить GitHub App для PR ревью |

### 10.2 Кастомные команды

Расположение: `.claude/commands/fix-issue.md`

```markdown
---
name: fix-issue
description: Исправить GitHub issue по номеру
---

Исправь GitHub issue: $ARGUMENTS

Процедура:
1. Получи детали issue: `gh issue view $ARGUMENTS`
2. Изучи проблему
3. Найди связанные файлы
4. Реализуй исправление
5. Напиши тесты
6. Проверь линтинг и типы
7. Создай описательный коммит
8. Создай PR

Используй GitHub CLI (`gh`) для всех операций с GitHub.
```

**Использование:**
```bash
/project:fix-issue 1234
```

### 10.3 Личные команды

Расположение: `~/.claude/commands/` — доступны во всех проектах

```markdown
# ~/.claude/commands/morning.md
---
name: morning
description: Утренний обзор проекта
---

Доброе утро! Пожалуйста:
1. Покажи статус git (ветка, изменения)
2. Проверь, есть ли незакрытые TODO в коде
3. Предложи 3 приоритетные задачи на сегодня
```

---

## 11. Permissions — разрешения

### 11.1 Философия: Principle of Least Privilege

Claude Code по умолчанию работает с минимальными правами — только в директории запуска и её поддиректориях.

### 11.2 Настройка разрешений

```json
// .claude/settings.json
{
  "permissions": {
    "allow": [
      // Разрешить конкретные bash команды
      "Bash(git status)",
      "Bash(git diff:*)",
      "Bash(npm test)",
      "Bash(npm run:*)",
      "Bash(python -m pytest:*)",
      
      // Разрешить чтение всего
      "Read",
      
      // Разрешить запись только в определённые файлы
      "Write(*.md)",
      "Write(*.py)",
      "Write(*.ts)",
      
      // Разрешить редактирование
      "Edit"
    ],
    "deny": [
      // Запретить опасные команды
      "Bash(rm -rf:*)",
      "Bash(sudo:*)",
      "Bash(chmod:*)",
      
      // Запретить доступ к секретам
      "Read(.env*)",
      "Write(.env*)",
      "Read(secrets/*)"
    ]
  }
}
```

### 11.3 allowedTools vs dangerously-skip-permissions

| Аспект | allowedTools | YOLO Mode |
|--------|-------------|-----------|
| Контроль | Гранулярный | Отсутствует |
| Прозрачность | Видно что разрешено | Всё разрешено |
| Безопасность | Высокая | Низкая |
| Рекомендация | ✅ Использовать | ⚠️ Только в контейнере |

### 11.4 Автоодобрение MCP серверов

```json
{
  "mcpAutoApprove": {
    "github": true,
    "postgres": false  // Требует подтверждения
  }
}
```

---

## 12. Практические сценарии

### 12.1 Сценарий: Рефакторинг по плану

Ваш план рефакторинга DocPrep можно выполнить разными способами:

**Вариант A: Пошаговый интерактивный**
```bash
cd docprep
claude

# В сессии:
> Прочитай план рефакторинга из .claude/DOCPREP_REFACTORING_PLAN.md
> Выполни ФАЗУ 1: Исправь все опечатки ErExtact → ErExtract и ErNormalaze → ErNormalize
> Проверь что всё работает: запусти тесты
> Выполни ФАЗУ 2: Создай logging_config.py
...
```

**Вариант B: Полуавтономный с Auto-Accept**
```bash
claude
# Нажать Shift+Tab для Auto-Accept
> Выполни план рефакторинга из .claude/DOCPREP_REFACTORING_PLAN.md фазу за фазой
```

**Вариант C: Полностью автономный (в Docker)**
```bash
# В изолированном контейнере
docker run --rm -v $(pwd):/workspace claude-safe \
  claude --dangerously-skip-permissions \
  -p "Выполни план рефакторинга из .claude/DOCPREP_REFACTORING_PLAN.md. 
      После каждой фазы запускай тесты. 
      Коммить после успешных тестов."
```

**Вариант D: Через кастомную команду**

Создайте `.claude/commands/refactor.md`:
```markdown
---
name: refactor
description: Выполнить рефакторинг по плану
---

Выполни рефакторинг согласно плану:

1. Прочитай план: .claude/DOCPREP_REFACTORING_PLAN.md
2. Выполни ФАЗУ $ARGUMENTS (или все если не указано)
3. После каждого изменения:
   - Проверь синтаксис: python -m py_compile
   - Запусти тесты: pytest tests/ -v
4. Если тесты падают — исправь
5. Создай коммит с описанием изменений
```

Использование:
```bash
/project:refactor 1    # Только фаза 1
/project:refactor      # Все фазы
```

### 12.2 Сценарий: Test-Driven Development (TDD)

```bash
claude

> Сначала напиши тесты для State Machine:
> - test_valid_transition
> - test_invalid_transition_raises
> - test_transition_with_metadata
> Затем исправь state_machine.py чтобы тесты проходили
```

### 12.3 Сценарий: Code Review с PR

```bash
# Установить GitHub App
/install-github-app

# Настроить ревью
# Claude создаст .github/workflows/claude-code-review.yml
```

Или вручную через subagent:
```bash
> Используй code-reviewer агента для анализа последних изменений
```

### 12.4 Сценарий: CI/CD интеграция

```yaml
# .github/workflows/claude-review.yml
name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Claude Code
        run: curl -fsSL https://claude.ai/install.sh | sh
      
      - name: Review PR
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          claude -p "Review the PR changes in this commit. 
                     Focus on: bugs, security, performance.
                     Output format: markdown" \
            --output-format json \
            --max-turns 5 > review.json
      
      - name: Comment on PR
        uses: actions/github-script@v7
        with:
          script: |
            const review = require('./review.json');
            github.rest.pulls.createReview({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
              body: review.content,
              event: 'COMMENT'
            });
```

---

## 13. Выполнение плана рефакторинга DocPrep

### 13.1 Подготовка

1. **Создайте CLAUDE.md:**
```bash
cp DOCPREP_REFACTORING_PLAN.md .claude/
```

2. **Создайте настройки:**
```json
// .claude/settings.json
{
  "permissions": {
    "allow": [
      "Read",
      "Write(*.py)",
      "Write(*.md)",
      "Edit",
      "Bash(python:*)",
      "Bash(pytest:*)",
      "Bash(black:*)",
      "Bash(isort:*)",
      "Bash(ruff:*)",
      "Bash(git:*)",
      "Bash(grep:*)",
      "Bash(find:*)"
    ],
    "deny": [
      "Bash(rm -rf:*)",
      "Write(.env*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"${TOOL_INPUT_FILE_PATH}\" == *.py ]]; then black \"${TOOL_INPUT_FILE_PATH}\" 2>/dev/null; isort \"${TOOL_INPUT_FILE_PATH}\" 2>/dev/null; fi"
          }
        ]
      }
    ]
  }
}
```

3. **Создайте команду рефакторинга:**

```markdown
# .claude/commands/refactor-phase.md
---
name: refactor-phase
description: Выполнить фазу рефакторинга DocPrep
---

Выполни фазу $ARGUMENTS плана рефакторинга:

1. Прочитай план: .claude/DOCPREP_REFACTORING_PLAN.md
2. Найди секцию "ФАЗА $ARGUMENTS"
3. Выполни все задачи этой фазы
4. После каждого файла:
   - Проверь синтаксис
   - Если есть тесты для этого модуля — запусти
5. После завершения фазы:
   - Запусти полный тест: pytest tests/ -v
   - Проверь что нет опечаток: grep -rn "ErExtact\|ErNormalaze" docprep/
6. Создай коммит: git commit -am "Refactoring Phase $ARGUMENTS: [описание]"
```

### 13.2 Рекомендуемый порядок выполнения

```bash
# Старт сессии
cd docprep
claude

# Фаза 1: Критические исправления (опечатки)
/project:refactor-phase 1

# Фаза 2: Архитектура (logging, state machine, constants)
/project:refactor-phase 2

# Фаза 3: Очистка (удаление мусора, упрощение CLI)
/project:refactor-phase 3

# Фаза 4: Тесты
/project:refactor-phase 4

# Финальная проверка
> Запусти полную проверку: тесты, линтинг, форматирование
> Проверь покрытие тестами
> Создай финальный коммит и тег версии
```

### 13.3 Мониторинг прогресса

```bash
# Проверка контекста
/context

# Проверка стоимости
/status

# Сжатие контекста если нужно
/compact
```

---

## 14. Лучшие практики

### 14.1 Общие принципы

1. **Давайте контекст, а не команды**
   - ❌ "Замени X на Y в файле Z"
   - ✅ "В проекте опечатка в названиях директорий. Найди все вхождения ErExtact и ErNormalaze и исправь на корректные"

2. **Разбивайте большие задачи**
   - ❌ "Сделай весь рефакторинг"
   - ✅ "Выполни фазу 1 плана — исправление опечаток"

3. **Используйте TDD**
   - Сначала тесты, потом реализация
   - Claude лучше работает с чёткими критериями успеха

4. **Проверяйте промежуточные результаты**
   - Запускайте тесты после каждого этапа
   - Используйте hooks для автоматической валидации

### 14.2 Оптимизация контекста

1. **Держите CLAUDE.md компактным** — только существенная информация
2. **Используйте Skills** — загружаются по требованию
3. **Регулярно сжимайте** — команда `/compact`
4. **Удаляйте неиспользуемые MCP** — каждый потребляет контекст

### 14.3 Безопасность

1. **Никогда не храните секреты в CLAUDE.md или Skills**
2. **Используйте переменные окружения** — `${VARIABLE}`
3. **Настройте deny-правила** — для .env, secrets/
4. **В YOLO режиме — только в контейнере**

### 14.4 Командная работа

1. **Коммитьте конфигурацию** — `.claude/`, `.mcp.json`, `CLAUDE.md`
2. **Личные настройки в .local** — `settings.local.json` в .gitignore
3. **Стандартизируйте commands и skills** — единый workflow для команды
4. **Используйте plugins** — пакетирование конфигурации

---

## 📚 Дополнительные ресурсы

- [Официальная документация](https://code.claude.com/docs)
- [Best Practices от Anthropic](https://www.anthropic.com/engineering/claude-code-best-practices)
- [GitHub репозиторий](https://github.com/anthropics/claude-code)
- [CHANGELOG](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)
- [Discord сообщество](https://discord.gg/claude-developers)

---

**Документ создан:** Январь 2026  
**Для проекта:** DocPrep Backend Refactoring  
**Автор:** Senior Developer с использованием Claude