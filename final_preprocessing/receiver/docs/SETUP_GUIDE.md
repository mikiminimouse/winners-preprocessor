# Руководство по настройке VPN и MongoDB

**Дата:** 2025-12-21  
**Статус:** ✅ Все компоненты настроены и работают

## Обзор

Это руководство описывает процесс настройки VPN и MongoDB для работы компонентов receiver.

## Выполненные действия

### 1. ✅ Настройка VPN

#### OpenVPN
- **Процесс:** Запущен
- **Интерфейс:** tun0 поднят (UP, LOWER_UP)
- **Конфигурация:** `/root/winners_preprocessor/final_preprocessing/receiver/vitaly_bychkov.ovpn`
- **Автоматический запуск скрипта:** Добавлена строка `up /root/winners_preprocessor/route-up-zakupki.sh`

#### Маршруты
- ✅ zakupki.gov.ru → через tun0
- ✅ www.zakupki.gov.ru → через tun0  
- ✅ 192.168.0.46 (Remote MongoDB) → через tun0

#### Скрипт route-up-zakupki.sh
- Обновлен для поддержки прямых IP адресов MongoDB
- Автоматически вызывается при поднятии VPN туннеля

### 2. ✅ Проверка доступности

#### zakupki.gov.ru
- **Статус:** ✅ Доступен через VPN
- **Время отклика:** ~4.5 секунд
- **Маршрут:** через tun0

#### Remote MongoDB
- **Адрес:** 192.168.0.46:8635
- **Статус:** ✅ Доступен через VPN
- **Маршрут:** через tun0

#### Local MongoDB
- **Адрес:** localhost:27018 (Docker контейнер)
- **Статус:** ✅ Доступен
- **Конфигурация:** Обновлена в `.env` на порт 27018

### 3. ✅ Health Checks

Результаты финальной проверки:
```
VPN: HEALTHY - Successfully connected to zakupki.gov.ru
REMOTE_MONGODB: HEALTHY - Successfully connected to remote MongoDB
LOCAL_MONGODB: HEALTHY - Successfully connected to local MongoDB
ENVIRONMENT: HEALTHY - All required environment variables are set
SSL_CERTIFICATE: HEALTHY - SSL certificate file is valid and readable
```

### 4. ✅ Код и диагностика

#### Улучшения в `receiver/vpn_utils.py`:
- ✅ `is_openvpn_running()` - проверка процесса OpenVPN
- ✅ `is_vpn_interface_up()` - проверка VPN интерфейса
- ✅ `check_vpn_routes()` - проверка маршрутов к целевым хостам
- ✅ `get_vpn_status()` - полный статус VPN с детальной диагностикой

#### Улучшения в `receiver/sync_db/enhanced_service.py`:
- ✅ Детальная диагностика VPN перед подключением к Remote MongoDB
- ✅ Проверка OpenVPN процесса, интерфейса и маршрутов
- ✅ Понятные сообщения об ошибках с инструкциями

#### Улучшения в `receiver/downloader/enhanced_service.py`:
- ✅ Детальная диагностика VPN перед загрузкой файлов
- ✅ Проверка маршрутов к zakupki.gov.ru

#### Улучшения в `receiver/sync_db/health_checks.py`:
- ✅ Использование новых функций VPN проверки
- ✅ Детальная диагностика проблем с подключением

#### Улучшения в WebUI:
- ✅ Детальное отображение статуса VPN
- ✅ Показ статуса OpenVPN процесса, интерфейса и маршрутов
- ✅ Поле для настройки пути к OpenVPN конфигу

## Референсы из архивов

### Из `archive/pilots/pilot_winers223/docs/technical/vpn_setup.md`:
1. **Автоматический вызов скрипта:**
   - В `.ovpn` файле должна быть строка: `up /root/winners_preprocessor/route-up-zakupki.sh`
   - ✅ Реализовано

2. **Проверка VPN:**
   - `ps aux | grep openvpn` - проверка процесса
   - `ip link show tun0` - проверка интерфейса
   - `ip route | grep zakupki` - проверка маршрутов
   - ✅ Все проверки реализованы в коде

3. **Настройка маршрутов:**
   - `sudo ip route replace <IP>/32 dev tun0 metric 100`
   - ✅ Реализовано в скрипте route-up-zakupki.sh

### Из `archive/preprocessing_docs/`:
1. **Health checks для MongoDB:**
   - Проверка подключения к remote и local MongoDB
   - ✅ Реализовано в `receiver/sync_db/health_checks.py`

2. **VPN для доступа:**
   - VPN требуется для zakupki.gov.ru и Remote MongoDB
   - ✅ Реализовано с детальной диагностикой

3. **Конфигурация из .env:**
   - Загрузка переменных окружения из `.env`
   - ✅ Реализовано в `receiver/core/config.py`

## Измененные файлы

1. **`vitaly_bychkov.ovpn`**
   - Добавлена строка: `up /root/winners_preprocessor/route-up-zakupki.sh`

2. **`route-up-zakupki.sh`**
   - Добавлена поддержка прямых IP адресов для MongoDB

3. **`receiver/.env`**
   - Обновлен `MONGO_METADATA_SERVER` на `localhost:27018`
   - Добавлен `LOCAL_MONGO_SERVER=localhost:27018`
   - Добавлен `VPN_CONFIG_FILE=/root/winners_preprocessor/final_preprocessing/receiver/vitaly_bychkov.ovpn`

## Текущее состояние системы

### VPN
- ✅ OpenVPN запущен
- ✅ Интерфейс tun0 поднят
- ✅ Маршруты настроены
- ✅ zakupki.gov.ru доступен
- ✅ Remote MongoDB доступен

### MongoDB
- ✅ Remote MongoDB (192.168.0.46:8635) доступен через VPN
- ✅ Local MongoDB (localhost:27018) доступен

### WebUI
- ✅ Запущен на http://localhost:7860
- ✅ Health checks работают
- ✅ Детальная диагностика отображается
- ✅ Поле для настройки OpenVPN конфига доступно

## Команды для проверки

### Проверка VPN:
```bash
# Процесс OpenVPN
ps aux | grep openvpn

# Интерфейс
ip link show tun0

# Маршруты
ip route | grep -E "(zakupki|192.168.0.46|tun0)"

# Доступность
curl -I --connect-timeout 5 https://zakupki.gov.ru
```

### Проверка через Python:
```python
from receiver.vpn_utils import get_vpn_status
status = get_vpn_status()
print(status)
```

### Health Checks:
```python
from receiver.sync_db.health_checks import run_comprehensive_health_check
results = run_comprehensive_health_check()
for name, result in results.items():
    print(f"{name}: {result.status} - {result.message}")
```

### Проверка Local MongoDB:
```python
from receiver.sync_db.health_checks import check_local_mongodb_connectivity
result = check_local_mongodb_connectivity()
print(f"Local MongoDB: {result.status}")
print(f"Server: {result.details.get('server')}")
```

## Настройка через WebUI

### Конфигурация VPN
1. Откройте WebUI: `http://localhost:7860`
2. Перейдите на вкладку "⚙️ Configuration"
3. Раскройте секцию "🔒 VPN Configuration"
4. Укажите путь к OpenVPN конфигу в поле `VPN_CONFIG_FILE`
5. Настройте переключатели `VPN_ENABLED_REMOTE_MONGO` и `VPN_ENABLED_ZAKUPKI`
6. Нажмите "💾 Save Configuration to .env"
7. Нажмите "🔄 Перезапустить WebUI" для применения изменений

### Конфигурация MongoDB
1. В секции "🗄️ MongoDB Configuration" укажите:
   - `MONGO_METADATA_SERVER`: `localhost:27018`
   - `LOCAL_MONGO_SERVER`: `localhost:27018`
   - Учетные данные для подключения
2. В секции "🌐 Remote MongoDB (for sync) - Requires VPN" укажите:
   - Адрес удаленного сервера
   - Учетные данные
   - Путь к SSL сертификату
   - Включите `REMOTE_MONGO_USE_VPN`
3. Нажмите "💾 Save Configuration to .env"

## Рекомендации

1. **После перезагрузки сервера:**
   - OpenVPN нужно запускать вручную или через systemd service
   - Маршруты настраиваются автоматически при поднятии туннеля
   - Команда запуска: `sudo openvpn --config /root/winners_preprocessor/final_preprocessing/receiver/vitaly_bychkov.ovpn`

2. **Мониторинг:**
   - Используйте WebUI Health Check для регулярной проверки
   - Все компоненты предоставляют детальную диагностику
   - Проверяйте статусы компонентов на Dashboard

3. **Логирование:**
   - Все проверки VPN логируются с детальной информацией
   - Ошибки содержат конкретные инструкции по исправлению
   - Логи доступны в WebUI Health Check панели

4. **Конфигурация:**
   - Все настройки можно изменить через WebUI Configuration
   - Изменения сохраняются в `receiver/.env`
   - После изменения критических настроек перезапустите WebUI

## Выводы

✅ **Все компоненты настроены и работают корректно:**
- VPN подключение работает
- Маршруты настроены автоматически
- Health checks проходят успешно
- Детальная диагностика доступна в WebUI
- Код готов к работе и предоставляет полную информацию о состоянии системы
- Local MongoDB использует правильный порт (27018)

**Система готова к использованию!** 🎉

