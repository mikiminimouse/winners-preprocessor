# DocPrep Master Control

**Версия:** v2.1.0-STABLE

Панель администратора для управления конвейером предварительной обработки документов с AI-анализом аномалий.

<div align="center">
  <img src="https://img.shields.io/badge/React-19.2.3-61DAFB?logo=react&logoColor=white" alt="React">
  <img src="https://img.shields.io/badge/TypeScript-5.8.2-3178C6?logo=typescript&logoColor=white" alt="TypeScript">
  <img src="https://img.shields.io/badge/Vite-6.2.0-646CFF?logo=vite&logoColor=white" alt="Vite">
  <img src="https://img.shields.io/badge/Gemini_AI-1.34.0-4285F4?logo=google&logoColor=white" alt="Gemini AI">
</div>

## 📋 Описание проекта

**DocPrep Master Control** - это веб-приложение для управления pipeline обработки документов с интеграцией AI-анализа через Google Gemini API. Система предоставляет реальный мониторинг, управление состояниями обработки, аналитику производительности и обнаружение аномалий в логах.

### Ключевые возможности

- 🎯 **Dashboard** - Обзор состояния системы в реальном времени
- 🚀 **Pipeline Run** - Управление запуском и остановкой обработки
- 📁 **Unit Explorer** - Просмотр и управление юнитами документов
- 📊 **Analytics** - Детальная аналитика и визуализация данных
- ⚙️ **System Config** - Настройка параметров системы

## 🏗️ Архитектура

### Стек технологий

```yaml
Frontend:
  - React 19.2.3 с TypeScript
  - Vite 6.2.0 для сборки
  - Recharts 3.6.0 для графиков
  - Lucide React 0.562.0 для иконок

Backend Integration:
  - Google Gemini API (@google/genai 1.34.0)
  - AI-анализ аномалий в логах
  - REST API endpoints для управления
```

### Структура приложения

```
Administrator/
├── components/
│   ├── App.tsx                 # Главный компонент с навигацией
│   ├── Dashboard.tsx           # Панель обзора (33KB)
│   ├── ProcessingControl.tsx   # Управление pipeline (37KB)
│   ├── UnitExplorer.tsx        # Обозреватель юнитов (13KB)
│   ├── Statistics.tsx          # Аналитика (59KB)
│   ├── Settings.tsx            # Настройки (9KB)
│   └── HelloWorld.tsx          # Тестовый компонент (3KB)
├── services/
│   ├── geminiService.ts        # AI-анализ через Gemini API
│   └── helloService.ts         # Тестовый сервис
├── types.ts                    # TypeScript типы данных
├── constants.tsx               # Глобальные константы
├── App.tsx                     # Точка входа приложения
└── index.tsx                   # Рендер приложения
```

## 🚀 Быстрый старт

### Требования

- **Node.js** 18.x или выше
- **npm** или **yarn**
- **GEMINI_API_KEY** от [Google AI Studio](https://ai.google.dev/)

### Установка и запуск

1. **Клонирование репозитория**
   ```bash
   cd Wnr223_webUi/Administrator
   ```

2. **Установка зависимостей**
   ```bash
   npm install
   ```

3. **Настройка API ключа**

   Создайте файл `.env.local` в корне проекта:
   ```env
   GEMINI_API_KEY=ваш_api_ключ_здесь
   ```

   Или установите переменную окружения:
   ```bash
   export GEMINI_API_KEY=ваш_api_ключ_здесь
   ```

4. **Запуск dev сервера**
   ```bash
   npm run dev
   ```

   Приложение будет доступно по адресу: `http://localhost:5173`

5. **Production сборка** (опционально)
   ```bash
   npm run build
   npm run preview
   ```

## 📖 Компоненты системы

### 1. Dashboard

Главная панель с обзором состояния pipeline:

- 📈 Метрики производительности в реальном времени
- 🎯 Статистика успешных и неудачных операций
- 📊 Визуализация данных с Recharts
- ⚡ Индикаторы состояния системы

### 2. Pipeline Run

Управление конвейером обработки:

- ▶️ Запуск/остановка обработки
- 🔄 Мониторинг стадий (CLASSIFY → CONVERT → EXTRACT → NORMALIZE → MERGE)
- ⚠️ Обработка исключений и ошибок
- 📋 Просмотр логов операций

### 3. Unit Explorer

Обозреватель юнитов документов:

- 📂 Древовидная структура файлов
- 🔍 Фильтрация по состояниям (UnitState)
- 🏷️ Категоризация по типам обработки
- 📊 Статистика по расширениям файлов

### 4. Analytics

Детальная аналитика и тренды:

- 📉 Графики производительности по циклам
- 📊 Распределение по категориям обработки
- 🔍 Выявление узких мест
- 💾 Исторические данные

### 5. System Config

Настройки системы:

- 🔑 Управление API ключами
- ⚙️ Параметры pipeline
- 🎨 Настройки интерфейса
- 📝 Конфигурация логирования

## 🎯 Типы данных

### UnitState (состояния обработки)

```typescript
enum UnitState {
  RAW,                      // Исходный файл
  CLASSIFIED_1/2/3,        // Классифицирован
  PENDING_CONVERT,         // Ожидает конвертации
  PENDING_EXTRACT,         // Ожидает извлечения
  PENDING_NORMALIZE,       // Ожидает нормализации
  MERGED_DIRECT,           // Прямое слияние
  MERGED_PROCESSED,        // Обработанное слияние
  READY_FOR_DOCLING,       // Готов к доклинигу
  EXCEPTION_1/2/3,         // Исключения
  MERGER_SKIPPED,          // Пропущено при слиянии
  ER_MERGE                 // Ошибка слияния
}
```

### ProcessingCategory (категории)

```typescript
type ProcessingCategory =
  | 'direct'      // Прямая обработка
  | 'convert'     // Требуется конвертация
  | 'extract'     // Требуется извлечение
  | 'normalize'   // Требуется нормализация
  | 'special'     // Специальная обработка
  | 'mixed'       // Смешанный тип
  | 'unknown'     // Неизвестный тип
  | 'empty';      // Пустой
```

## 🔧 AI-интеграция

### Gemini Service

Сервис `geminiService.ts` предоставляет AI-анализ логов:

```typescript
import { analyzeLogAnomaly } from './services/geminiService';

// Анализ аномалий в логах
const analysis = await analyzeLogAnomaly(logs);
// Возвращает: краткое описание проблем + рекомендации
```

**Возможности:**
- 🔍 Выявление узких мест в pipeline
- ⚠️ Обнаружение повторяющихся ошибок
- 💡 Рекомендации по оптимизации
- 📊 Технический анализ сценариев Decision Engine

## 🛠️ Разработка

### Структура компонента

```typescript
// 1. Импорты
import React, { useState, useEffect } from 'react';

// 2. Типы
interface MyComponentProps {
  // ...
}

// 3. Компонент
const MyComponent: React.FC<MyComponentProps> = ({ ... }) => {
  // 4. Hooks
  const [state, setState] = useState();

  // 5. Event handlers
  const handleClick = () => { ... };

  // 6. Helper functions
  const helper = () => { ... };

  // 7. Render
  return (
    <div>...</div>
  );
};

export default MyComponent;
```

### Конвенции кода

- 📁 **Components:** PascalCase.tsx
- 🔧 **Services:** camelCase.ts
- 📝 **Types:** PascalCase в types.ts
- 🎨 **Constants:** PascalCase.tsx

### Скрипты

```bash
npm run dev      # Dev сервер
npm run build    # Production сборка
npm run preview  # Предпросмотр сборки
```

## 📚 Дополнительная документация

- [Steering документы](../.claude/steering/product.md)
- [Технический контекст](../.claude/steering/tech.md)
- [Структура проекта](../.claude/steering/structure.md)

## 🔐 Безопасность

- ⚠️ Никогда не коммитьте `.env.local` с API ключами
- 🔑 Используйте переменные окружения для чувствительных данных
- 🛡️ API ключи должны храниться в безопасном месте

## 🤝 Поддержка

Для вопросов и проблем:
- Создайте issue в репозитории
- Обратитесь к документации в `/docs`
- Проверьте логи в консоли браузера

## 📄 Лицензия

该项目遵循相应的开源许可证。具体细节请参阅 LICENSE 文件。

---

**Создано с ❤️ для эффективной обработки документов**
