# Code Review Report: Docling Integration
## Финальный анализ проекта перед сдачей

**Дата:** 2026-01-17  
**Версия:** 1.0  
**Статус:** Финальная стадия разработки

---

## 📋 Содержание

1. [Executive Summary](#1-executive-summary)
2. [Роль в микросервисной архитектуре](#2-роль-в-микросервисной-архитектуре)
3. [Анализ архитектуры компонентов](#3-анализ-архитектуры-компонентов)
4. [Детальный анализ Pipeline по форматам](#4-детальный-анализ-pipeline-по-форматам)
5. [Code Review: Критические проблемы](#5-code-review-критические-проблемы)
6. [Code Review: Рекомендации по рефакторингу](#6-code-review-рекомендации-по-рефакторингу)
7. [Мусорный код для удаления](#7-мусорный-код-для-удаления)
8. [Чек-лист перед релизом](#8-чек-лист-перед-релизом)
9. [Заключение и рекомендации](#9-заключение-и-рекомендации)

---

## 1. Executive Summary

### 1.1 Общая оценка проекта

| Аспект | Оценка | Комментарий |
|--------|--------|-------------|
| **Архитектура** | ✅ Хорошо | Модульная структура, чёткое разделение ответственности |
| **Контрактная модель** | ✅ Хорошо | Строгая валидация, единый источник истины |
| **Конфигурация** | ⚠️ Требует доработки | Дублирование YAML + legacy код |
| **Обработка ошибок** | ⚠️ Требует доработки | Quarantine есть, но логирование неполное |
| **Тестирование** | ❌ Недостаточно | Отсутствуют unit-тесты для критических компонентов |
| **Документация** | ✅ Отлично | Полная техническая документация |

### 1.2 Ключевые достижения

1. **Контрактная строгость** — система использует только `docprep.contract.json`, manifest.json исключён из входных данных
2. **Модульность** — чёткое разделение на bridge, config, runner, pipeline, exporters
3. **YAML templates** — гибкая конфигурация для разных типов документов
4. **Quarantine mechanism** — изоляция проблемных документов

### 1.3 Критические проблемы для исправления перед релизом

1. ❌ Неиспользуемые параметры в YAML templates (language.*, некоторые docling.*)
2. ❌ Сложная логика импорта в config.py из-за конфликта имён
3. ❌ Отсутствие unit-тестов
4. ❌ Дублирование конфигурации (YAML + legacy hardcoded)

---

## 2. Роль в микросервисной архитектуре

### 2.1 Позиция в pipeline

```
┌─────────────┐      ┌─────────────────┐      ┌─────────────────────┐      ┌─────────────┐
│   DocPrep   │  →   │  Ready2Docling  │  →   │ Docling Integration │  →   │   Output    │
│  Pipeline   │      │     (UNIT)      │      │     Pipeline        │      │  (RAG/DB)   │
└─────────────┘      └─────────────────┘      └─────────────────────┘      └─────────────┘
     ↓                      ↓                         ↓                         ↓
 Preprocessing        Контрактный             Layout-aware              Structured
 Normalization        интерфейс               Parsing + OCR             Data Export
 Conversion           docprep.contract.json   Semantic Structure        JSON/MD/MongoDB
```

### 2.2 Ключевые принципы

1. **"Docling — двигатель, наш код — водитель"**
   - Не дублируем parsing логику IBM Docling
   - Фокус на orchestration, конфигурации, экспорте

2. **Используем manifest из DocPrep через contract**
   - Не определяем тип файла заново
   - Route приходит из `docprep.contract.json`

3. **Минимальная структура**
   - Только orchestration, конфигурация, экспорт
   - Без кастомных парсеров

### 2.3 Граница ответственности

| Компонент | Ответственность | НЕ делает |
|-----------|-----------------|-----------|
| **DocPrep** | Preprocessing, нормализация, классификация, конвертация (RTF→DOCX и т.д.) | Parsing структуры |
| **Docling Integration** | Orchestration, конфигурация Docling, экспорт | Определение типа файла |
| **IBM Docling** | Layout parsing, OCR, table extraction, semantic structure | Preprocessing |

---

## 3. Анализ архитектуры компонентов

### 3.1 Структура проекта

```
docling_integration/
├── __init__.py
├── pipeline.py          # ✅ Основной orchestration
├── bridge_docprep.py    # ✅ Мост с DocPrep
├── runner.py            # ✅ Запуск DocumentConverter
├── config.py            # ⚠️ Требует рефакторинга
├── constants.py         # ✅ Константы и маппинги
├── pipeline_templates/  # ⚠️ Неиспользуемые параметры
│   ├── pdf_text.yaml
│   ├── pdf_scan.yaml
│   ├── pdf_scan_table.yaml
│   ├── docx.yaml
│   ├── xlsx.yaml
│   ├── pptx.yaml
│   ├── html.yaml
│   ├── xml.yaml
│   └── image_ocr.yaml
├── exporters/
│   ├── json.py          # ✅ Хороший код
│   ├── markdown.py      # ⚠️ Требует улучшения
│   └── mongodb.py       # ✅ Хороший код
├── tests/
│   ├── test_integration.py
│   └── test_pipeline.py
└── docs/                # ✅ Отличная документация
```

### 3.2 Анализ компонента: bridge_docprep.py

**Статус:** ✅ Хорошо

**Сильные стороны:**
- Строгая валидация наличия contract
- Чёткое разделение функций (load_unit, get_main_file)
- Корректная обработка AST nodes

**Код (ключевые моменты):**
```python
# КРИТИЧЕСКИ ВАЖНО: Docling использует ТОЛЬКО contract.json как вход
contract_path = unit_path / "docprep.contract.json"

if not contract_path.exists():
    raise FileNotFoundError(
        f"Contract not found: {contract_path}. "
        f"Unit must have docprep.contract.json to be processed by Docling."
    )
```

**Рекомендации:**
- Добавить JSON Schema валидацию contract
- Расширить логирование для debugging

### 3.3 Анализ компонента: config.py

**Статус:** ⚠️ Требует рефакторинга

**Проблема 1: Сложная логика импорта**
```python
# Текущий код - слишком сложно
site_dirs = []
for site_dir in site.getsitepackages():
    if (Path(site_dir) / 'docling' / '__init__.py').exists():
        if site_dir in sys.path:
            sys.path.remove(site_dir)
        sys.path.insert(0, site_dir)

if 'docling' in sys.modules:
    mod = sys.modules['docling']
    if 'final_preprocessing' in str(mod.__file__):
        for k in list(sys.modules.keys()):
            if k.startswith('docling'):
                sys.modules.pop(k, None)
```

**Рекомендация:** Переименовать модуль в `docling_processor` или использовать абсолютные импорты

**Проблема 2: Дублирование конфигурации**
- YAML templates + legacy hardcoded в `_build_options_legacy()`
- Необходимо удалить legacy код после миграции

**Проблема 3: Неприменяемые параметры YAML**
Многие параметры из YAML templates не маппятся в PipelineOptions:
- `language.*` — не используется
- `performance.threads`, `performance.batch_pages` — не применяются
- `processing_constraints.*` — не применяются

### 3.4 Анализ компонента: runner.py

**Статус:** ✅ Хорошо

**Сильные стороны:**
- Минимальная обёртка над DocumentConverter
- Retry mechanism с настраиваемыми параметрами
- Корректное логирование

**Рекомендации:**
- Добавить метрики времени выполнения
- Добавить timeout handling

### 3.5 Анализ компонента: pipeline.py

**Статус:** ✅ Хорошо с замечаниями

**Сильные стороны:**
- Чёткая orchestration логика
- Quarantine mechanism для проблемных документов
- Гибкие опции экспорта

**Замечания:**
- Отсутствует параллельная обработка
- Нет batch processing для группы файлов

### 3.6 Анализ компонента: exporters/markdown.py

**Статус:** ⚠️ Требует улучшения

**Проблемы:**
- Неполная обработка иерархии документа
- CREF ссылки могут обрабатываться некорректно
- Потеря layout информации

**Рекомендация:**
Использовать встроенный метод Docling `document.export_to_markdown()` вместо кастомной реализации

---

## 4. Детальный анализ Pipeline по форматам

### 4.1 PDF с текстовым слоем (pdf_text)

**Конфигурация:** `pipeline_templates/pdf_text.yaml`

```yaml
route: pdf_text
models:
  layout: publaynet_detectron2    # ✅ Корректный выбор
  tables: table-transformer       # ✅ Корректный выбор  
  ocr: off                        # ✅ Правильно - текст уже есть
```

**Анализ:**
| Аспект | Статус | Комментарий |
|--------|--------|-------------|
| Layout detection | ✅ | publaynet_detectron2 подходит для digital PDF |
| Table extraction | ✅ | table-transformer работает хорошо |
| OCR | ✅ | Правильно отключен |
| Производительность | ⚠️ | ~40-70 PDF/час/CPU — можно оптимизировать |

**Типовые ошибки и их статус:**

| Ошибка | Статус | Решение |
|--------|--------|---------|
| Принудительный OCR | ✅ Решено | `ocr: off` в YAML |
| Потеря таблиц | ✅ Решено | table-transformer включен |
| Смешивание текста и артефактов | ⚠️ Частично | Layout model помогает, но не идеально |

### 4.2 Сканированный PDF (pdf_scan)

**Конфигурация:** `pipeline_templates/pdf_scan.yaml`

```yaml
route: pdf_scan
models:
  ocr: tesseract                 # ⚠️ Нет настройки языка rus
  layout: docbank                # ✅ Подходит для сканов
  tables: off                    # ⚠️ Спорное решение
```

**Анализ:**
| Аспект | Статус | Комментарий |
|--------|--------|-------------|
| OCR engine | ⚠️ | Tesseract без явной настройки `rus` |
| Layout detection | ✅ | docbank хорош для сканов |
| Table extraction | ❌ | Отключено — потеря таблиц |
| Производительность | ✅ | ~10-20 PDF/час/CPU — ожидаемо |

**Критические проблемы:**

1. **❌ OCR не настроен для русского языка**
   ```yaml
   # Текущее
   docling:
     language: eng   # ← Проблема!
   
   # Нужно
   docling:
     language: rus
   ```

2. **⚠️ Таблицы отключены в pdf_scan**
   - Если в сканах есть таблицы — они теряются
   - Решение: использовать pdf_scan_table для документов с таблицами

### 4.3 Сканированный PDF с таблицами (pdf_scan_table)

**Конфигурация:** `pipeline_templates/pdf_scan_table.yaml`

```yaml
route: pdf_scan_table
models:
  ocr: tesseract
  layout: docbank
  tables: table-transformer       # ✅ Таблицы включены
```

**Статус:** ✅ Корректная конфигурация

**Замечание:** Самый тяжёлый workload (~4-8 PDF/час/CPU)

### 4.4 Смешанные PDF (pdf_mixed)

**Статус:** ⚠️ КРИТИЧЕСКАЯ ЗОНА РИСКА

**Проблема:** В документации указано, что route="mixed" фильтруется в merger и не попадает в Ready2Docling. Однако механизм определения страниц, требующих OCR, внутри одного PDF не реализован.

**Текущее поведение:**
- UNIT с route="mixed" НЕ попадают в Ready2Docling
- Это приводит к потере таких документов

**Рекомендация:**
Реализовать page-level OCR detection в Docling:
```python
# Псевдокод желаемого поведения
for page in document.pages:
    if page.has_text_layer():
        extract_text(page)
    else:
        run_ocr(page)
```

### 4.5 DOCX (нативный формат)

**Конфигурация:** `pipeline_templates/docx.yaml`

```yaml
route: docx
models:
  layout: native
  tables: native
  ocr: off
```

**Статус:** ✅ Отлично

**Производительность:** ~100-200 документов/час/CPU

**Результаты тестирования (из документации):**
```
| Формат | Кол-во | Success | Время avg |
|--------|--------|---------|-----------|
| DOCX   | 17     | 100%    | 0.16 сек  |
```

### 4.6 XLSX (нативный формат)

**Конфигурация:** `pipeline_templates/xlsx.yaml`

**Статус:** ✅ Отлично

**Результаты тестирования:**
```
| XLSX | 1 | 100% | 0.34 сек |
```

### 4.7 PPTX (нативный формат)

**Конфигурация:** `pipeline_templates/pptx.yaml`

**Статус:** ✅ Корректная конфигурация

**Замечание:** Структура слайдов может теряться при экспорте в Markdown

### 4.8 HTML / XML

**Статус:** ✅ Хорошо

**Производительность:** ~150-300 документов/час/CPU

### 4.9 Изображения (image_ocr)

**Конфигурация:** `pipeline_templates/image_ocr.yaml`

```yaml
route: image_ocr
models:
  ocr: tesseract
  layout: docbank
```

**Статус:** ⚠️ Требует проверки языковых настроек

---

## 5. Code Review: Критические проблемы

### 5.1 Критическая проблема #1: Язык OCR

**Файл:** `pipeline_templates/pdf_scan.yaml`

**Проблема:**
```yaml
docling:
  language: eng  # ❌ Для русских документов некорректно
```

**Решение:**
```yaml
docling:
  language: rus
  # Или fallback логика
  # language_primary: rus
  # language_fallback: eng
```

**Приоритет:** 🔴 КРИТИЧЕСКИЙ

### 5.2 Критическая проблема #2: Конфликт имён модуля

**Файл:** `config.py`

**Проблема:** Модуль `docling_integration` конфликтует с библиотекой `docling`

**Текущий workaround (плохой код):**
```python
# 50+ строк манипуляции с sys.path и sys.modules
site_dirs = []
for site_dir in site.getsitepackages():
    # ...сложная логика...
```

**Решение:**
1. Переименовать директорию `docling_integration` в `docling_processor`
2. Или использовать namespace packages
3. Или явные абсолютные импорты с alias:
   ```python
   import docling as ibm_docling
   ```

**Приоритет:** 🔴 КРИТИЧЕСКИЙ (архитектурный долг)

### 5.3 Критическая проблема #3: Неприменяемые параметры YAML

**Проблема:** Параметры указаны в YAML, но не используются в `_build_options_from_template()`

**Список неприменяемых параметров:**
```yaml
# Эти параметры НЕ используются:
performance:
  threads: 4          # ❌ Не применяется
  batch_pages: 8      # ❌ Не применяется

language:
  primary: ru         # ❌ Не применяется
  fallback: en        # ❌ Не применяется

processing_constraints:
  allow_gpu: false    # ❌ Не применяется
  max_runtime_sec: 180  # ❌ Не применяется
```

**Решение:** Либо удалить, либо реализовать применение

**Приоритет:** 🟡 СРЕДНИЙ

### 5.4 Критическая проблема #4: Отсутствие unit-тестов

**Текущее состояние:**
- `tests/test_integration.py` — интеграционный тест
- `tests/test_pipeline.py` — тест pipeline

**Отсутствуют:**
- Unit-тесты для `bridge_docprep.py`
- Unit-тесты для `config.py`
- Unit-тесты для exporters

**Приоритет:** 🟡 СРЕДНИЙ (но важно перед production)

---

## 6. Code Review: Рекомендации по рефакторингу

### 6.1 Рефакторинг config.py

**Текущие проблемы:**
1. Сложная логика импорта (50+ строк)
2. Дублирование: YAML templates + legacy hardcoded
3. Неприменяемые параметры

**Рекомендуемый рефакторинг:**

```python
# БЫЛО (плохо):
def build_docling_options(route, manifest=None):
    template = load_pipeline_template(route)
    if template:
        return _build_options_from_template(template)
    return _build_options_legacy(route, manifest)  # ← Удалить

# ДОЛЖНО БЫТЬ (хорошо):
def build_docling_options(route: str) -> PipelineOptions:
    """
    Строит PipelineOptions из YAML template.
    
    Raises:
        ValueError: если template не найден
    """
    template = load_pipeline_template(route)
    if not template:
        raise ValueError(f"Template not found for route: {route}")
    return _build_options_from_template(template)
```

**Удалить функцию:** `_build_options_legacy()`

### 6.2 Рефакторинг exporters/markdown.py

**Текущая проблема:** Кастомная реализация экспорта, которая может терять структуру

**Рекомендация:** Использовать встроенный метод Docling

```python
# БЫЛО (кастомная реализация):
def export_to_markdown(document, output_path, metadata=None):
    content = _extract_structured_content(document)
    # ... сложная логика ...

# ДОЛЖНО БЫТЬ (использовать Docling):
def export_to_markdown(document, output_path, metadata=None):
    """
    Экспорт через встроенный метод Docling.
    """
    md_content = document.export_to_markdown()
    
    # Добавляем метаданные в начало
    if metadata:
        header = _format_metadata_header(metadata)
        md_content = header + "\n\n" + md_content
    
    output_path.write_text(md_content, encoding='utf-8')
```

### 6.3 Добавление параллельной обработки

**Файл:** `pipeline.py`

**Текущее:** Последовательная обработка

**Рекомендация:** Добавить ThreadPoolExecutor

```python
from concurrent.futures import ThreadPoolExecutor, as_completed

def process_directory_parallel(self, ready2docling_dir: Path, 
                                max_workers: int = 4,
                                limit: Optional[int] = None) -> Dict[str, Any]:
    """
    Параллельная обработка UNIT в директории.
    """
    units = list(ready2docling_dir.rglob("UNIT_*"))
    if limit:
        units = units[:limit]
    
    results = []
    with ThreadPoolExecutor(max_workers=max_workers) as executor:
        future_to_unit = {
            executor.submit(self.process_unit, unit): unit 
            for unit in units
        }
        
        for future in as_completed(future_to_unit):
            unit = future_to_unit[future]
            try:
                result = future.result()
                results.append(result)
            except Exception as e:
                logger.error(f"Error processing {unit}: {e}")
                if self.quarantine_dir:
                    self._quarantine_unit(unit, str(e))
    
    return self._aggregate_results(results)
```

---

## 7. Мусорный код для удаления

### 7.1 Файлы для удаления

```
# Legacy код и устаревшие файлы

# 1. В config.py - удалить функцию _build_options_legacy()
#    после полной миграции на YAML templates

# 2. Архивные файлы (если есть):
archive/docling/processor.py          # Монолитная версия
archive/docling/experiments/          # Эксперименты
```

### 7.2 Код для удаления в config.py

```python
# УДАЛИТЬ эту функцию полностью:
def _build_options_legacy(route: str, manifest: Optional[Dict[str, Any]] = None) -> PipelineOptions:
    """
    Legacy hardcoded конфигурация.
    DEPRECATED: Использовать YAML templates.
    """
    # ... весь код функции ...
```

### 7.3 Неиспользуемые параметры в YAML templates

**Удалить из всех YAML templates:**
```yaml
# Удалить эти секции (не применяются):
performance:
  threads: 4
  batch_pages: 8

language:
  primary: ru
  fallback: en
  encoding: utf-8

processing_constraints:
  allow_gpu: false
  max_runtime_sec: 180
  max_memory_mb: 4096
```

**ИЛИ** реализовать их применение в `_build_options_from_template()`

### 7.4 Дублирующиеся импорты

**В config.py проверить и очистить:**
```python
# Убедиться, что нет дублирующихся импортов после рефакторинга
```

---

## 8. Чек-лист перед релизом

### 8.1 Критические задачи (MUST HAVE)

- [ ] Исправить язык OCR в pdf_scan.yaml (eng → rus)
- [ ] Решить конфликт имён модуля docling_integration
- [ ] Удалить или пометить deprecated функцию `_build_options_legacy()`
- [ ] Проверить, что все YAML templates загружаются корректно
- [ ] Провести smoke test на реальных документах всех форматов

### 8.2 Важные задачи (SHOULD HAVE)

- [ ] Добавить unit-тесты для bridge_docprep.py
- [ ] Добавить unit-тесты для config.py
- [ ] Улучшить markdown экспорт (использовать встроенный метод)
- [ ] Удалить неиспользуемые параметры из YAML templates
- [ ] Добавить логирование времени обработки каждого этапа

### 8.3 Желательные задачи (COULD HAVE)

- [ ] Добавить параллельную обработку
- [ ] Реализовать page-level OCR detection для смешанных PDF
- [ ] Добавить метрики и мониторинг
- [ ] Интегрировать Cloud.ru VLM для улучшения OCR

### 8.4 Результаты текущего тестирования

Из документации проекта:

```
| Формат    | Кол-во | Success | Время avg   |
|-----------|--------|---------|-------------|
| DOCX      | 17     | 100%    | 0.16 сек    |
| XLSX      | 1      | 100%    | 0.34 сек    |
| PDF (dig) | 5      | 100%    | ~68 сек     |
| Image OCR | 3      | 100%    | 107 сек     |
```

**Вывод:** Основной pipeline работает стабильно.

---

## 9. Заключение и рекомендации

### 9.1 Общая оценка готовности

**Готовность к production:** 🟡 80% (требуются доработки)

**Блокеры релиза:**
1. Некорректный язык OCR для русских документов
2. Конфликт имён модуля (технический долг)

### 9.2 Приоритизированный план действий

**Неделя 1 (критические исправления):**
1. Исправить язык OCR в YAML templates
2. Решить конфликт имён модуля (переименование)
3. Smoke testing всех форматов

**Неделя 2 (стабилизация):**
1. Удалить legacy код
2. Добавить unit-тесты
3. Очистить неиспользуемые параметры YAML

**Неделя 3 (оптимизация):**
1. Параллельная обработка
2. Улучшение markdown экспорта
3. Мониторинг и метрики

### 9.3 Сильные стороны проекта

1. ✅ Отличная архитектура с чётким разделением ответственности
2. ✅ Строгая контрактная модель
3. ✅ Полная техническая документация
4. ✅ Quarantine механизм для обработки ошибок
5. ✅ Гибкая конфигурация через YAML templates

### 9.4 Области для улучшения

1. ⚠️ Тестовое покрытие
2. ⚠️ Поддержка русского языка в OCR
3. ⚠️ Параллельная обработка
4. ⚠️ Обработка смешанных PDF

### 9.5 Финальная рекомендация

**Проект готов к beta-релизу** при условии исправления критических проблем с языком OCR и конфликтом имён. Полный production-релиз рекомендуется после добавления unit-тестов и проведения нагрузочного тестирования.

---

## Приложение A: Матрица pipeline по форматам

| Формат | Preprocess | Docling | OCR | Layout Model | Table Model | Риски |
|--------|------------|---------|-----|--------------|-------------|-------|
| PDF (text) | normalize | ✅ | ❌ | publaynet_detectron2 | table-transformer | layout loss |
| PDF (scan) | normalize | ✅ | ✅ tesseract | docbank | off | OCR lang |
| PDF (scan+table) | normalize | ✅ | ✅ tesseract | docbank | table-transformer | OCR quality |
| PDF (mixed) | normalize | ❌ | N/A | N/A | N/A | **Не поддерживается** |
| DOCX | minimal | ✅ | ❌ | native | native | tables |
| XLSX | minimal | ✅ | ❌ | native | native | minimal |
| PPTX | minimal | ✅ | ❌ | native | native | structure |
| HTML | minimal | ✅ | ❌ | native | native | minimal |
| XML | minimal | ✅ | ❌ | native | native | minimal |
| Image | normalize | ✅ | ✅ tesseract | docbank | off | OCR quality |
| RTF | **конвертирован в DocPrep** | ✅ (как DOCX) | ❌ | native | native | minimal |

---

**Конец отчёта**

*Отчёт подготовлен на основании анализа проектной документации и кода Docling Integration.*