# 🚀 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ - НОВЫЕ МИКРОСЕРВИСЫ

## ✅ ИСПРАВЛЕННЫЕ ПРОБЛЕМЫ

1. **SyncResult.success AttributeError** - ✅ исправлено
2. **Переменные окружения MongoDB** - ✅ исправлено (автозагрузка из .env)
3. **Импорты в CLI** - ✅ исправлено
4. **Основной CLI** - ⚠️ имеет проблемы с импортами, используйте микросервисы напрямую

## 🎯 КАК ТЕСТИРОВАТЬ СЕЙЧАС

### 1. ПРЯМАЯ РАБОТА С МИКРОСЕРВИСАМИ (РЕКОМЕНДУЕТСЯ)

```bash
cd /root/winners_preprocessor

# Синхронизация протоколов из MongoDB
python -m services.sync_microservice.cli sync-daily --limit 10

# Проверка доступности zakupki.gov.ru
python -m services.downloader_microservice.cli --health-check

# Скачивание протоколов (если есть данные в MongoDB)
python -m services.downloader_microservice.cli --limit 5
```

### 2. РАБОТА ЧЕРЕЗ VENV СКРИПТ (ОПЦИОНАЛЬНО)

```bash
cd /root/winners_preprocessor
source preprocessing/activate_venv.sh

# Теперь можно использовать CLI микросервисов
python -m services.sync_microservice.cli sync-daily --limit 10
python -m services.downloader_microservice.cli --health-check
```

### 3. ОСНОВНОЙ CLI (ИМЕЕТ ПРОБЛЕМЫ)

Основной CLI (`python preprocessing/cli/main.py`) имеет проблемы с импортами и не рекомендуется для использования. Используйте микросервисы напрямую.

## 📊 ТЕКУЩИЙ СТАТУС

### ✅ РАБОТАЮЩИЕ КОМПОНЕНТЫ:
- **Sync Microservice**: Подключается к локальной MongoDB ✅
- **Downloader Microservice**: Проверяет доступность zakupki.gov.ru ✅
- **Переменные окружения**: Загружаются автоматически ✅
- **CLI интерфейсы**: Работают напрямую ✅

### ⚠️ ОГРАНИЧЕНИЯ:
- **Удаленная MongoDB**: Требует VPN для доступа
- **Реальные данные**: Нужны данные протоколов в MongoDB для полного тестирования
- **Основной CLI**: Имеет проблемы с импортами (используйте микросервисы напрямую)

## 🔄 СЛЕДУЮЩИЕ ШАГИ ДЛЯ ПОЛНОГО ТЕСТИРОВАНИЯ

1. **Настройка VPN** для доступа к удаленной MongoDB
2. **Заполнение MongoDB** тестовыми данными протоколов
3. **Тестирование полного цикла**: синхронизация → скачивание → обработка

## 📝 ПРИМЕРЫ КОМАНД

```bash
# Синхронизация за вчерашний день
python -m services.sync_microservice.cli sync-daily --limit 100

# Синхронизация за конкретную дату
python -m services.sync_microservice.cli sync-date --date 2025-12-16 --limit 50

# Полная синхронизация за 7 дней
python -m services.sync_microservice.cli sync-full --days 7 --limit 1000

# Проверка доступности сайта
python -m services.downloader_microservice.cli --health-check

# Скачивание протоколов
python -m services.downloader_microservice.cli --limit 10
```

## 🎉 ВЫВОД

Теперь микросервисы работают корректно и готовы к тестированию! Основные компоненты:
- ✅ Локальная MongoDB подключается
- ✅ zakupki.gov.ru доступен
- ✅ Переменные окружения загружаются
- ✅ CLI интерфейсы функциональны

Для полного тестирования нужен VPN доступ к удаленной MongoDB и тестовые данные.
