# 📋 ИНСТРУКЦИЯ: ЗАПУСК И ТЕСТИРОВАНИЕ CLI МЕНЮ

## 🚀 ЗАПУСК CLI

### Вариант 1: Локальный запуск (рекомендуется для тестирования)

```bash
# Переходим в директорию preprocessing
cd /root/winners_preprocessor/preprocessing

# Активируем виртуальное окружение
source activate_venv.sh

# Запускаем CLI
python run_cli.py
```

### Вариант 2: Запуск через Docker

```bash
# Из корневой директории проекта
cd /root/winners_preprocessor

# Запускаем CLI сервис в Docker
docker-compose -f preprocessing/docker-compose.yml up cli

# Или если сервисы уже запущены
docker-compose -f preprocessing/docker-compose.yml exec cli python /app/cli.py
```

## 📋 СТРУКТУРА МЕНЮ

После запуска вы увидите меню:

```
============================================================
=== ПРЕПРОЦЕССИНГ ДОКУМЕНТОВ - CLI ТЕСТИРОВАНИЯ ===
============================================================

=== ЗАГРУЗКА И ПОДГОТОВКА ДАННЫХ ===
1. Синхронизация протоколов из MongoDB
2. Скачивание протоколов за дату
3. Проверка доступности файлов в INPUT_DIR

=== ТЕСТИРОВАНИЕ ЭТАПОВ ПРЕПРОЦЕССИНГА ===
4. ТЕСТ 1: Определение типа файла
5. ТЕСТ 2: Распаковка архивов
6. ТЕСТ 3: Нормализация unit'ов
7. ТЕСТ 4: Создание manifest'ов
8. ТЕСТ 5: Docling обработка

=== ПОШАГОВАЯ ОБРАБОТКА ===
9. ШАГ 1: Сканирование и детекция типов файлов
10. ШАГ 2: Классификация файлов по категориям
11. ШАГ 3: Проверка дубликатов
12. ШАГ 4: Определение mixed units
13. ШАГ 5: Распределение по pending директориям
14. ПОЛНАЯ ОБРАБОТКА: Все шаги (1-5)

=== РАСШИРЕННАЯ СТАТИСТИКА ===
15. Просмотр структуры pending директорий
16. Детальная статистика по категориям
17. Отчет по обработанным units

=== MERGE И ФИНАЛИЗАЦИЯ ===
18. Merge (DRY RUN)
19. Merge (РЕАЛЬНЫЙ)

=== ПОЛНОЕ ТЕСТИРОВАНИЕ PIPELINE ===
20. ПОЛНЫЙ ТЕСТ: Весь pipeline (шаги 1-5)
21. ИНТЕГРАЦИОННЫЙ ТЕСТ: Router API

=== МОНИТОРИНГ И СТАТИСТИКА ===
22. Просмотр текущих метрик сессии
23. Просмотр логов обработки
24. Статус MongoDB подключений

=== СЛУЖЕБНЫЕ ФУНКЦИИ ===
25. Очистка тестовых данных
26. Создание тестовых файлов
27. Проверка инфраструктуры
0. Выход
```

## 🧪 ТЕСТИРОВАНИЕ ОСНОВНЫХ ФУНКЦИЙ

### Тест 1: Проверка инфраструктуры (рекомендуется начать с этого)

```bash
# Выберите опцию 27: "Проверка инфраструктуры"
27
```

**Что проверяется:**
- ✅ Доступность директорий (input, temp, output, etc.)
- ✅ MongoDB подключения (локальная и удаленная)
- ✅ VPN доступ к zakupki.gov.ru
- ✅ Наличие необходимых микросервисов

### Тест 2: Синхронизация протоколов

```bash
# Выберите опцию 1: "Синхронизация протоколов из MongoDB"
1
```

**Что происходит:**
- 🔍 Проверяет подключение к локальной MongoDB
- 🔍 Проверяет подключение к удаленной MongoDB
- 📅 Предлагает выбрать тип синхронизации:
  - Одна дата (вчера по умолчанию)
  - Диапазон дат
  - Начальная дата + количество дней
- 🔢 Запрашивает лимит протоколов (200 по умолчанию)
- 🚀 Синхронизирует документы из `protocols223.purchaseProtocol` в `docling_metadata.protocols`

**Ожидаемый результат:**
```
✅ Подключение к локальной MongoDB успешно
✅ Подключение к удалённой MongoDB успешно
✅ Индексы в локальной MongoDB созданы

🚀 ЗАПУСК СИНХРОНИЗАЦИИ ПРОТОКОЛОВ
Дата: 2025-12-17
Лимит: 200

✅ СИНХРОНИЗАЦИЯ ЗАВЕРШЕНА УСПЕШНО!
📊 РЕЗУЛЬТАТЫ:
   📅 Дата/Период: 2025-12-17
   🔍 Просмотрено документов: 200
   💾 Новых протоколов: 150
   ⏭️  Пропущено дубликатов: 50
   ⚠️  Ошибок обработки: 0
   ⏱️  Длительность: 5.08 секунд
```

### Тест 3: Скачивание протоколов

```bash
# Выберите опцию 2: "Скачивание протоколов за дату"
2
```

**Что происходит:**
- 🌐 Проверяет VPN доступ к zakupki.gov.ru
- 🔢 Запрашивает лимит для скачивания (200 по умолчанию)
- 📁 Скачивает документы в директорию input

**Ожидаемый результат:**
```
=== СКАЧИВАНИЕ ПРОТОКОЛОВ ИЗ MONGODB (С VPN) ===

1. Проверка доступности zakupki.gov.ru через VPN...
✓ zakupki.gov.ru доступен через VPN

2. Лимит протоколов/units для скачивания (по умолчанию: 200): 100

3. Запуск скачивания протоколов...
   Лимит: 100 протоколов
   Директория: /app/input

✓ Скачивание завершено!
   Время: 45.2 сек
   Протоколы обработано: 95
   Документы скачано: 245
   Ошибок: 5
```

### Тест 4: Проверка файлов в INPUT_DIR

```bash
# Выберите опцию 3: "Проверка доступности файлов в INPUT_DIR"
3
```

**Показывает:**
- 📁 Содержимое input директории
- 📊 Статистику по типам файлов
- 🔍 Детальную информацию о каждом файле

## 🔧 ДОПОЛНИТЕЛЬНЫЕ КОМАНДЫ ДЛЯ ТЕСТИРОВАНИЯ

### Автоматическое тестирование с предустановленными ответами

```bash
# Тест синхронизации с лимитом 100
cd /root/winners_preprocessor/preprocessing
source activate_venv.sh
echo -e "1\n\n\n100\n" | python run_cli.py

# Тест скачивания с лимитом 50
echo -e "2\n50\n" | python run_cli.py

# Тест полной проверки инфраструктуры
echo -e "27\n" | python run_cli.py
```

### Проверка логов после выполнения

```bash
# Просмотр логов обработки (опция 23)
echo -e "23\n" | python run_cli.py

# Просмотр метрик сессии (опция 22)
echo -e "22\n" | python run_cli.py
```

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### Перед запуском убедитесь:

1. **VPN настроен** (для скачивания протоколов)
2. **MongoDB подключения работают**
3. **Переменные окружения загружены** (автоматически через `activate_venv.sh`)
4. **Все микросервисы доступны**

### Возможные проблемы:

- **❌ "Модули скачивания не доступны"** → Проверьте импорты, перезапустите venv
- **❌ "Не все параметры локальной Mongo заданы"** → Проверьте `.env` файл
- **❌ "zakupki.gov.ru недоступен"** → Проверьте VPN подключение

### Очистка после тестирования:

```bash
# Очистка тестовых данных (опция 25)
echo -e "25\n" | python run_cli.py
```

## 🎯 БЫСТРЫЙ СТАРТ ДЛЯ ТЕСТИРОВАНИЯ

```bash
cd /root/winners_preprocessor/preprocessing
source activate_venv.sh

# 1. Проверка инфраструктуры
echo -e "27\n" | python run_cli.py

# 2. Синхронизация протоколов (маленький лимит для теста)
echo -e "1\n\n\n50\n" | python run_cli.py

# 3. Скачивание (если VPN работает)
echo -e "2\n10\n" | python run_cli.py

# 4. Проверка результатов
echo -e "3\n" | python run_cli.py
```

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ

При успешном выполнении всех функций вы должны увидеть:
- ✅ Все подключения к базам данных работают
- ✅ Синхронизация протоколов завершается без ошибок E11000
- ✅ Скачивание работает через VPN
- ✅ Файлы появляются в соответствующих директориях
- ✅ Нет ошибок импорта модулей
