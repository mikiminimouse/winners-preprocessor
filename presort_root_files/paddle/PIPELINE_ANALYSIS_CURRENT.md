# Анализ текущего состояния Pipeline PaddleOCR-VL

**Дата:** 2025-12-14  
**Версия образа:** `docling-granite-258m.cr.cloud.ru/paddleocr-vl-service:2.0.13`  
**SHA256:** `7dafb63c32b5` (или `4a54a26641b8970ddea53861bc8522453ca803fbbf3dcf885235c1dc67c2615d`)

---

## 📋 Текущее состояние проекта

### Архитектура Pipeline

```
Входное изображение (Base64/URL/Multipart)
    ↓
FastAPI Endpoint: POST /ocr
    ↓
[1] Подготовка изображения
    ├─ Base64 → decode → PIL Image → temp file
    ├─ URL → download → PIL Image → temp file
    └─ Multipart → save → temp file
    ↓
[2] PaddleOCR-VL Processing
    ├─ init_paddleocr() - ленивая инициализация
    ├─ ocr.predict(image_path) - основной вызов
    └─ Результат: список объектов с данными документа
    ↓
[3] Сохранение результатов
    ├─ save_results_locally()
    │   ├─ res.save_to_markdown() → Markdown файл
    │   └─ res.save_to_json() → JSON файл
    └─ upload_to_cloudru() → Cloud.ru S3 (опционально)
    ↓
[4] Возврат результата
    └─ JSON с путями к файлам и опционально содержимым
```

---

## 🔍 Детальный анализ компонентов

### 1. Docker образ

**Базовый образ:**
```dockerfile
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest-gpu-sm120-offline
```

**Особенности:**
- ✅ Офлайн-образ с предзагруженными моделями
- ✅ Модели PaddleOCR-VL и PP-DocLayoutV2 уже включены
- ✅ Не требуется доступ к интернету в runtime
- ✅ Размер образа: ~12.4GB

**Проблемы в Dockerfile:**
- ❌ **КРИТИЧНО:** Используются старые пути `paddle_docker_servise/` вместо `ocr_vl/service/`
  - Строка 13: `COPY paddle_docker_servise/requirements.txt`
  - Строка 68: `COPY paddle_docker_servise/server.py`

**Модели в образе:**
- `PaddleOCR-VL-0.9B` - основная модель OCR
- `PP-DocLayoutV2` - анализ макета документа
- Расположение: `/home/paddleocr/.paddlex/official_models/`

---

### 2. PaddleOCR-VL Pipeline

#### Инициализация (ленивая)

```python
def init_paddleocr():
    """Инициализация PaddleOCR-VL pipeline"""
    global paddle_ocr
    if paddle_ocr is None:
        paddle_ocr = PaddleOCRVL()
    return paddle_ocr
```

**Особенности:**
- Ленивая инициализация при первом запросе
- Фоновая инициализация при старте сервера (с задержкой 10 секунд)
- Настройка PaddlePaddle для dynamic graph mode
- Пути к моделям: `/home/paddleocr/.paddlex`

#### Обработка изображения

```python
def process_with_paddleocr(image_path: Union[str, Path]) -> Any:
    """Обрабатывает изображение через PaddleOCR-VL"""
    ocr = init_paddleocr()
    result = ocr.predict(str(image_path))
    # Обработка результата (list или iterator)
    return result_list
```

**Что делает PaddleOCR-VL:**
1. **Layout Detection** (PP-DocLayoutV2)
   - Определение блоков документа
   - Распознавание заголовков, параграфов, таблиц
   - Определение reading order

2. **OCR Recognition** (PaddleOCR-VL-0.9B)
   - Распознавание текста в каждом блоке
   - Поддержка русского языка
   - Обработка таблиц и сложных структур

3. **Markdown Generation**
   - Автоматическая генерация структурированного Markdown
   - Сохранение структуры документа
   - Обработка таблиц в Markdown формате

---

### 3. Сохранение результатов

#### Метод save_results_locally()

```python
def save_results_locally(results: Any, output_dir: Path, base_filename: str):
    """Сохраняет результаты в Markdown и JSON"""
    for res in results_list:
        # Markdown
        res.save_to_markdown(save_path=temp_dir)
        # JSON
        res.save_to_json(save_path=temp_dir)
```

**Процесс:**
1. Создает временные директории для каждого результата
2. Вызывает `res.save_to_markdown()` - официальный метод PaddleOCR-VL
3. Вызывает `res.save_to_json()` - официальный метод PaddleOCR-VL
4. Объединяет результаты в финальные файлы
5. Сохраняет с timestamp в имени файла

**Формат выходных файлов:**
- Markdown: `{timestamp}_{base_filename}.md`
- JSON: `{timestamp}_{base_filename}.json`
- Расположение: `/app/output/`

**Fallback механизмы:**
- Если `save_to_markdown()` не работает → пробует `to_markdown()`
- Если `save_to_json()` не работает → пробует `to_json()`
- Если ничего не работает → сохраняет строковое представление

---

### 4. API Endpoints

#### POST /ocr

**Поддерживаемые форматы входных данных:**
1. **Base64:** `image_base64` (Form parameter)
2. **URL:** `image_url` (Form parameter)
3. **Multipart:** `file` (File upload)

**Параметры:**
- `return_content` (bool) - вернуть содержимое в ответе

**Ответ:**
```json
{
  "status": "success",
  "input_type": "multipart|base64|url",
  "local_files": {
    "markdown": "/app/output/20251214_120000_image.md",
    "json": "/app/output/20251214_120000_image.json"
  },
  "s3_files": {
    "status": "uploaded",
    "markdown": "https://bucket-winners223.s3.cloud.ru/...",
    "json": "https://bucket-winners223.s3.cloud.ru/..."
  },
  "timestamp": "2025-12-14T12:00:00",
  "elapsed_sec": 5.234
}
```

#### Другие endpoints:
- `GET /health` - проверка здоровья сервиса
- `GET /files` - список сохраненных файлов
- `GET /files/{file_type}/{filename}` - получение файла
- `POST /test/s3-upload` - тестовая загрузка в S3

---

## ✅ Что работает

1. **Инициализация PaddleOCR-VL**
   - ✅ Ленивая инициализация работает
   - ✅ Фоновая инициализация при старте
   - ✅ Модели загружаются из офлайн-образа

2. **Обработка изображений**
   - ✅ Поддержка Base64, URL, Multipart
   - ✅ Вызов `ocr.predict()` работает
   - ✅ Обработка результатов (list/iterator)

3. **Сохранение результатов**
   - ✅ Использование официальных методов `save_to_markdown()` и `save_to_json()`
   - ✅ Fallback механизмы для совместимости
   - ✅ Локальное сохранение работает

4. **S3 интеграция**
   - ✅ Загрузка в Cloud.ru Object Storage
   - ✅ Правильная конфигурация для Cloud.ru S3
   - ✅ Проверка загрузки

---

## ⚠️ Проблемы и ограничения

### Критические проблемы

1. **Dockerfile использует старые пути**
   ```dockerfile
   COPY paddle_docker_servise/requirements.txt  # ❌ Неправильный путь
   COPY paddle_docker_servise/server.py          # ❌ Неправильный путь
   ```
   **Решение:** Обновить пути на `ocr_vl/service/`

2. **Неопределенность с SHA256 образа**
   - Указан SHA256: `4a54a26641b8970ddea53861bc8522453ca803fbbf3dcf885235c1dc67c2615d`
   - Но локально есть образ с SHA256: `7dafb63c32b5`
   - Нужно проверить какой образ реально используется

### Потенциальные проблемы

1. **Обработка ошибок save_to_markdown/json**
   - Fallback на `to_markdown()` может не сохранять полную структуру
   - Нужно проверить качество fallback результатов

2. **Производительность**
   - Ленивая инициализация может задержать первый запрос
   - Нет кэширования результатов для одинаковых изображений

3. **Ограничения PaddleOCR-VL**
   - Batch size = 1 (только одно изображение за раз)
   - Требуется GPU для работы
   - Модель 0.9B может быть недостаточной для сложных документов

---

## 🔄 Текущий Pipeline Flow

### Детальный поток обработки

```
[1] Запрос POST /ocr
    │
    ├─ [1.1] Определение типа входных данных
    │   ├─ Base64 → decode_base64_image()
    │   ├─ URL → download_image_from_url()
    │   └─ Multipart → save_temp_image()
    │
    ├─ [1.2] Сохранение во временный файл
    │   └─ TEMP_DIR / tmp_{uuid}.jpg
    │
[2] PaddleOCR-VL Processing
    │
    ├─ [2.1] Инициализация (если еще не инициализирован)
    │   ├─ init_paddleocr()
    │   ├─ paddle_ocr = PaddleOCRVL()
    │   └─ Загрузка моделей из /home/paddleocr/.paddlex
    │
    ├─ [2.2] Обработка изображения
    │   ├─ ocr.predict(image_path)
    │   ├─ PP-DocLayoutV2: Layout Detection
    │   ├─ PaddleOCR-VL-0.9B: OCR Recognition
    │   └─ Генерация структурированных данных
    │
    └─ [2.3] Результат
        └─ Список объектов с данными документа
    │
[3] Сохранение результатов
    │
    ├─ [3.1] save_results_locally()
    │   ├─ Для каждого результата:
    │   │   ├─ res.save_to_markdown(temp_dir)
    │   │   └─ res.save_to_json(temp_dir)
    │   ├─ Объединение результатов
    │   └─ Сохранение в OUTPUT_DIR
    │
    ├─ [3.2] Проверка файлов
    │   ├─ Проверка существования MD файла
    │   ├─ Проверка размера MD файла
    │   ├─ Проверка существования JSON файла
    │   └─ Проверка размера JSON файла
    │
    └─ [3.3] Загрузка в S3 (если настроено)
        ├─ upload_to_cloudru(md_path, json_path)
        ├─ _upload_file_to_s3() для каждого файла
        └─ Проверка загрузки
    │
[4] Формирование ответа
    │
    ├─ [4.1] Базовый ответ
    │   ├─ status, input_type, local_files, timestamp, elapsed_sec
    │
    ├─ [4.2] S3 пути (если загружено)
    │   └─ s3_files с публичными URL
    │
    └─ [4.3] Контент (если return_content=true)
        ├─ markdown_text
        └─ json_data
    │
[5] Очистка
    └─ Удаление временного файла изображения
```

---

## 📊 Анализ качества Pipeline

### Сильные стороны

1. **Использование официального API PaddleOCR-VL**
   - ✅ `ocr.predict()` - официальный метод
   - ✅ `res.save_to_markdown()` - официальный метод
   - ✅ `res.save_to_json()` - официальный метод

2. **Обработка различных форматов входных данных**
   - ✅ Base64 для API интеграций
   - ✅ URL для удаленных изображений
   - ✅ Multipart для прямых загрузок

3. **Надежность**
   - ✅ Fallback механизмы
   - ✅ Проверка файлов перед загрузкой
   - ✅ Детальное логирование

4. **Интеграция с Cloud.ru**
   - ✅ Правильная конфигурация S3
   - ✅ Загрузка результатов
   - ✅ Публичные URL для доступа

### Слабые стороны

1. **Обработка ошибок**
   - ⚠️ Fallback может терять структуру документа
   - ⚠️ Нет валидации качества результатов

2. **Производительность**
   - ⚠️ Нет параллельной обработки
   - ⚠️ Нет кэширования
   - ⚠️ Ленивая инициализация задерживает первый запрос

3. **Масштабируемость**
   - ⚠️ Batch size = 1 (ограничение модели)
   - ⚠️ Нет очереди задач
   - ⚠️ Нет распределенной обработки

---

## 🎯 Рекомендации

### Немедленные исправления

1. **Обновить Dockerfile**
   ```dockerfile
   COPY ocr_vl/service/requirements.txt /tmp/requirements.txt
   COPY ocr_vl/service/server.py /app/server.py
   ```

2. **Проверить SHA256 образа**
   - Уточнить какой образ реально используется
   - Обновить документацию

### Улучшения Pipeline

1. **Предзагрузка моделей**
   - Инициализировать PaddleOCR-VL при старте контейнера
   - Убрать ленивую инициализацию

2. **Улучшить обработку ошибок**
   - Валидация результатов перед сохранением
   - Лучшие fallback механизмы

3. **Добавить метрики**
   - Время обработки
   - Качество распознавания
   - Статистика использования

---

## 📝 Выводы

### Текущее состояние: ✅ Работает

Pipeline реализован и работает согласно официальной документации PaddleOCR-VL:
- ✅ Использует официальные методы API
- ✅ Правильно обрабатывает изображения
- ✅ Генерирует Markdown и JSON
- ✅ Интегрирован с Cloud.ru S3

### Основные проблемы: ⚠️ Требуют исправления

1. **Dockerfile** - старые пути (критично)
2. **SHA256 образа** - неопределенность
3. **Производительность** - можно улучшить

### Следующие шаги

1. Исправить пути в Dockerfile
2. Пересобрать образ с правильными путями
3. Протестировать полный Pipeline
4. Оптимизировать производительность

---

**Статус:** Pipeline реализован и работает, требуется исправление путей в Dockerfile.

