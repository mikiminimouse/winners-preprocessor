# Быстрый старт: Тестирование Cloud.RU ML Inference

## 🚀 За 3 шага

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements_test.txt
```

### Шаг 2: Настройка переменных окружения

Создайте `.env` файл или экспортируйте переменные:

```bash
# Вариант 1: Создать .env файл
cat > .env << EOF
CLOUD_RU_INFERENCE_URL=https://your-endpoint.inference.cloud.ru
CLOUD_RU_IAM_KEY_ID=AKIA...
CLOUD_RU_IAM_SECRET=your-secret-key
CLOUD_RU_INFERENCE_NAME=your-inference-name
EOF

# Вариант 2: Экспорт переменных
export CLOUD_RU_INFERENCE_URL="https://your-endpoint.inference.cloud.ru"
export CLOUD_RU_IAM_KEY_ID="AKIA..."
export CLOUD_RU_IAM_SECRET="your-secret-key"
export CLOUD_RU_INFERENCE_NAME="your-inference-name"
```

### Шаг 3: Запуск тестов

```bash
python test_inference.py
```

## 📍 Где взять данные?

### 1. CLOUD_RU_INFERENCE_URL
- Cloud.RU → AI Factory → ML Inference → Model RUN
- Скопируйте публичный URL инференса

### 2. CLOUD_RU_IAM_KEY_ID и CLOUD_RU_IAM_SECRET
- Cloud.RU → Управление доступом (IAM) → Сервисные аккаунты
- Выберите аккаунт → Ключи → Создать ключ (HMAC/AccessKey)
- ⚠️ Сохраните secret сразу - после закрытия окна его не покажут!

### 3. CLOUD_RU_INFERENCE_NAME (опционально)
- Cloud.RU → ML Inference → Model RUN
- Название вашего инференса

## ✅ Что должно получиться

```
✅ Endpoint доступен
✅ SDK подключение успешно
✅ Вызов модели успешен
```

## ❌ Если что-то не работает

1. Проверьте, что инференс **запущен** (статус "Запущен" в Cloud.RU)
2. Проверьте правильность ключей (не путайте с ID модели!)
3. Убедитесь, что сервисный аккаунт имеет права на Inference
4. Посмотрите детальный отчет в JSON файле

Подробности: см. `INFERENCE_TEST_README.md`


