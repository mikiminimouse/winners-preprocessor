# Инструкции по перезапуску и проверке WebUI

## Текущий статус

WebUI сервер должен быть запущен на `http://localhost:7860`

## Проверка статуса сервера

### 1. Проверка процесса
```bash
ps aux | grep -E "python.*webui|gradio" | grep -v grep
```

### 2. Проверка порта
```bash
netstat -tlnp 2>/dev/null | grep 7860 || ss -tlnp 2>/dev/null | grep 7860
```

### 3. Проверка доступности
```bash
curl http://localhost:7860
```

## Перезапуск WebUI сервера

### Способ 1: Автоматический перезапуск (рекомендуется)

```bash
cd /root/winners_preprocessor

# Остановить текущий процесс
pkill -f "python.*receiver.*webui" || pkill -f "gradio"

# Подождать 2 секунды
sleep 2

# Запустить новый процесс
nohup python3 -m receiver.webui.app > /tmp/webui.log 2>&1 &
echo $! > /tmp/webui.pid

# Подождать запуска
sleep 5

# Проверить логи
tail -20 /tmp/webui.log
```

### Способ 2: Ручной запуск с выводом в консоль

```bash
cd /root/winners_preprocessor
python3 -m receiver.webui.app
```

Это запустит сервер в текущей консоли, и вы увидите все логи в реальном времени.
Для остановки нажмите `Ctrl+C`.

## Проверка обновлений в браузере

### 1. Очистка кэша браузера

**Chrome/Edge:**
- Нажмите `Ctrl+Shift+Delete` (Windows/Linux) или `Cmd+Shift+Delete` (Mac)
- Выберите "Изображения и файлы в кэше"
- Нажмите "Удалить данные"

**Firefox:**
- Нажмите `Ctrl+Shift+Delete` (Windows/Linux) или `Cmd+Shift+Delete` (Mac)
- Выберите "Кэш"
- Нажмите "Удалить сейчас"

### 2. Принудительное обновление страницы

- **Windows/Linux**: `Ctrl+F5` или `Ctrl+Shift+R`
- **Mac**: `Cmd+Shift+R`

### 3. Открытие в режиме инкогнито

Откройте браузер в режиме инкогнито/приватном режиме:
- **Chrome/Edge**: `Ctrl+Shift+N` (Windows/Linux) или `Cmd+Shift+N` (Mac)
- **Firefox**: `Ctrl+Shift+P` (Windows/Linux) или `Cmd+Shift+P` (Mac)

Затем перейдите на `http://localhost:7860`

## Что проверить в WebUI

### 1. Вкладка "🔄 Sync Control"
Должны быть видны:
- Поле для ввода даты (по умолчанию "2025-03-20")
- Поле для лимита записей
- Кнопка "🚀 Запустить синхронизацию"
- После запуска должны появиться:
  - Статус синхронизации
  - Детальные метрики
  - Ошибки и предупреждения
  - График результатов

### 2. Вкладка "💾 Download Control"
Должны быть видны:
- Поле для ввода даты (опционально)
- Поле для лимита протоколов
- Кнопка "📥 Запустить загрузку"
- После запуска должны появиться:
  - Статус загрузки
  - Детальные метрики
  - Ошибки и предупреждения
  - График результатов

### 3. Вкладка "🏥 Health Check"
Должны быть видны:
- Плашки статусов компонентов (VPN, Remote MongoDB, Local MongoDB)
- Кнопки для индивидуальных проверок
- Окно для вывода логов

### 4. Вкладка "⚙️ Configuration"
Должны быть видны:
- Все настройки из .env файла
- Переключатель VPN для Remote MongoDB
- Детальные настройки Scheduler

## Проверка логов

### Просмотр логов в реальном времени
```bash
tail -f /tmp/webui.log
```

### Поиск ошибок
```bash
grep -i "error\|exception\|traceback" /tmp/webui.log
```

### Последние 50 строк логов
```bash
tail -50 /tmp/webui.log
```

## Устранение проблем

### Проблема: Страница не загружается

1. Проверьте что сервер запущен:
   ```bash
   ps aux | grep python | grep webui
   ```

2. Проверьте порт:
   ```bash
   netstat -tlnp | grep 7860
   ```

3. Проверьте логи на ошибки:
   ```bash
   tail -50 /tmp/webui.log
   ```

### Проблема: Видны старые элементы интерфейса

1. Очистите кэш браузера (см. выше)
2. Перезапустите сервер
3. Откройте в режиме инкогнито

### Проблема: Ошибки при импорте

1. Проверьте что все зависимости установлены:
   ```bash
   cd /root/winners_preprocessor
   pip3 install -r receiver/requirements.txt
   ```

2. Проверьте импорты:
   ```bash
   python3 -c "from receiver.webui.app import demo; print('OK')"
   ```

### Проблема: Порт 7860 занят

1. Найдите процесс использующий порт:
   ```bash
   lsof -i :7860 || fuser 7860/tcp
   ```

2. Остановите процесс:
   ```bash
   kill -9 <PID>
   ```

3. Перезапустите WebUI

## Быстрая команда для перезапуска

Создайте скрипт `restart_webui.sh`:

```bash
#!/bin/bash
cd /root/winners_preprocessor
pkill -f "python.*receiver.*webui" || pkill -f "gradio"
sleep 2
nohup python3 -m receiver.webui.app > /tmp/webui.log 2>&1 &
echo $! > /tmp/webui.pid
echo "WebUI перезапущен. PID: $(cat /tmp/webui.pid)"
echo "Логи: tail -f /tmp/webui.log"
echo "URL: http://localhost:7860"
```

Сделайте его исполняемым:
```bash
chmod +x restart_webui.sh
```

Затем используйте:
```bash
./restart_webui.sh
```

## Контакты и поддержка

Если проблемы сохраняются:
1. Проверьте логи: `tail -100 /tmp/webui.log`
2. Проверьте что код актуален: `git status` (если используется git)
3. Убедитесь что все файлы сохранены

