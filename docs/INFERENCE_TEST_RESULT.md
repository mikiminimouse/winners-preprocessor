# Результаты тестирования подключения к Cloud.RU ML Inference

## 📋 Тестируемые параметры

- **Endpoint URL**: `https://5bf2f0a2-cdc8-4831-be41-5b2f9d6a7b55.modelrun.inference.cloud.ru`
- **Модель**: `model-run-msguj-among`
- **Дата теста**: 2025-11-25

## 🔍 Результаты тестирования

### Тест 1: Доступность endpoint (без авторизации)

Все проверенные пути возвращают **404 Not Found**:

- `/health` → 404
- `/` → 404  
- `/v1/models` → 404

**Вывод**: Endpoint требует авторизацию или использует другие пути. Статус 404 вместо 401 может означать, что:
1. Endpoint требует авторизацию для всех запросов
2. Пути API отличаются от стандартных
3. Модель может быть не запущена (проверьте статус в Cloud.RU)

### Тест 2: Проверка с IAM ключами

⚠️ **IAM ключи не заданы в `.env` файле**

Для полного тестирования нужны:
- `CLOUD_RU_IAM_KEY_ID` - Access Key ID из сервисного аккаунта
- `CLOUD_RU_IAM_SECRET` - Secret Key

## 📝 Что нужно сделать дальше

### 1. Получить IAM ключи

1. Зайдите в **Cloud.RU → Управление доступом (IAM) → Сервисные аккаунты**
2. Выберите сервисный аккаунт (или создайте новый)
3. Перейдите на вкладку **"Ключи"**
4. Нажмите **"Создать ключ (Access Key)"**
5. Выберите тип: **HMAC / AccessKey** (не JWT)
6. **Сохраните сразу** `key_id` и `secret` - после закрытия окна secret больше не покажут!

### 2. Добавить ключи в `.env`

Откройте файл `.env` и добавьте:

```env
CLOUD_RU_IAM_KEY_ID=ваш_key_id_здесь
CLOUD_RU_IAM_SECRET=ваш_secret_здесь
```

### 3. Запустить полный тест

```bash
# Установить SDK (если еще не установлен)
pip install --break-system-packages evolution-openai python-dotenv

# Запустить тест
python3 test_inference.py
```

Или упрощенный тест:

```bash
python3 test_inference_simple.py
```

## 🔧 Альтернативный способ проверки

Если у вас есть IAM ключи, можно протестировать напрямую через Python:

```python
from evolution_openai import EvolutionOpenAI

client = EvolutionOpenAI(
    key_id="ваш_key_id",
    secret="ваш_secret",
    base_url="https://5bf2f0a2-cdc8-4831-be41-5b2f9d6a7b55.modelrun.inference.cloud.ru/v1"
)

response = client.chat.completions.create(
    model="model-run-msguj-among",
    messages=[
        {"role": "system", "content": "Вы очень полезный ассистент."},
        {"role": "user", "content": "Что такое искусственный интеллект?"}
    ],
    max_tokens=5000,
    temperature=0.5
)

print(response.choices[0].message.content)
```

## ⚠️ Важные замечания

1. **Безопасность**: Никогда не коммитьте `.env` файл с реальными ключами в git!
2. **Проверка статуса**: Убедитесь, что инференс **запущен** в Cloud.RU (статус "Запущен")
3. **Права доступа**: Сервисный аккаунт должен иметь права на вызов Inference
4. **Формат URL**: Для SDK нужен URL с `/v1` в конце: `...cloud.ru/v1`

## 📊 Следующие шаги

1. ✅ Endpoint URL настроен
2. ✅ Название модели задано
3. ⏳ **Нужны IAM ключи для полного тестирования**
4. ⏳ После получения ключей - запустить полный тест с вызовом модели

## 📞 Если что-то не работает

1. Проверьте статус инференса в Cloud.RU → ML Inference → Model RUN
2. Убедитесь, что сервисный аккаунт имеет права на Inference
3. Проверьте правильность формата ключей (не путайте с ID модели!)
4. Посмотрите детальные логи в JSON файлах результатов


