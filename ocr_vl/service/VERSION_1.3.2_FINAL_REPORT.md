# Финальный отчет версии 1.3.2

**Дата:** 05.12.2025  
**Версия:** 1.3.2

## ✅ Исправления

### Проблема версии 1.3.1:

- ❌ Ошибка 502 при обработке OCR запросов
- ⚠️ Переменные окружения не устанавливались при запуске

### Решение:

1. ✅ Добавлена установка переменных окружения в `start.sh`:
   ```bash
   export PADDLEX_HOME=/home/paddleocr/.paddlex
   export HF_HOME=/home/paddleocr/.cache/huggingface
   ```

2. ✅ Двойная защита:
   - Переменные устанавливаются в `start.sh`
   - Дополнительная проверка в `server.py` при инициализации

## 📦 Развертывание

### Образ:

- ✅ Версия: 1.3.2
- ✅ Размер: 12.3GB
- ✅ Загружен в Artifact Registry

**URI:**
```
docling-granite-258m.cr.cloud.ru/paddleocr-vl-service:1.3.2
docling-granite-258m.cr.cloud.ru/paddleocr-vl-service:latest
```

## ✅ Ожидаемые результаты

После развертывания версии 1.3.2:

1. **Переменные окружения установлены:**
   - `PADDLEX_HOME=/home/paddleocr/.paddlex`
   - `HF_HOME=/home/paddleocr/.cache/huggingface`

2. **Модели доступны:**
   - PaddleOCR-VL найдет модели при инициализации
   - Инициализация должна пройти успешно

3. **Обработка OCR:**
   - Первая страница может занять время (инициализация)
   - Последующие страницы обработаются быстрее

## 📋 Инструкции для тестирования

После развертывания на Cloud.ru:

```bash
export ML_INFERENCE_API_KEY="your-api-key"
export BASE_URL="https://your-endpoint.modelrun.inference.cloud.ru"

# Тест 1: Health Check
curl -H "Authorization: Bearer $ML_INFERENCE_API_KEY" \
  "$BASE_URL/health"

# Тест 2: Конвертация PDF в MD
python3 test_pdf_to_md.py
```

### Проверка логов:

Должно быть в логах:
```
✅ PADDLEX_HOME=/home/paddleocr/.paddlex
✅ Set PADDLEX_HOME to /home/paddleocr/.paddlex
✅ PaddleOCR-VL initialized successfully
```

---

**Статус:** ✅ Образ собран и загружен с исправлением переменных окружения. Готов к тестированию!

