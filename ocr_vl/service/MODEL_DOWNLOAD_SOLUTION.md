# Решение проблемы с загрузкой моделей

**Дата:** 05.12.2025  
**Проблема:** Модели не предзагружены или не сохранились в образе

## 🔍 Анализ проблемы

### Что происходит:

1. При сборке образа добавлена предзагрузка моделей
2. При runtime модели все равно пытаются скачаться
3. Ошибка: "No available model hosting platforms detected"

### Возможные причины:

1. **Предзагрузка не сработала при сборке** - нет доступа к интернету
2. **Модели не сохранились в образе** - загрузились во временную директорию
3. **Неправильный путь к моделям** - PaddleX ищет модели в других местах

## 🔧 Решения

### Вариант 1: Использовать офлайн образ (РЕКОМЕНДУЕТСЯ)

Использовать официальный офлайн образ с предзагруженными моделями:

```dockerfile
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest-offline
```

Или:
```dockerfile
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-genai-vllm-server:latest-offline
```

**Преимущества:**
- ✅ Модели уже предзагружены
- ✅ Работает без интернета
- ✅ Проверенное решение

### Вариант 2: Ручная загрузка моделей в Dockerfile

Явно указать пути к моделям и загрузить их вручную:

```dockerfile
# Создаем директорию для моделей
RUN mkdir -p /root/.paddlex/models && chmod -R 777 /root/.paddlex

# Загружаем модели через Python скрипт
RUN python3 << 'PYTHON_SCRIPT'
from paddlex.inference.utils.official_models import OfficialModels
om = OfficialModels()

# Загружаем PP-DocLayoutV2
model_dir = om.get_model_local_path('PP-DocLayoutV2', backend=None)
print(f"Model downloaded to: {model_dir}")
PYTHON_SCRIPT

# Сохраняем директорию с моделями в образ
RUN cp -r /root/.paddlex /app/.paddlex_backup || true
```

### Вариант 3: Использовать переменные окружения для указания путей

Настроить PaddleOCR-VL на использование локальных моделей через переменные окружения или параметры инициализации.

## 🎯 Рекомендация

**Использовать офлайн образ** - это самый надежный способ гарантировать наличие моделей.

---

**Статус:** Требуется решение проблемы с предзагрузкой моделей

