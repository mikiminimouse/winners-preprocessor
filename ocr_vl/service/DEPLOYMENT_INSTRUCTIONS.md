# Инструкции по развертыванию PaddleOCR-VL Service

**Версия:** 1.0.8  
**Дата обновления:** 05.12.2025

## 📦 Образ Docker

**URI образа:**
```
docling-granite-258m.cr.cloud.ru/paddleocr-vl-service:latest
```

**Версия с тегом:**
```
docling-granite-258m.cr.cloud.ru/paddleocr-vl-service:1.0.8
```

**Digest:**
```
sha256:517c7cc435e81889905d7a3bfc03846ec4a2a5b308cb8bfb34d0c752ff918fb6
```

## 🚀 Запуск на Cloud.ru ML Inference

### 1. Базовая конфигурация

**Docker Image URI:**
```
docling-granite-258m.cr.cloud.ru/paddleocr-vl-service:latest
```

**Порты:**
- **8081** - FastAPI handler (основной сервис OCR)

**Примечание:** vLLM сервер убран в версии 1.0.8. FastAPI работает через PaddlePaddle бэкенд напрямую.

**Health Check Endpoint:**
```
GET /health
```

### 2. Настройки Health Check

**Liveness Probe:**
- **Path:** `/health`
- **Port:** `8081`
- **Initial Delay:** `180` секунд (увеличено для инициализации)
- **Period:** `30` секунд
- **Timeout:** `10` секунд
- **Failure Threshold:** `3`

**Readiness Probe:**
- **Path:** `/health`
- **Port:** `8081`
- **Initial Delay:** `240` секунд
- **Period:** `15` секунд
- **Timeout:** `5` секунд
- **Failure Threshold:** `3`

**Startup Probe (рекомендуется):**
- **Path:** `/health`
- **Port:** `8081`
- **Initial Delay:** `0` секунд
- **Period:** `15` секунд
- **Timeout:** `10` секунд
- **Failure Threshold:** `40` (до 10 минут на инициализацию)

### 3. Переменные окружения (опционально)

**Cloud.ru Object Storage (для сохранения результатов):**
```bash
CLOUDRU_S3_ENDPOINT=https://s3.cloud.ru
CLOUDRU_S3_BUCKET=your-bucket-name
CLOUDRU_S3_ACCESS_KEY=your-access-key
CLOUDRU_S3_SECRET_KEY=your-secret-key
```

### 4. Ресурсы

**Рекомендуемые ресурсы:**
- **CPU:** 4+ cores
- **Memory:** 16GB+ RAM
- **GPU:** 1x NVIDIA GPU (CUDA 12.6+)
- **Storage:** 10GB+ для моделей и кэша

### 5. Особенности запуска

**Важно:**
- ✅ FastAPI handler запускается автоматически и работает на порту 8081
- ✅ PaddleOCR-VL инициализируется при первом запросе (ленивая инициализация)
- ✅ Работает через PaddlePaddle бэкенд напрямую (без vLLM ускорения)

## 🔍 Проверка работоспособности

### 1. Проверка Health Check

```bash
curl -H "Authorization: Bearer YOUR_API_KEY" \
  https://your-endpoint.cloud.ru/health
```

**Ожидаемый ответ:**
```json
{
    "status": "healthy",
    "paddleocr": "not_initialized",
    "s3_storage": "not_configured",
    "output_dir": "/app/output",
    "temp_dir": "/app/temp",
    "docs": "/docs - Swagger UI для тестирования API"
}
```

### 2. Проверка корневого endpoint

```bash
curl -H "Authorization: Bearer YOUR_API_KEY" \
  https://your-endpoint.cloud.ru/
```

### 3. Проверка Swagger UI

Откройте в браузере:
```
https://your-endpoint.cloud.ru/docs
```

## 📝 Исправления в версии 1.0.8

### Критическое исправление:

**Проблема:** `PermissionError: [Errno 13] Permission denied: '/.paddlex'`

**Решение:** Добавлено создание директории `/.paddlex` в Dockerfile с правильными правами доступа.

**Результат:**
- ✅ Импорт PaddleOCRVL работает корректно
- ✅ FastAPI сервер запускается без ошибок
- ✅ Health check отвечает успешно

### Дополнительные исправления:

**Проблема:** vLLM сервер не может скачать модель и не используется правильно.

**Решение:** vLLM сервер убран из запуска. FastAPI работает через PaddlePaddle бэкенд напрямую.

**Результат:**
- ✅ Быстрее запуск (нет попыток запустить vLLM)
- ✅ Меньше ошибок в логах
- ✅ Стабильнее работа

## 🐛 Известные проблемы и решения

### 1. vLLM сервер не запускается без GPU

**Проблема:** vLLM сервер требует GPU для работы.

**Решение:** Это нормальное поведение. FastAPI handler работает независимо от vLLM сервера. Для production используйте GPU.

### 2. Долгая инициализация

**Проблема:** PaddleOCR-VL может долго инициализироваться при первом запросе.

**Решение:** Увеличьте таймауты health check (см. настройки выше).

### 3. Проблемы с torch/NCCL

**Проблема:** Возможны ошибки совместимости torch/NCCL в базовом образе.

**Статус:** Образ включает попытки исправления, но может потребоваться проверка на конкретном GPU.

## 🔗 Полезные ссылки

- **API Документация:** `/docs` (Swagger UI) или `/redoc` (ReDoc)
- **OpenAPI Schema:** `/openapi.json`
- **Health Check:** `/health`

## 📧 Поддержка

При возникновении проблем проверьте:
1. Логи контейнера на наличие ошибок
2. Health check endpoint
3. Доступность GPU (для vLLM)
4. Переменные окружения для S3 (если используются)

