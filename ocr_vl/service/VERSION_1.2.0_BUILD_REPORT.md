# Отчет о сборке версии 1.2.0

**Дата:** 05.12.2025  
**Версия:** 1.2.0

## ✅ Выполнено

### 1. Обновления

- ✅ Улучшен Dockerfile с детальной предзагрузкой моделей
- ✅ Обновлена версия до 1.2.0 в server.py
- ✅ Добавлена проверка загрузки моделей
- ✅ Образ успешно собран

### 2. Сборка образа

**Команда:**
```bash
docker build --no-cache -t paddleocr-vl-service:1.2.0 -t paddleocr-vl-service:latest .
```

**Результат:**
- ✅ Образ собран успешно
- ✅ Размер: 20.5GB
- ✅ Теги: 1.2.0 и latest

## ⚠️ Проблема

### Модели не предзагружены

**Проверка:**
```bash
du -sh /root/.paddlex /root/.cache /.paddlex /app/models
```

**Результат:**
- `/root/.paddlex`: 20K (почти пусто)
- `/.paddlex`: 8K (почти пусто)
- `/app/models`: 4K (пусто)

**Причина:**
Предзагрузка моделей при сборке не сработала или модели не сохранились в образе.

## 🔍 Анализ

### Проблема с предзагрузкой:

1. **Модели не загружаются при сборке:**
   - Python скрипт выполняется, но модели не загружаются
   - Возможно, нет доступа к репозиториям моделей при сборке
   - Или модели загружаются, но не сохраняются в образе

2. **Требуется решение:**
   - Использовать офлайн образ с предзагруженными моделями
   - Или вручную загрузить модели и скопировать в образ
   - Или настроить переменные окружения для указания путей к моделям

## 📋 Рекомендации

### Вариант 1: Использовать офлайн образ (РЕКОМЕНДУЕТСЯ)

Использовать официальный офлайн образ с предзагруженными моделями:
```dockerfile
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest-gpu-sm120-offline
```

### Вариант 2: Ручная предзагрузка

Загрузить модели вручную перед сборкой образа и скопировать их в образ.

### Вариант 3: Загрузить модели отдельно

Создать отдельный Dockerfile для загрузки моделей, затем использовать multi-stage build.

---

**Статус:** ⚠️ Образ собран, но модели не предзагружены. Требуется решение.

