# План предзагрузки моделей в образ

**Дата:** 05.12.2025

## 🔍 Проблема

При инициализации PaddleOCR-VL требуется скачивание моделей:
- `PP-DocLayoutV2` - для layout detection
- Другие модели для PaddleOCR-VL pipeline

Но в runtime окружении нет доступа к репозиториям моделей.

## 💡 Решение

Предзагрузить модели на этапе сборки Docker образа, когда есть доступ к интернету.

## 📋 Необходимые модели

Для PaddleOCR-VL нужны:
1. **PP-DocLayoutV2** - layout detection model
2. Другие компоненты pipeline (VLM recognition и т.д.)

## 🔧 Реализация

### Вариант 1: Предзагрузка через PaddleX API

Добавить в Dockerfile после установки PaddleOCR:

```dockerfile
# Предзагрузка моделей для PaddleOCR-VL
RUN python3 << 'PYTHON_SCRIPT'
import sys
from pathlib import Path

print("Предзагрузка моделей для PaddleOCR-VL...")

try:
    # Пробуем инициализировать PaddleOCR-VL, что автоматически загрузит модели
    from paddleocr import PaddleOCRVL
    print("Инициализация PaddleOCR-VL для предзагрузки моделей...")
    
    # Инициализируем (это загрузит все необходимые модели)
    ocr = PaddleOCRVL()
    print("✅ Модели предзагружены успешно")
    
except Exception as e:
    print(f"⚠️  Ошибка при предзагрузке моделей: {e}")
    print("Модели будут загружены при первом использовании (если есть доступ)")
    # Не падаем - модели могут быть предзагружены позже
PYTHON_SCRIPT
```

### Вариант 2: Прямая загрузка через OfficialModels API

```dockerfile
# Предзагрузка моделей через PaddleX API
RUN python3 << 'PYTHON_SCRIPT'
import sys
from pathlib import Path
import os

print("Предзагрузка моделей для PaddleOCR-VL...")

try:
    from paddlex.inference.utils.official_models import OfficialModels
    
    om = OfficialModels()
    
    # Модели, которые нужны для PaddleOCR-VL
    models_to_download = [
        'PP-DocLayoutV2',  # Layout detection
        # Добавить другие по мере необходимости
    ]
    
    for model_name in models_to_download:
        try:
            print(f"Загрузка модели: {model_name}...")
            model_dir = om.get_model_local_path(model_name, backend=None)
            print(f"✅ Модель {model_name} загружена в: {model_dir}")
        except Exception as e:
            print(f"⚠️  Ошибка загрузки {model_name}: {e}")
            
except Exception as e:
    print(f"⚠️  Ошибка при предзагрузке: {e}")
    # Не падаем - попробуем загрузить при использовании
PYTHON_SCRIPT
```

### Вариант 3: Использование офлайн образа как базового

Если есть офлайн образ с предзагруженными моделями:
- `paddleocr-vl:latest-offline`
- `paddleocr-genai-vllm-server:latest-offline`

## ✅ Рекомендация

Использовать **Вариант 1** - простая инициализация PaddleOCR-VL при сборке образа, что автоматически загрузит все необходимые модели.

---

**Статус:** Готов к реализации

