# Анализ переменных окружения для Docker контейнеров

**Дата:** 05.12.2025  
**Статус:** Полный анализ всех версий

## 📋 Содержание

1. [Обзор версий и базовых образов](#обзор-версий)
2. [Переменные окружения для Cloud.ru](#переменные-для-cloudru)
3. [Внутренние переменные контейнера](#внутренние-переменные)
4. [Рекомендации по версиям](#рекомендации)

---

## 📦 Обзор версий и базовых образов

### Версии 1.0.8 - 1.0.9 (устаревшие)

**Базовый образ:**
```dockerfile
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddlex-genai-vllm-server:latest
```

**Стек:**
- PaddlePaddle с vLLM сервером
- FastAPI handler на порту 8081
- vLLM сервер на порту 8080 (версия 1.0.8, убран в 1.0.9)

**Особенности:**
- ❌ Требовал доступ к интернету для загрузки моделей
- ⚠️ Проблемы с vLLM в версии 1.0.8
- ✅ Улучшен в версии 1.0.9 (убраны проблемы vLLM)

**Статус:** ⚠️ Устаревшие, не рекомендуются

---

### Версии 1.1.0 - 1.2.0 (экспериментальные)

**Базовый образ:**
```dockerfile
FROM ccr-2vdh3abv-pub.cnc.bj.baiduce.com/paddlepaddle/paddlex-genai-vllm-server:latest
```

**Стек:**
- PaddlePaddle
- Попытка предзагрузки моделей при сборке
- FastAPI handler

**Особенности:**
- ⚠️ Модели не предзагружались корректно
- ❌ Требовал доступ к интернету в runtime
- ⚠️ Большой размер образа (~20.5GB)

**Статус:** ⚠️ Не рекомендуется, проблемы с моделями

---

### Версия 1.3.0+ (рекомендуемые) ⭐

**Базовый образ:**
```dockerfile
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest-gpu-sm120-offline
```

**Стек:**
- PaddleOCR-VL (офлайн-образ)
- Модели предзагружены в образе
- FastAPI handler на порту 8081
- PaddlePaddle dynamic graph mode

**Особенности:**
- ✅ Модели предзагружены в образ
- ✅ Не требует доступа к интернету в runtime
- ✅ Меньший размер образа (~12.3GB)
- ✅ Оптимизирован для production

**Актуальные версии:**
- `1.3.0` - первая версия на офлайн-образе
- `1.3.2` - улучшения health check
- `1.3.4` - исправления static graph mode
- `1.3.5` - улучшения инициализации
- `1.3.6` - фоновый запуск инициализации (текущая)

**Статус:** ✅ Рекомендуется использовать версию 1.3.6

---

## 🔧 Переменные окружения для Cloud.ru

### Опциональные переменные (для S3 сохранения)

Эти переменные **НЕ обязательны** для базовой работы сервиса, но **требуются** для сохранения результатов в Cloud.ru Object Storage.

#### Для всех версий 1.3.0+:

```bash
# Cloud.ru Object Storage конфигурация
CLOUDRU_S3_ENDPOINT=https://s3.cloud.ru
CLOUDRU_S3_BUCKET=bucket-winners223
CLOUDRU_S3_ACCESS_KEY=your-access-key
CLOUDRU_S3_SECRET_KEY=your-secret-key
```

**Формат для веб-консоли Cloud.ru ML Inference:**

В разделе "Environment Variables" добавьте:

```
CLOUDRU_S3_ENDPOINT = https://s3.cloud.ru
CLOUDRU_S3_BUCKET = bucket-winners223
CLOUDRU_S3_ACCESS_KEY = 502f76f0-9017-493d-bda4-9e1bb278da84:ce94860ccc8780b2bc5f00f31459d24e
CLOUDRU_S3_SECRET_KEY = 759469c88d6e450b584e2487c5174770
```

**Поведение:**
- ✅ Если все 4 переменные заданы → результаты сохраняются и локально, и в S3
- ✅ Если хотя бы одна отсутствует → результаты сохраняются только локально
- ✅ Сервис работает в любом случае

---

### Переменные, которые НЕ нужны

Следующие переменные **НЕ требуются** и **не используются** сервисом:

```bash
# VLM API переменные (не используются)
USE_CLOUDRU_VLM=false
CLOUDRU_API_URL=...
CLOUDRU_API_KEY=...
CLOUDRU_MODEL=...

# Дополнительные S3 переменные (не используются)
USE_CLOUDRU_S3=false
CLOUDRU_S3_TENANT_ID=...
CLOUDRU_S3_KEY_ID=...
CLOUDRU_S3_REGION=...
```

**Причина:** Сервис работает напрямую с PaddleOCR-VL, не использует внешний VLM API.

---

## ⚙️ Внутренние переменные контейнера

Эти переменные **устанавливаются автоматически** внутри контейнера в `start.sh` и **НЕ требуют** настройки через Docker run или веб-консоль.

### Для версий 1.3.0+ (автоматические):

Эти переменные устанавливаются в `/app/start.sh` при запуске контейнера:

```bash
# Пути к моделям (автоматически)
export PADDLEX_HOME=/home/paddleocr/.paddlex
export HF_HOME=/home/paddleocr/.cache/huggingface

# Настройки PaddlePaddle для dynamic graph mode (автоматически)
export FLAGS_enable_eager_mode=1
export FLAGS_eager_delete_tensor_gb=0
export FLAGS_use_mkldnn=0
```

**⚠️ Важно:**
- Эти переменные **НЕ нужно** пробрасывать через Docker run
- Они устанавливаются автоматически при старте контейнера
- Изменение их вручную может привести к проблемам

---

## 📊 Таблица сравнения версий

| Версия | Базовый образ | Требует интернет? | Размер | Рекомендуется? |
|--------|--------------|-------------------|--------|----------------|
| 1.0.8 | `paddlex-genai-vllm-server:latest` | ❌ Да | ~20GB | ❌ Нет |
| 1.0.9 | `paddlex-genai-vllm-server:latest` | ❌ Да | ~20GB | ❌ Нет |
| 1.1.0 | `paddlex-genai-vllm-server:latest` | ❌ Да | ~20.5GB | ❌ Нет |
| 1.2.0 | `paddlex-genai-vllm-server:latest` | ❌ Да | ~20.5GB | ❌ Нет |
| 1.3.0+ | `paddleocr-vl:latest-gpu-sm120-offline` | ✅ Нет | ~12.3GB | ✅ Да |

---

## 🎯 Рекомендации

### Для запуска в веб-консоли Cloud.ru ML Inference:

#### 1. Используйте версию 1.3.6:

```
Docker Image: docling-granite-258m.cr.cloud.ru/paddleocr-vl-service:1.3.6
```

или

```
Docker Image: docling-granite-258m.cr.cloud.ru/paddleocr-vl-service:latest
```

#### 2. Переменные окружения (опционально):

**Для сохранения в S3 (рекомендуется):**
```
CLOUDRU_S3_ENDPOINT = https://s3.cloud.ru
CLOUDRU_S3_BUCKET = bucket-winners223
CLOUDRU_S3_ACCESS_KEY = your-access-key
CLOUDRU_S3_SECRET_KEY = your-secret-key
```

**Без S3 (только локальное сохранение):**
- Переменные окружения не требуются
- Результаты сохраняются в `/app/output` внутри контейнера

#### 3. Порты:

```
8081 - FastAPI handler (основной сервис)
```

#### 4. Health Check:

```
Path: /health
Port: 8081
```

---

## 📝 Примеры конфигурации

### Пример 1: Минимальная конфигурация (без S3)

**В веб-консоли Cloud.ru:**

```
Docker Image: docling-granite-258m.cr.cloud.ru/paddleocr-vl-service:1.3.6
Port: 8081
Environment Variables: (пусто)
```

**Результат:**
- ✅ Сервис работает
- ✅ Результаты сохраняются локально в `/app/output`
- ❌ Результаты НЕ сохраняются в S3

---

### Пример 2: Полная конфигурация (с S3)

**В веб-консоли Cloud.ru:**

```
Docker Image: docling-granite-258m.cr.cloud.ru/paddleocr-vl-service:1.3.6
Port: 8081

Environment Variables:
  CLOUDRU_S3_ENDPOINT = https://s3.cloud.ru
  CLOUDRU_S3_BUCKET = bucket-winners223
  CLOUDRU_S3_ACCESS_KEY = 502f76f0-9017-493d-bda4-9e1bb278da84:ce94860ccc8780b2bc5f00f31459d24e
  CLOUDRU_S3_SECRET_KEY = 759469c88d6e450b584e2487c5174770
```

**Результат:**
- ✅ Сервис работает
- ✅ Результаты сохраняются локально в `/app/output`
- ✅ Результаты сохраняются в S3 bucket

---

## ⚠️ Важные замечания

### 1. Внутренние переменные

**НЕ пробрасывайте** следующие переменные через Docker run:
- `PADDLEX_HOME`
- `HF_HOME`
- `FLAGS_enable_eager_mode`
- `FLAGS_eager_delete_tensor_gb`
- `FLAGS_use_mkldnn`

Они устанавливаются автоматически и их изменение может привести к проблемам.

### 2. Формат ACCESS_KEY

`CLOUDRU_S3_ACCESS_KEY` должен быть в формате:
```
tenant_id:key_id
```

Например:
```
502f76f0-9017-493d-bda4-9e1bb278da84:ce94860ccc8780b2bc5f00f31459d24e
```

### 3. Все 4 переменные S3 обязательны

Для работы S3 сохранения **все 4 переменные** должны быть заданы:
- `CLOUDRU_S3_ENDPOINT`
- `CLOUDRU_S3_BUCKET`
- `CLOUDRU_S3_ACCESS_KEY`
- `CLOUDRU_S3_SECRET_KEY`

Если хотя бы одна отсутствует, S3 сохранение отключается автоматически.

---

## 🔍 Проверка конфигурации

### Проверка переменных внутри контейнера:

```bash
# Подключитесь к контейнеру
docker exec -it <container-id> bash

# Проверьте переменные S3
echo $CLOUDRU_S3_ENDPOINT
echo $CLOUDRU_S3_BUCKET

# Проверьте внутренние переменные
echo $PADDLEX_HOME
echo $FLAGS_enable_eager_mode
```

### Проверка через Health Check:

```bash
curl https://your-service-url/health
```

**Ответ должен содержать:**
```json
{
  "status": "healthy",
  "paddleocr": "ready",
  "s3_storage": "configured"  // или "not_configured"
}
```

---

## 📚 Дополнительная документация

- `ML_INFERENCE_ENV_VARS.md` - Детальная информация о переменных
- `CLOUDRU_DEPLOYMENT_GUIDE.md` - Руководство по развертыванию
- `QUICK_FIX_CHECKLIST.md` - Быстрая настройка

---

**Версия документа:** 1.0  
**Последнее обновление:** 05.12.2025

