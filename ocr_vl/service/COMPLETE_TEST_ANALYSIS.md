# Комплексный анализ: PaddleOCR-VL-0.9B - компоненты, работа, проблемы

**Дата:** 06.12.2025  
**Версия сервиса:** 1.3.6  
**URL:** `https://9525a16c-09c1-4489-87d3-bf1946792a53.modelrun.inference.cloud.ru`

## 📋 Содержание

1. [Результаты тестирования](#результаты-тестирования)
2. [Компоненты и модели инфраструктуры](#компоненты-и-модели)
3. [Как работает PaddleOCR-VL-0.9B](#как-работает)
4. [Проблемы в процессе извлечения текста](#проблемы)
5. [Логика вызова моделей](#логика-вызова)
6. [Структура инфраструктуры](#структура)

---

## 📊 Результаты тестирования

### ✅ Успешные тесты:

1. **Health Check** ✅
   ```json
   {
     "status": "healthy",
     "paddleocr": "ready",
     "s3_storage": "not_configured"
   }
   ```
   - Сервис работает
   - PaddleOCR-VL готов

2. **OCR Multipart** ✅
   - Время: 103.44 секунды
   - Статус: success
   - Результаты: Markdown и JSON сохранены

3. **PDF to MD - Страница 1** ✅
   - Время: 32.14 секунды
   - Статус: success

### ⚠️ Проблемные тесты:

1. **PDF to MD - Страница 2** ❌
   - HTTP 502 (EOF)
   - Возможная причина: таймаут при последовательной обработке

2. **Service Info** ❌
   - HTTP 502

---

## 🧩 Компоненты и модели инфраструктуры

### Структура инфраструктуры PaddleOCR-VL:

```
┌─────────────────────────────────────────────┐
│         PaddleOCR-VL Infrastructure         │
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │     PaddlePaddle Framework          │   │
│  │  - Deep Learning Framework          │   │
│  │  - GPU Support (CUDA 12.6+)         │   │
│  │  - Dynamic Graph Mode               │   │
│  └──────────────┬──────────────────────┘   │
│                 │                           │
│  ┌──────────────▼──────────────────────┐   │
│  │         PaddleX Library             │   │
│  │  - Model Management                 │   │
│  │  - Model Loading                    │   │
│  │  - Model Caching                    │   │
│  └──────────────┬──────────────────────┘   │
│                 │                           │
│  ┌──────────────▼──────────────────────┐   │
│  │      PaddleOCR-VL Pipeline          │   │
│  │                                     │   │
│  │  ┌───────────────────────────────┐  │   │
│  │  │   PP-DocLayoutV2              │  │   │
│  │  │   Layout Detection Model      │  │   │
│  │  │   ~500MB                      │  │   │
│  │  └──────────────┬────────────────┘  │   │
│  │                 │                    │   │
│  │  ┌──────────────▼────────────────┐  │   │
│  │  │   PaddleOCR-VL-0.9B           │  │   │
│  │  │   Vision-Language Model       │  │   │
│  │  │   ~2-3GB                      │  │   │
│  │  │   Qwen-VL Architecture        │  │   │
│  │  └──────────────┬────────────────┘  │   │
│  │                 │                    │   │
│  │  ┌──────────────▼────────────────┐  │   │
│  │  │   Post-processing             │  │   │
│  │  │   Markdown/JSON Generation    │  │   │
│  │  └───────────────────────────────┘  │   │
│  └─────────────────────────────────────┘   │
│                                             │
└─────────────────────────────────────────────┘
```

### Компоненты по версиям:

#### Версия 1.3.6 (текущая):

**Базовый образ:**
```
ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest-gpu-sm120-offline
```

**Компоненты:**
1. ✅ **PaddlePaddle Framework**
   - Версия: определяется базовым образом
   - Режим: Dynamic graph mode
   - GPU: CUDA 12.6+ support

2. ✅ **PaddleX**
   - Управление моделями
   - Загрузка и кэширование
   - Путь: `/home/paddleocr/.paddlex`

3. ✅ **PP-DocLayoutV2**
   - Layout detection model
   - Размер: ~500MB
   - Путь: `/home/paddleocr/.paddlex/official_models/PP-DocLayoutV2`
   - Назначение: Определение структуры документа

4. ✅ **PaddleOCR-VL-0.9B**
   - Vision-Language Model
   - Размер: ~2-3GB
   - Путь: `/home/paddleocr/.paddlex/official_models/PaddleOCR-VL`
   - Архитектура: Qwen-VL
   - Параметров: 0.9B
   - Назначение: Распознавание текста

5. ✅ **FastAPI Handler**
   - HTTP API сервер
   - Порт: 8081
   - Обработка запросов

---

## 🔄 Как работает PaddleOCR-VL-0.9B

### Полный процесс обработки:

#### Шаг 1: Получение изображения

```
Клиент → HTTP POST /ocr → FastAPI → Сохранение во временный файл
```

**Поддерживаемые форматы:**
- Base64 (кодированная строка)
- URL (ссылка на изображение)
- Multipart (загруженный файл)

**Рекомендуется:** Multipart (наиболее надежный)

---

#### Шаг 2: Инициализация PaddleOCR-VL

**Ленивая инициализация (при первом запросе):**

```python
paddle_ocr = PaddleOCRVL()
```

**Что происходит:**

1. **Проверка путей к моделям:**
   ```python
   PADDLEX_HOME=/home/paddleocr/.paddlex
   HF_HOME=/home/paddleocr/.cache/huggingface
   ```

2. **Загрузка PP-DocLayoutV2:**
   - Модель находится в кэше
   - Загрузка весов (~500MB)
   - Инициализация модели

3. **Загрузка PaddleOCR-VL-0.9B:**
   - Модель находится в кэше
   - Загрузка конфигурации (config.json)
   - Загрузка весов (model.safetensors, ~2-3GB)
   - Инициализация 18 слоев трансформера
   - Настройка GQA (Grouped Query Attention)

**Время инициализации:** ~80-90 секунд (первый раз)

---

#### Шаг 3: Обработка изображения

```python
result = paddle_ocr.predict(image_path)
```

**Что происходит внутри:**

**3.1. Layout Detection (PP-DocLayoutV2):**

```
Изображение → Предобработка → Encoder → Decoder → Detection Head → Регионы
```

**Процесс:**
1. Предобработка изображения:
   - Ресайз до оптимального размера
   - Нормализация
   - Конвертация в тензор

2. Анализ структуры:
   - PP-DocLayoutV2 анализирует изображение
   - Определяет типы блоков:
     * `text` - текстовые блоки
     * `title` - заголовки
     * `table` - таблицы
     * `figure` - изображения/графики
     * `list` - списки
     * `formula` - математические формулы

3. Выделение регионов:
   - Возвращает координаты каждого региона
   - Тип региона
   - Уверенность обнаружения

**Пример выхода:**
```json
{
  "regions": [
    {
      "type": "title",
      "bbox": [100, 50, 500, 100],
      "confidence": 0.98
    },
    {
      "type": "text",
      "bbox": [100, 120, 500, 300],
      "confidence": 0.95
    }
  ]
}
```

**3.2. Text Recognition (PaddleOCR-VL-0.9B):**

```
Регион изображения → Vision Encoder → Language Model → Текст
```

**Процесс:**

1. Извлечение региона:
   - Обрезка изображения по координатам
   - Предобработка для VLM

2. VLM Recognition:
   - Vision Encoder обрабатывает изображение региона
   - Language Model генерирует текст
   - Понимание контекста документа

3. Результат:
   - Распознанный текст
   - Координаты символов (опционально)
   - Уверенность распознавания

**Пример выхода:**
```json
{
  "text": "Пример распознанного текста",
  "bbox": [100, 120, 500, 300],
  "confidence": 0.95,
  "chars": [...]
}
```

**3.3. Post-processing:**

1. Структурирование:
   - Сохранение порядка блоков
   - Учет иерархии (заголовки, списки)
   - Обработка таблиц

2. Генерация Markdown:
   ```markdown
   # Заголовок
   
   Обычный текст
   
   | Таблица | Столбец 2 |
   |---------|-----------|
   | Данные  | Значение  |
   ```

3. Генерация JSON:
   - Структурированные данные
   - Координаты всех элементов
   - Метаданные

---

#### Шаг 4: Сохранение результатов

```python
result.save_to_markdown(save_path)
result.save_to_json(save_path)
```

**Результаты:**
- Локально: `/app/output/{timestamp}_{filename}.md`
- Опционально: S3 bucket (если настроено)

---

## 🔍 Проблемы в процессе извлечения текста

### Проблема 1: HTTP 502 при последовательной обработке

**Симптомы:**
- Первая страница PDF: ✅ Успешно
- Вторая страница PDF: ❌ HTTP 502 (EOF)
- Service Info: ❌ HTTP 502

**Анализ:**

**Временная шкала:**
```
T+0:    Начало обработки страницы 1
T+32:   Страница 1 завершена ✅
T+32:   Начало обработки страницы 2
T+62+:  Страница 2 в процессе обработки
T+62+:  Прокси разрывает соединение (таймаут) ❌
T+62+:  Сервер продолжает обработку
```

**Возможные причины:**

1. **Таймаут прокси/балансировщика:**
   - Прокси имеет таймаут ~30-60 секунд
   - Обработка занимает больше времени
   - Прокси разрывает соединение

2. **Проблема с памятью GPU:**
   - После первой обработки память может быть перегружена
   - Вторая обработка требует больше времени
   - Может вызвать проблемы

3. **Долгая обработка:**
   - 32+ секунд может превышать таймауты
   - Балансировщик считает соединение мертвым

**Решения:**

1. **Увеличить таймауты:**
   - На стороне клиента: 600+ секунд
   - На стороне прокси/балансировщика

2. **Добавить задержки:**
   ```python
   time.sleep(5)  # Между запросами
   ```

3. **Обрабатывать по одной странице:**
   - Разделить на отдельные запросы
   - Обрабатывать с паузами

---

### Проблема 2: Долгая обработка

**Симптомы:**
- OCR: ~100 секунд
- PDF страница: ~30 секунд

**Причина:**
- Большие модели требуют времени
- Сложные вычисления на GPU
- Это нормально для таких моделей

**Это проблема?**
- ❌ Нет, это нормально
- ⚠️ Но нужно учитывать при настройке таймаутов

---

### Проблема 3: Base64 вызывает 502

**Симптомы:**
- Base64 запросы возвращают 502
- Multipart работает стабильно

**Причина:**
- Base64 увеличивает размер на ~33%
- Большие данные могут вызвать проблемы

**Решение:**
- ✅ Использовать Multipart upload
- ✅ Избегать Base64 для больших файлов

---

## 🔄 Логика вызова моделей

### Для версии 1.3.6 (текущая):

#### Инициализация (один раз):

```
1. Запуск FastAPI сервера
   ↓
2. Фоновая инициализация (через 10 сек)
   ↓
3. Создание PaddleOCRVL() объекта
   ↓
4. Загрузка PP-DocLayoutV2
   ├─> Проверка кэша: /home/paddleocr/.paddlex/official_models/PP-DocLayoutV2
   ├─> Загрузка весов
   └─> Инициализация модели
   ↓
5. Загрузка PaddleOCR-VL-0.9B
   ├─> Проверка кэша: /home/paddleocr/.paddlex/official_models/PaddleOCR-VL
   ├─> Загрузка config.json
   ├─> Загрузка model.safetensors (~2-3GB)
   ├─> Инициализация 18 слоев
   └─> Настройка GQA
   ↓
6. Модели готовы к работе
```

#### Обработка запроса:

```
1. HTTP POST /ocr
   ↓
2. Сохранение изображения во временный файл
   ↓
3. Вызов paddle_ocr.predict(image_path)
   ↓
4. Внутри predict():
   
   a. Layout Detection (PP-DocLayoutV2):
      ├─> Предобработка изображения
      ├─> Анализ структуры
      ├─> Определение типов блоков
      └─> Выделение регионов с координатами
      ↓
   
   b. Text Recognition (PaddleOCR-VL-0.9B):
      ├─> Для каждого текстового региона:
      │   ├─> Извлечение региона
      │   ├─> Vision Encoder обработка
      │   ├─> Language Model генерация
      │   └─> Распознавание текста
      └─> Сбор результатов
      ↓
   
   c. Post-processing:
      ├─> Структурирование результатов
      ├─> Генерация Markdown
      └─> Генерация JSON
      ↓
   
5. Возврат результата
   ↓
6. Сохранение в файлы
   ↓
7. Отправка ответа клиенту
```

---

## 📦 Структура инфраструктуры

### Модели в офлайн-образе:

```
/home/paddleocr/.paddlex/official_models/
├── PP-DocLayoutV2/
│   ├── Конфигурация
│   ├── Веса модели (~500MB)
│   └── Метаданные
│
└── PaddleOCR-VL/
    ├── config.json (конфигурация модели)
    ├── generation_config.json (конфигурация генерации)
    ├── model.safetensors (веса модели, ~2-3GB)
    └── Другие файлы
```

### Компоненты по ролям:

**1. PaddlePaddle Framework:**
- Низкоуровневая работа с тензорами
- GPU вычисления
- Управление памятью

**2. PaddleX:**
- Управление моделями
- Загрузка из репозиториев
- Кэширование моделей

**3. PP-DocLayoutV2:**
- Определение структуры документа
- Выделение регионов

**4. PaddleOCR-VL-0.9B:**
- Распознавание текста
- Понимание контекста

**5. FastAPI Handler:**
- HTTP API
- Обработка запросов
- Сохранение результатов

---

## 📊 Сравнение версий

### Версия 1.3.6 (текущая):

**Базовый образ:**
```
paddleocr-vl:latest-gpu-sm120-offline
```

**Компоненты:**
- ✅ PaddlePaddle (встроен)
- ✅ PaddleX (встроен)
- ✅ PP-DocLayoutV2 (предзагружен)
- ✅ PaddleOCR-VL-0.9B (предзагружен)
- ✅ FastAPI Handler (наш код)

**Логика вызова:**
- Ленивая инициализация
- Фоновая инициализация
- Прямой вызов через PaddlePaddle

---

## 🔍 Проблемы в процессе извлечения текста

### Известные проблемы:

1. **HTTP 502 при последовательной обработке:**
   - Симптом: Вторая страница падает с 502
   - Решение: Увеличить таймауты, добавить задержки

2. **Долгая обработка:**
   - Симптом: 30-100 секунд на изображение
   - Решение: Это нормально, учитывать при настройке таймаутов

3. **Base64 вызывает 502:**
   - Симптом: Base64 запросы падают
   - Решение: Использовать Multipart

---

## 💡 Рекомендации

### Для стабильной работы:

1. **Использовать Multipart upload**
2. **Увеличить таймауты до 600+ секунд**
3. **Добавлять задержки между запросами (5-10 сек)**
4. **Обрабатывать по одной странице за раз**

---

**Версия документа:** 1.0  
**Последнее обновление:** 06.12.2025

