# Версия 1.3.4 - Релиз с исправлением static graph mode

**Дата:** 05.12.2025  
**Версия:** 1.3.4

## ✅ Изменения

### Исправление проблемы "static graph mode"

**Проблема:**
```
"int(Tensor) is not supported in static graph mode"
```

**Решение:**
Добавлены переменные окружения в `start.sh` для принудительного использования dynamic graph mode:

```bash
export FLAGS_enable_eager_mode=1      # Включает dynamic graph mode
export FLAGS_eager_delete_tensor_gb=0 # Отключает агрессивную очистку памяти
export FLAGS_use_mkldnn=0             # Отключает MKLDNN
```

### Другие изменения

- Обновлена версия в `server.py` до 1.3.4
- Сохранена фоновая инициализация из версии 1.3.3

## 📦 Образ

**URI:** `docling-granite-258m.cr.cloud.ru/paddleocr-vl-service:1.3.4`

**Базовый образ:** `ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest-gpu-sm120-offline`

## 🧪 Тестирование

### Локальное тестирование:

1. ✅ Образ собран успешно
2. ✅ Контейнер запускается
3. ✅ Health check доступен

### Ожидаемые результаты:

- ✅ Нет ошибки "static graph mode"
- ✅ OCR запросы обрабатываются корректно
- ✅ Модели загружаются из кэша

## 📋 Развертывание

### Настройки Cloud.ru ML Inference:

**Health Check:**
- Startup Probe: `failureThreshold=30` (до 5 минут)
- Liveness Probe: `initialDelaySeconds=60`
- Readiness Probe: `initialDelaySeconds=30`

**Ресурсы:**
- Минимум 8GB памяти (16GB рекомендуется)
- GPU обязателен

**Переменные окружения:**
- Автоматически устанавливаются в `start.sh`
- Не требуют ручной настройки

## 📝 Примечания

Если проблема "static graph mode" сохраняется, рекомендуется:
1. Проверить версию PaddlePaddle в базовом образе
2. Рассмотреть переход на базовый образ `paddle:3.0.0-gpu-cuda12.6` с ручной установкой PaddleOCR-VL

---

**Статус:** ✅ Готово к развертыванию

