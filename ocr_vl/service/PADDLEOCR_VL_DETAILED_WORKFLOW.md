# Детальный workflow PaddleOCR-VL: как работает извлечение текста

**Дата:** 06.12.2025  
**Версия:** 1.3.6

## 🔄 Полный процесс обработки изображения

### Этап 1: Получение и подготовка изображения

```
Клиент → FastAPI → Сохранение → PaddleOCR-VL
```

**Шаги:**

1. **HTTP запрос приходит в `/ocr` endpoint:**
   ```python
   POST /ocr
   Content-Type: multipart/form-data
   
   {
     "file": <изображение>,
     или
     "image_base64": "data:image/jpeg;base64,...",
     или
     "image_url": "https://..."
   }
   ```

2. **FastAPI обрабатывает запрос:**
   ```python
   # server.py, строка 495
   @app.post("/ocr")
   async def process_ocr(
       image_base64: Optional[str] = Form(None),
       image_url: Optional[str] = Form(None),
       file: Optional[UploadFile] = File(None)
   ):
   ```

3. **Конвертация в файл:**
   ```python
   # Base64 → PIL Image → файл
   # URL → скачивание → PIL Image → файл
   # Multipart → файл напрямую
   
   temp_image_path = TEMP_DIR / f"tmp_{uuid}.jpg"
   image.save(temp_image_path)
   ```

### Этап 2: Инициализация PaddleOCR-VL (ленивая)

```
Первый запрос → Инициализация → Загрузка моделей
```

**Шаги:**

1. **Проверка инициализации:**
   ```python
   # server.py, строка 322
   ocr = init_paddleocr()  # Ленивая инициализация
   ```

2. **Инициализация PaddleOCR-VL:**
   ```python
   # server.py, строка 156
   paddle_ocr = PaddleOCRVL()
   ```

3. **Внутри PaddleOCRVL():**
   ```
   a. Загрузка PP-DocLayoutV2:
      - Путь: /home/paddleocr/.paddlex/official_models/PP-DocLayoutV2
      - Размер: ~500MB
      - Назначение: Определение структуры документа
   
   b. Загрузка PaddleOCR-VL-0.9B:
      - Путь: /home/paddleocr/.paddlex/official_models/PaddleOCR-VL
      - Размер: ~2-3GB
      - Назначение: Распознавание текста
   
   c. Инициализация процессоров:
      - Image preprocessor
      - Text post-processor
      - Markdown formatter
   ```

### Этап 3: Обработка изображения

```
Изображение → Layout Detection → Text Recognition → Post-processing
```

**Шаг 1: Layout Detection (PP-DocLayoutV2)**

```python
# Внутри paddle_ocr.predict():

1. Предобработка изображения:
   - Ресайз до оптимального размера
   - Нормализация
   - Конвертация в тензор

2. Layout Detection:
   - PP-DocLayoutV2 анализирует изображение
   - Определяет типы блоков:
     * Text (текстовые блоки)
     * Title (заголовки)
     * Table (таблицы)
     * Figure (изображения)
     * List (списки)
   
   - Возвращает координаты регионов:
     {
       "type": "text",
       "bbox": [x1, y1, x2, y2],
       "confidence": 0.95
     }
```

**Шаг 2: Text Recognition (PaddleOCR-VL-0.9B)**

```python
# Для каждого текстового региона:

1. Извлечение региона:
   - Обрезка изображения по координатам
   - Предобработка для VLM

2. VLM Recognition:
   - PaddleOCR-VL-0.9B обрабатывает регион
   - Распознает текст с пониманием контекста
   - Возвращает:
     * Распознанный текст
     * Координаты символов
     * Уверенность распознавания

3. Обработка таблиц:
   - Специальная обработка табличных блоков
   - Определение структуры ячеек
   - Сохранение табличной структуры
```

**Шаг 3: Post-processing**

```python
# Форматирование результатов:

1. Структурирование текста:
   - Сохранение порядка блоков
   - Учет иерархии (заголовки, списки)
   - Обработка таблиц

2. Генерация Markdown:
   - Заголовки: # Заголовок
   - Текст: обычный текст
   - Таблицы: Markdown таблицы
   - Списки: - элемент

3. Генерация JSON:
   - Структурированные данные
   - Координаты всех элементов
   - Метаданные
```

### Этап 4: Сохранение результатов

```
Результаты → Локальное сохранение → S3 (опционально)
```

**Шаги:**

1. **Сохранение в Markdown:**
   ```python
   # server.py, строка 384
   results.save_to_markdown(save_path=str(md_path))
   ```

2. **Сохранение в JSON:**
   ```python
   # server.py, строка 391
   results.save_to_json(save_path=str(json_path))
   ```

3. **Загрузка в S3 (если настроено):**
   ```python
   # server.py, строка 571
   s3_paths = upload_to_cloudru(...)
   ```

---

## 🧩 Детали работы моделей

### PP-DocLayoutV2 (Layout Detection)

**Архитектура:**
- Основа: DETR (Detection Transformer)
- Input: Изображение (RGB)
- Output: Список регионов с типами

**Процесс:**

```
Изображение → Encoder → Decoder → Detection Head → Регионы
```

**Типы регионов:**
- `text` - обычный текст
- `title` - заголовки
- `table` - таблицы
- `figure` - изображения/графики
- `list` - списки
- `formula` - математические формулы

**Пример выхода:**
```json
{
  "regions": [
    {
      "type": "title",
      "bbox": [100, 50, 500, 100],
      "confidence": 0.98
    },
    {
      "type": "text",
      "bbox": [100, 120, 500, 300],
      "confidence": 0.95
    }
  ]
}
```

### PaddleOCR-VL-0.9B (Text Recognition)

**Архитектура:**
- Основа: Qwen-VL (Vision-Language Model)
- Параметров: 0.9B
- Input: Изображение региона + контекст
- Output: Распознанный текст

**Процесс:**

```
Регион изображения → Vision Encoder → Language Model → Текст
```

**Особенности:**
- Понимает контекст документа
- Распознает текст с учетом структуры
- Поддерживает многоязычность
- Сохраняет форматирование

**Пример выхода:**
```json
{
  "text": "Пример распознанного текста",
  "bbox": [100, 120, 500, 300],
  "confidence": 0.95,
  "chars": [
    {"char": "П", "bbox": [100, 120, 110, 140]},
    ...
  ]
}
```

---

## 📊 Временная диаграмма обработки

```
Время →
│
├─ 0ms:    Получение HTTP запроса
├─ 10ms:   Конвертация в файл
├─ 50ms:   Инициализация PaddleOCR-VL (если первый раз: +80 сек)
├─ 100ms:  Предобработка изображения
├─ 500ms:  Layout Detection (PP-DocLayoutV2)
├─ 2000ms: Text Recognition (PaddleOCR-VL-0.9B) - зависит от количества регионов
├─ 2500ms: Post-processing
├─ 2600ms: Сохранение результатов
└─ 2700ms: Отправка ответа клиенту

Итого: ~3 секунды для одного изображения
(плюс 80-90 секунд при первой инициализации)
```

---

## 🔧 Конфигурация и оптимизация

### Переменные окружения

```bash
# Пути к моделям
PADDLEX_HOME=/home/paddleocr/.paddlex
HF_HOME=/home/paddleocr/.cache/huggingface

# Настройки PaddlePaddle
FLAGS_enable_eager_mode=1        # Dynamic graph mode
FLAGS_eager_delete_tensor_gb=0   # Не удалять тензоры агрессивно
FLAGS_use_mkldnn=0               # Отключить MKLDNN
```

### Оптимизация производительности

1. **Использование GPU:**
   - Модели автоматически используют GPU если доступен
   - CPU режим также поддерживается (медленнее)

2. **Кэширование:**
   - Модели загружаются один раз при инициализации
   - Повторные запросы используют кэш

3. **Размер изображения:**
   - Рекомендуется: до 2048x2048 пикселей
   - Большие изображения автоматически ресайзятся

---

## 📝 Пример полного процесса

### Входные данные:

```
Файл: document.jpg (1920x1080, текст с таблицей)
```

### Процесс:

1. **Загрузка:**
   ```
   POST /ocr
   Content-Type: multipart/form-data
   file: document.jpg
   ```

2. **Layout Detection:**
   ```
   PP-DocLayoutV2 находит:
   - Заголовок: [0, 0, 1920, 100]
   - Текст: [0, 120, 1920, 600]
   - Таблица: [0, 620, 1920, 900]
   ```

3. **Text Recognition:**
   ```
   Заголовок → "Отчет о продажах"
   Текст → "Продажи увеличились на 20%..."
   Таблица → [
     ["Продукт", "Количество", "Цена"],
     ["A", "100", "1000"],
     ...
   ]
   ```

4. **Результат:**
   ```markdown
   # Отчет о продажах
   
   Продажи увеличились на 20%...
   
   | Продукт | Количество | Цена |
   |---------|------------|------|
   | A       | 100        | 1000 |
   ```
   ```

---

**Версия документа:** 1.0  
**Последнее обновление:** 06.12.2025

