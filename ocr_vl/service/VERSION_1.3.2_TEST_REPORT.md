# Отчет о тестировании версии 1.3.2

**Дата:** 05.12.2025  
**Версия:** 1.3.2

## ✅ Статус развертывания

### Сервер запущен:

- ✅ FastAPI handler работает
- ✅ Health check доступен
- ✅ Переменные окружения установлены
- ✅ PADDLEX_HOME=/home/paddleocr/.paddlex

## ⚠️ Проблема при обработке OCR

### Ошибка 502 (Bad Gateway):

**При попытке обработки OCR запроса:**
- ❌ Статус: 502 Bad Gateway
- ❌ EOF в ответе

### Возможные причины:

1. **Сервер падает при инициализации:**
   - Ошибка при загрузке моделей
   - Несовместимость версий
   - Недостаточно памяти/ресурсов

2. **Таймаут:**
   - Инициализация занимает слишком много времени
   - Прокси/балансировщик прерывает долгие запросы

3. **Ошибка в коде:**
   - Исключение при инициализации PaddleOCR-VL
   - Не обрабатывается корректно

## 📋 Рекомендации для диагностики

### 1. Проверить логи контейнера на Cloud.ru

При попытке обработки OCR запроса в логах должны быть:
- Сообщение "Initializing PaddleOCR-VL (lazy initialization)..."
- Установка PADDLEX_HOME
- Ошибка (если есть) при инициализации

### 2. Проверить ресурсы

- Достаточно ли памяти (8GB+ рекомендуется)
- Достаточно ли места на диске
- Доступен ли GPU

### 3. Увеличить timeout

В настройках ML Inference:
- Увеличить timeout для запросов
- Увеличить initialDelaySeconds для health check

## 📝 Следующие шаги

1. ✅ Запросить логи контейнера при ошибке 502
2. ⏳ Проверить, что происходит при инициализации
3. ⏳ Найти конкретную ошибку и исправить

---

**Статус:** ⚠️ Требуется диагностика - проверить логи контейнера при ошибке 502

