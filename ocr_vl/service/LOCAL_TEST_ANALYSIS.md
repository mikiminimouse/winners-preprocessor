# Анализ локального тестирования образа

**Дата:** 05.12.2025  
**Образ:** `paddleocr-vl-service:latest` (digest: 13a378427714)

## ✅ Что работает локально

### 1. FastAPI сервер: ✅ РАБОТАЕТ

```
INFO:     Started server process [1]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8081
```

- ✅ Сервер запустился успешно
- ✅ Порт 8081 слушается
- ✅ Процесс работает: `uvicorn server:app`

### 2. Health Check: ✅ РАБОТАЕТ

```json
{
  "status": "healthy",
  "paddleocr": "not_initialized",
  "s3_storage": "not_configured",
  "output_dir": "/app/output",
  "temp_dir": "/app/temp",
  "docs": "/docs - Swagger UI для тестирования API"
}
```

- ✅ HTTP 200 OK
- ✅ Endpoint отвечает корректно

### 3. Импорт PaddleOCR-VL: ✅ РАБОТАЕТ

```
PaddleOCRVL imported successfully
```

- ✅ PaddleOCR-VL импортируется без ошибок
- ✅ Нет ошибок torch/NCCL при импорте

### 4. Файловая система: ✅ РАБОТАЕТ

```
TEMP_DIR is writable: /app/temp
OUTPUT_DIR is writable: /app/output
```

- ✅ Директории доступны для записи
- ✅ Права доступа корректны

### 5. Веб-интерфейсы: ✅ РАБОТАЮТ

- ✅ `/docs` - Swagger UI
- ✅ `/redoc` - ReDoc
- ✅ `/openapi.json` - OpenAPI schema
- ✅ `/` - Корневой endpoint с информацией

## ⚠️ Ожидаемые проблемы (локально без GPU)

### vLLM сервер: ❌ Не запускается (ожидаемо)

```
ImportError: libcuda.so.1: cannot open shared object file: No such file or directory
```

**Причина:** Отсутствие GPU/CUDA драйверов локально  
**Влияние:** Не критично - vLLM не используется напрямую, используется прямой вызов PaddleOCR-VL  
**Статус:** Это нормальное поведение без GPU

### PaddleOCR-VL инициализация: ⚠️ Не протестирована

- PaddleOCR-VL инициализируется при первом запросе (ленивая инициализация)
- Без GPU может быть проблема при инициализации
- Нужно протестировать с реальным запросом

## 🔍 Почему может не запускаться на Cloud.ru ML Inference

### Возможные причины:

1. **Torch/NCCL проблема**
   - Ошибка: `undefined symbol: ncclCommWindowRegister`
   - Статус: Попытка исправления через переустановку torch
   - Требуется: Проверка на сервере с GPU

2. **Права доступа** 
   - Исправлено в последней версии
   - Но может быть проблема с пользователем контейнера

3. **Инициализация при старте**
   - Проблема может возникать при первом OCR запросе
   - Нужно проверить логи на сервере

4. **Health check таймауты**
   - Если инициализация занимает больше времени
   - Нужно увеличить таймауты

5. **Переустановка torch**
   - Может конфликтовать с существующими библиотеками в базовом образе
   - Нужно проверить совместимость

## 📊 Результаты тестирования

| Компонент | Локально (без GPU) | Cloud.ru (с GPU) | Статус |
|-----------|-------------------|------------------|--------|
| FastAPI запуск | ✅ | ⚠️ | Требует проверки |
| Health check | ✅ | ⚠️ | Требует проверки |
| Импорт PaddleOCR | ✅ | ❌ | Проблема torch/NCCL |
| vLLM сервер | ❌ (нормально) | ❌ | Проблема torch/NCCL |
| Файловая система | ✅ | ✅ | Исправлено |
| Веб-интерфейсы | ✅ | ⚠️ | Требует проверки |

## 🔧 Рекомендации для Cloud.ru

### 1. Проверить логи на сервере:

```bash
# В Cloud.ru ML Inference → Логи
# Ищите:
- "PaddleOCRVL imported successfully" - должно быть
- "ImportError: torch" - проблема torch/NCCL
- "TEMP_DIR is writable" - должно быть
- "Application startup complete" - должно быть
```

### 2. Проверить инициализацию PaddleOCR:

- После запуска контейнера отправьте тестовый OCR запрос
- Проверьте логи на ошибки инициализации

### 3. Увеличить таймауты:

Если инициализация занимает время:
- Startup probe: `failureThreshold: 40-50`
- Initial delay: `180-240` секунд

### 4. Проверить переустановку torch:

Может потребоваться более аккуратная переустановка или использование версии из базового образа.

## ✅ Итоговый статус

**Локально (без GPU):**
- ✅ FastAPI работает
- ✅ Health check работает
- ✅ Импорт PaddleOCR работает
- ❌ vLLM не работает (ожидаемо без GPU)
- ⚠️ Инициализация PaddleOCR не протестирована

**На Cloud.ru (с GPU):**
- ⚠️ Требуется проверка логов
- ⚠️ Возможна проблема torch/NCCL
- ⚠️ Возможна проблема инициализации

## 📝 Следующие шаги

1. Проверить логи Cloud.ru ML Inference на конкретные ошибки
2. Отправить тестовый OCR запрос после запуска
3. Проверить, инициализируется ли PaddleOCR при первом запросе
4. При необходимости доработать переустановку torch

