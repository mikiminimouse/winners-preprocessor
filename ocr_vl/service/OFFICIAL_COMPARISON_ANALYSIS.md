# Анализ соответствия официальному подходу PaddleOCR-VL

**Дата:** 05.12.2025  
**Источник:** Официальная документация PaddleOCR-VL Usage Tutorial

## 🔴 КРИТИЧЕСКИЕ ОТКЛОНЕНИЯ

### 1. ❌ Неправильный базовый образ

**Официальный подход:**
```dockerfile
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-genai-vllm-server:latest
# или для API сервиса
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest
```

**Наш подход:**
```dockerfile
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddlex-genai-vllm-server:latest
```

**Проблема:**
- Используем **`paddlex-genai-vllm-server`** вместо **`paddleocr-genai-vllm-server`**
- Это **разные образы** с разными командами и зависимостями
- Может быть причиной проблем с torch/NCCL и инициализацией

**Решение:** Заменить базовый образ на официальный

---

### 2. ❌ Неправильная команда запуска vLLM сервера

**Официальный подход:**
```bash
paddleocr genai_server --model_name PaddleOCR-VL-0.9B --backend vllm --port 8118
```

**Наш подход:**
```bash
paddlex_genai_server --model_name PaddleOCR-VL-0.9B --backend vllm --port 8080
```

**Проблема:**
- Используем `paddlex_genai_server` вместо `paddleocr genai_server`
- Это разные команды из разных пакетов
- Команда может не существовать в образе или работать некорректно

**Решение:** Использовать официальную команду `paddleocr genai_server`

---

### 3. ❌ НЕПРАВИЛЬНОЕ ИСПОЛЬЗОВАНИЕ PaddleOCR-VL

**Официальный подход:**
```python
# При использовании vLLM сервера для ускорения:
pipeline = PaddleOCRVL(
    vl_rec_backend="vllm-server", 
    vl_rec_server_url="http://127.0.0.1:8118/v1"
)

# Или при использовании без vLLM (только PaddlePaddle):
pipeline = PaddleOCRVL()  # Использует PaddlePaddle напрямую
```

**Наш подход:**
```python
# Запускаем vLLM сервер в фоне, но НЕ используем его!
paddle_ocr = PaddleOCRVL()  # Инициализация без подключения к vLLM
```

**КРИТИЧЕСКАЯ ПРОБЛЕМА:**
- Мы запускаем vLLM сервер в фоне, но **НЕ ПОДКЛЮЧАЕМСЯ к нему**!
- PaddleOCR-VL инициализируется без параметров `vl_rec_backend` и `vl_rec_server_url`
- Это означает, что мы используем **медленный PaddlePaddle бэкенд** вместо **быстрого vLLM**
- vLLM сервер работает впустую и потребляет ресурсы

**Решение:** 
1. Либо использовать vLLM сервер правильно (передавать `vl_rec_backend` и `vl_rec_server_url`)
2. Либо не запускать vLLM сервер вообще, если используем прямой PaddlePaddle

---

### 4. ❌ Неправильная архитектура развертывания

**Официальный подход (рекомендуемый):**
- **Docker Compose** с двумя отдельными контейнерами:
  1. Контейнер vLLM сервера (порт 8118)
  2. Контейнер PaddleOCR-VL API сервиса (порт 8080)
- Контейнеры общаются через сеть Docker

**Наш подход:**
- Один контейнер с двумя процессами:
  1. Процесс vLLM сервера в фоне (порт 8080)
  2. Процесс FastAPI handler (порт 8081)
- Процессы общаются через localhost

**Проблемы:**
- Не соответствует официальной рекомендации
- Сложнее масштабировать
- Смешивание ответственности

**Однако:** Для монолитного развертывания наш подход технически корректен, но нужно исправить использование vLLM.

---

## ⚠️ ЧАСТИЧНЫЕ ОТКЛОНЕНИЯ

### 5. Прямая инициализация вместо подключения к сервису

**Официальный подход:**
- Для production рекомендуется использовать vLLM/SGLang/FastDeploy для ускорения
- Клиент подключается к серверу через `vl_rec_backend="vllm-server"`

**Наш подход:**
- Используем прямую инициализацию без ускорения vLLM
- Это медленнее, но проще

**Оценка:** ⚠️ Работает, но не оптимально для production

---

### 6. Использование PaddleX вместо PaddleOCR CLI

**Официальный подход:**
```bash
paddleocr genai_server --model_name PaddleOCR-VL-0.9B --backend vllm --port 8118
```

**Наш подход:**
```bash
paddlex_genai_server --model_name PaddleOCR-VL-0.9B --backend vllm --port 8080
```

**Проблема:** Разные CLI инструменты могут иметь разные параметры и поведение

---

## ✅ ЧТО ДЕЛАЕТСЯ ПРАВИЛЬНО

### 1. Установка зависимостей
- ✅ `paddlepaddle-gpu==3.2.1` - правильная версия для CUDA 12.6
- ✅ `paddleocr[doc-parser]` - правильная установка с поддержкой документного парсинга
- ✅ Специальный safetensors для CUDA 12.6 - соответствует документации

### 2. Использование PaddleOCR-VL API
- ✅ Импорт `from paddleocr import PaddleOCRVL` - правильный
- ✅ Использование `pipeline.predict()` - правильный метод
- ✅ Обработка результатов через `result.save_to_markdown()` и `result.save_to_json()` - правильные методы

### 3. Обработка входных данных
- ✅ Поддержка Base64, URL, multipart/form-data
- ✅ Правильная декодировка изображений

---

## 🔧 РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ

### Приоритет 1: Критические исправления

#### 1.1 Заменить базовый образ
```dockerfile
# БЫЛО:
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddlex-genai-vllm-server:latest

# ДОЛЖНО БЫТЬ (для vLLM сервера):
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-genai-vllm-server:latest

# ИЛИ (для простого использования без vLLM):
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest
```

#### 1.2 Исправить команду запуска vLLM
```bash
# БЫЛО:
paddlex_genai_server --model_name PaddleOCR-VL-0.9B --backend vllm --port 8080 &

# ДОЛЖНО БЫТЬ:
paddleocr genai_server --model_name PaddleOCR-VL-0.9B --backend vllm --host 0.0.0.0 --port 8118 &
```

#### 1.3 Подключить PaddleOCR-VL к vLLM серверу
```python
# БЫЛО:
paddle_ocr = PaddleOCRVL()

# ДОЛЖНО БЫТЬ (если используем vLLM):
paddle_ocr = PaddleOCRVL(
    vl_rec_backend="vllm-server",
    vl_rec_server_url="http://localhost:8118/v1"
)
```

### Приоритет 2: Улучшения архитектуры

#### 2.1 Рассмотреть разделение на два контейнера
- Использовать Docker Compose
- Отдельный контейнер для vLLM сервера
- Отдельный контейнер для API сервиса

#### 2.2 Исправить порты
- vLLM сервер: порт 8118 (как в документации)
- API сервис: порт 8080 (как в документации)

---

## 📊 СРАВНИТЕЛЬНАЯ ТАБЛИЦА

| Аспект | Официальный подход | Наш подход | Статус |
|--------|-------------------|------------|--------|
| Базовый образ | `paddleocr-genai-vllm-server` | `paddlex-genai-vllm-server` | ❌ Неправильно |
| Команда vLLM | `paddleocr genai_server` | `paddlex_genai_server` | ❌ Неправильно |
| Использование vLLM | `vl_rec_backend="vllm-server"` | Не используется | ❌ Неправильно |
| Архитектура | 2 контейнера (Compose) | 1 контейнер (2 процесса) | ⚠️ Работает, но не оптимально |
| Порты | 8118 (vLLM), 8080 (API) | 8080 (vLLM), 8081 (API) | ⚠️ Отличается |
| Установка зависимостей | `paddlepaddle-gpu==3.2.1` | `paddlepaddle-gpu==3.2.1` | ✅ Правильно |
| PaddleOCR установка | `paddleocr[doc-parser]` | `paddleocr[doc-parser]` | ✅ Правильно |
| Использование API | `PaddleOCRVL().predict()` | `PaddleOCRVL().predict()` | ✅ Правильно |

---

## 🎯 ИТОГОВЫЕ ВЫВОДЫ

### Основные проблемы:

1. **Используем неправильный базовый образ** - `paddlex` вместо `paddleocr`
2. **Неправильная команда запуска vLLM** - `paddlex_genai_server` вместо `paddleocr genai_server`
3. **НЕ используем vLLM сервер**, хотя запускаем его - это основная причина неоптимальной работы
4. **Неправильные порты** - используем 8080/8081 вместо 8118/8080

### Почему это критично:

- **Производительность:** Без подключения к vLLM используем медленный PaddlePaddle бэкенд
- **Совместимость:** Неправильный базовый образ может вызывать проблемы с зависимостями
- **Стабильность:** Неофициальные команды могут работать нестабильно или непредсказуемо

### Что нужно сделать:

1. ✅ Заменить базовый образ на официальный `paddleocr-genai-vllm-server`
2. ✅ Исправить команду запуска vLLM на `paddleocr genai_server`
3. ✅ Подключить PaddleOCR-VL к vLLM серверу через параметры инициализации
4. ✅ Исправить порты согласно документации
5. ⚠️ Рассмотреть разделение на два контейнера (опционально)

---

## 📝 ПЛАН ИСПРАВЛЕНИЙ

### Шаг 1: Обновить Dockerfile
- Заменить базовый образ
- Исправить команду запуска vLLM
- Исправить порты

### Шаг 2: Обновить server.py
- Добавить параметры `vl_rec_backend` и `vl_rec_server_url` при инициализации
- Обновить URL vLLM сервера на правильный порт

### Шаг 3: Протестировать
- Проверить, что vLLM сервер запускается правильно
- Проверить, что PaddleOCR-VL подключается к vLLM
- Проверить производительность и стабильность

---

## 🔗 Ссылки на документацию

- [PaddleOCR-VL Usage Tutorial](https://www.paddleocr.ai/latest/en/version3.x/pipeline_usage/PaddleOCR-VL.html)
- [3.1 Launching the VLM Inference Service](https://www.paddleocr.ai/latest/en/version3.x/pipeline_usage/PaddleOCR-VL.html#31-launching-the-vlm-inference-service)
- [3.2 Client Usage Methods](https://www.paddleocr.ai/latest/en/version3.x/pipeline_usage/PaddleOCR-VL.html#32-client-usage-methods)
- [4. Service Deployment](https://www.paddleocr.ai/latest/en/version3.x/pipeline_usage/PaddleOCR-VL.html#4-service-deployment)

