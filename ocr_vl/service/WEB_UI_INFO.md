# Веб-интерфейс для PaddleOCR-VL Service

## ✅ Встроенный веб-интерфейс

FastAPI автоматически предоставляет **встроенные веб-интерфейсы** для использования сервиса:

### 1. Swagger UI (Рекомендуется)

**URL:** `https://your-endpoint.modelrun.inference.cloud.ru/docs`

**Описание:**
- ✅ Интерактивная документация API
- ✅ Возможность тестировать все endpoints прямо в браузере
- ✅ Загрузка файлов через веб-интерфейс
- ✅ Просмотр схем запросов/ответов
- ✅ Автоматическая генерация из кода

**Использование:**
1. Откройте `/docs` в браузере
2. Найдите endpoint `POST /ocr`
3. Нажмите "Try it out"
4. Загрузите изображение через интерфейс
5. Нажмите "Execute" для отправки запроса

### 2. ReDoc

**URL:** `https://your-endpoint.modelrun.inference.cloud.ru/redoc`

**Описание:**
- ✅ Альтернативная документация API
- ✅ Более читаемый формат
- ✅ Подходит для просмотра документации

### 3. OpenAPI Schema

**URL:** `https://your-endpoint.modelrun.inference.cloud.ru/openapi.json`

**Описание:**
- JSON схема OpenAPI
- Используется для генерации клиентов
- Может быть импортирована в Postman, Insomnia и другие инструменты

### 4. Корневой endpoint

**URL:** `https://your-endpoint.modelrun.inference.cloud.ru/`

**Описание:**
- Информация о сервисе
- Список доступных endpoints
- Ссылки на документацию

## 🔐 Авторизация

При использовании через браузер:
- Если включена авторизация в ML Inference, нужно использовать API Key
- В Swagger UI можно добавить авторизацию через кнопку "Authorize"
- Или использовать заголовок: `Authorization: Bearer YOUR_API_KEY`

## 📝 Примеры использования

### Через Swagger UI:

1. Откройте `https://your-endpoint/docs`
2. Нажмите "Authorize" (если требуется)
3. Введите API Key: `Bearer YOUR_API_KEY`
4. Найдите `POST /ocr`
5. Выберите способ загрузки:
   - **file**: Загрузите файл через интерфейс
   - **image_url**: Введите URL изображения
   - **image_base64**: Вставьте Base64 строку
6. Нажмите "Execute"
7. Просмотрите результат

### Через curl:

```bash
# С авторизацией
curl -X POST "https://your-endpoint.modelrun.inference.cloud.ru/ocr" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -F "file=@image.jpg"
```

## ✅ Доступные endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/` | GET | Информация о сервисе |
| `/docs` | GET | Swagger UI (веб-интерфейс) |
| `/redoc` | GET | ReDoc документация |
| `/openapi.json` | GET | OpenAPI схема |
| `/health` | GET | Проверка здоровья |
| `/health/vllm` | GET | Проверка vLLM сервера |
| `/ocr` | POST | OCR обработка изображений |

## 🎯 Рекомендации

1. **Для тестирования:** Используйте `/docs` - самый удобный способ
2. **Для интеграции:** Используйте `/openapi.json` для генерации клиентов
3. **Для документации:** Используйте `/redoc` для просмотра

## ⚠️ Важно

- Swagger UI и ReDoc доступны **по умолчанию** в FastAPI
- Не требуют дополнительной установки или настройки
- Автоматически генерируются из кода
- Работают "из коробки" при запуске сервиса

## 📦 В текущей сборке

✅ **Swagger UI включен** - `/docs`  
✅ **ReDoc включен** - `/redoc`  
✅ **OpenAPI схема** - `/openapi.json`  
✅ **Корневой endpoint** - `/` с информацией о сервисе

Все веб-интерфейсы встроены и работают автоматически!

