# Отчет о тестировании PaddleOCR-VL-0.9B

**Дата:** 06.12.2025  
**Версия сервиса:** 1.3.6  
**URL:** `https://9525a16c-09c1-4489-87d3-bf1946792a53.modelrun.inference.cloud.ru`

## 📊 Результаты тестирования

### ✅ Успешные тесты (2/4):

1. **Health Check** ✅
   - Статус: healthy
   - PaddleOCR: ready
   - Время: <1 секунда

2. **OCR Multipart** ✅
   - Статус: success
   - Время обработки: **103.44 секунды**
   - Результаты: Markdown и JSON сохранены

### ⚠️ Частично успешные тесты (1/4):

3. **PDF to MD (Multipart)** ⚠️
   - Страница 1: ✅ Успешно (32.14 сек)
   - Страница 2: ❌ HTTP 502 (EOF)

### ❌ Неудачные тесты (1/4):

4. **Service Info** ❌
   - HTTP 502

---

## 🔍 Анализ работы PaddleOCR-VL-0.9B

### ✅ Что работает отлично:

1. **Инициализация:**
   - ✅ Модели загружены: PP-DocLayoutV2 и PaddleOCR-VL-0.9B
   - ✅ Сервис готов к работе
   - ✅ Health check показывает "ready"

2. **OCR обработка:**
   - ✅ Распознавание текста работает
   - ✅ Генерация Markdown работает
   - ✅ Генерация JSON работает
   - ✅ Multipart upload работает стабильно

3. **PDF конвертация:**
   - ✅ Конвертация PDF в изображения работает (pdf2image)
   - ✅ Обработка первой страницы успешна

### ⚠️ Проблемы:

1. **HTTP 502 при последовательной обработке:**
   - Первая страница: ✅ Успешно
   - Вторая страница: ❌ 502 ошибка
   - Возможные причины:
     * Таймаут на стороне прокси/балансировщика
     * Проблема с памятью GPU после первой обработки
     * Слишком долгая обработка

2. **Долгая обработка:**
   - OCR: ~103 секунды
   - PDF страница: ~32 секунды
   - Это нормально для больших моделей, но может вызывать таймауты

---

## 🧩 Компоненты, задействованные в обработке

### При обработке изображения:

```
Изображение
    ↓
PP-DocLayoutV2 (Layout Detection)
    ↓
    - Определяет структуру документа
    - Находит текстовые блоки
    - Выделяет таблицы, изображения
    - Возвращает координаты регионов
    ↓
PaddleOCR-VL-0.9B (Text Recognition)
    ↓
    - Распознает текст в каждом регионе
    - Понимает контекст документа
    - Генерирует структурированный текст
    ↓
Post-processing
    ↓
    - Форматирует в Markdown
    - Сохраняет структуру
    - Генерирует JSON с координатами
    ↓
Результат (Markdown + JSON)
```

---

## 📋 Проблемы в процессе извлечения текста

### Проблема 1: HTTP 502 при последовательной обработке

**Симптомы:**
- Первая страница PDF обрабатывается успешно
- Вторая страница возвращает HTTP 502 (EOF)
- Service Info также возвращает 502

**Возможные причины:**

1. **Таймаут на стороне прокси:**
   - Прокси разрывает соединение при долгой обработке
   - Таймаут может быть меньше времени обработки

2. **Проблема с памятью GPU:**
   - После первой обработки память может быть перегружена
   - Модель может быть не полностью освобождена

3. **Слишком долгая обработка:**
   - 32+ секунды на страницу может превышать таймауты
   - Балансировщик может считать соединение мертвым

**Решения:**

1. **Увеличить таймауты:**
   ```python
   timeout=600  # 10 минут
   ```

2. **Добавить задержку между запросами:**
   ```python
   time.sleep(5)  # Дать время на освобождение ресурсов
   ```

3. **Обрабатывать по одной странице:**
   - Разделить PDF на отдельные запросы
   - Обрабатывать с паузами

### Проблема 2: Долгая обработка

**Симптомы:**
- OCR: ~100 секунд
- PDF страница: ~30 секунд

**Причина:**
- Большие модели (PaddleOCR-VL-0.9B, PP-DocLayoutV2)
- Сложная обработка изображений
- GPU вычисления

**Это нормально?**
- ✅ Да, для таких моделей это нормально
- ⚠️ Но нужно учитывать при настройке таймаутов

**Решения:**
- Увеличить таймауты на стороне клиента
- Увеличить таймауты на стороне прокси/балансировщика
- Использовать асинхронную обработку

---

## 🎯 Рекомендации

### Для улучшения стабильности:

1. **Увеличить таймауты:**
   - Клиент: 600 секунд (10 минут)
   - Прокси/балансировщик: 600+ секунд

2. **Добавить задержки:**
   - 5-10 секунд между запросами
   - Дать время на освобождение ресурсов

3. **Обрабатывать последовательно:**
   - По одной странице за раз
   - С проверкой health check между запросами

4. **Оптимизировать изображения:**
   - Уменьшить DPI при конвертации PDF (200 вместо 300)
   - Использовать сжатие изображений

---

## 📄 Детальный анализ результатов

### Успешный OCR Multipart:

```
Время: 103.44 секунды
Статус: success
Результаты: Markdown и JSON сохранены
```

**Что происходило:**

1. Создание тестового изображения (мгновенно)
2. Отправка через Multipart (мгновенно)
3. Инициализация PaddleOCR-VL (если не была) (~10-30 сек)
4. Layout Detection через PP-DocLayoutV2 (~5-10 сек)
5. Text Recognition через PaddleOCR-VL-0.9B (~50-80 сек)
6. Post-processing и сохранение (~5-10 сек)

**Итого:** ~103 секунды - это нормально для первого запроса

### Успешная обработка PDF страницы 1:

```
Время: 32.14 секунды
Статус: success
Результат: Markdown сохранен
```

**Что происходило:**

1. Конвертация PDF страницы в изображение (мгновенно)
2. Отправка через Multipart (мгновенно)
3. Обработка через PaddleOCR-VL (~30 секунд)
4. Сохранение результатов (мгновенно)

**Быстрее чем первый OCR** потому что:
- Модели уже были загружены (warm-up)
- Изображение меньше по размеру

### Неудачная обработка PDF страницы 2:

```
Ошибка: HTTP 502 (EOF)
Время: ~30+ секунд (прервано)
```

**Что могло произойти:**

1. Началась обработка (сервер принял запрос)
2. Обработка заняла ~30+ секунд
3. Прокси/балансировщик разорвал соединение (таймаут)
4. Сервер продолжил обработку, но ответ не дошел до клиента

---

## 🔧 Как улучшить тестирование

### Обновленный скрипт с улучшениями:

1. **Увеличенные таймауты**
2. **Задержки между запросами**
3. **Retry механизм**
4. **Обработка по одной странице**

### Рекомендации:

1. **Тестировать по одной странице:**
   ```bash
   python3 test_single_page.py page_1.pdf
   python3 test_single_page.py page_2.pdf
   ```

2. **Использовать большие таймауты:**
   ```python
   timeout=600  # 10 минут
   ```

3. **Добавить проверки между запросами:**
   ```python
   # Проверить health перед каждым запросом
   check_health()
   time.sleep(5)  # Пауза
   process_next_page()
   ```

---

**Версия документа:** 1.0  
**Последнее обновление:** 06.12.2025

