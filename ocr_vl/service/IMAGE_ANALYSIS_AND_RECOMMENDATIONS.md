# Анализ и рекомендации по выбору Docker образа

**Дата:** 05.12.2025

## 🔍 Проблемы текущего решения

### Текущий базовый образ:
```
ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest-gpu-sm120-offline
```

### Проблемы:
1. ❌ **Ошибка "int(Tensor) is not supported in static graph mode"**
   - Возникает при обработке OCR запросов
   - Связана с режимом работы PaddlePaddle

2. ⚠️ **Потенциальная несовместимость версий**
   - Неизвестна точная версия PaddlePaddle в образе
   - Может быть конфликт с static graph mode

## 📊 Доступные официальные образы PaddleOCR-VL

### 1. Основные образы PaddleOCR-VL

#### A. `paddleocr-vl:latest` (основной образ)
```
ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest
```

**Характеристики:**
- ✅ Официальный образ от PaddlePaddle
- ✅ Включает все необходимые зависимости
- ✅ Оптимизирован для GPU
- ⚠️ Требует загрузки моделей при первом запуске

**Подходит для:**
- Разработки и тестирования
- Сетей с доступом к интернету

#### B. `paddleocr-vl:latest-gpu-sm120-offline` (офлайн версия)
```
ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest-gpu-sm120-offline
```

**Характеристики:**
- ✅ Модели предзагружены
- ✅ Работает без интернета
- ✅ Быстрый старт
- ⚠️ Больший размер образа

**Подходит для:**
- Production без интернета
- Изолированных сетей

#### C. `paddleocr-genai-vllm-server:latest` (для vLLM)
```
ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-genai-vllm-server:latest
```

**Характеристики:**
- ✅ Включает vLLM сервер
- ✅ OpenAI-совместимый API
- ⚠️ Больше зависимостей
- ⚠️ Требует больше ресурсов

**Подходит для:**
- Высокопроизводительного инференса
- С vLLM бэкендом

### 2. Базовые образы PaddlePaddle

#### A. PaddlePaddle 3.0.0 для CUDA 12.6
```
ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddle:3.0.0-gpu-cuda12.6-cudnn9.5-trt10.5
```

**Характеристики:**
- ✅ Последняя стабильная версия (3.0.0)
- ✅ CUDA 12.6 поддержка
- ✅ TensorRT 10.5 для ускорения
- ⚠️ Требует установки PaddleOCR-VL вручную

**Подходит для:**
- Кастомных сборок
- Полного контроля зависимостей

## 🎯 Рекомендации

### Рекомендация #1: Использовать официальный образ PaddleOCR-VL (ОФЛАЙН)

**Образ:**
```dockerfile
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest-gpu-sm120-offline
```

**Преимущества:**
- ✅ Официальный образ от PaddlePaddle
- ✅ Модели предзагружены
- ✅ Работает без интернета
- ✅ Оптимизирован для production

**Недостатки:**
- ⚠️ Больший размер образа
- ⚠️ Может иметь ту же проблему со static graph mode

**Изменения в Dockerfile:**
```dockerfile
# Базовый офлайн-образ с предзагруженными моделями
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest-gpu-sm120-offline

# Переключаемся на root для установки зависимостей
USER root

# Устанавливаем зависимости для FastAPI handler
COPY requirements.txt /tmp/requirements.txt
RUN python3 -m pip install --no-cache-dir -r /tmp/requirements.txt

# Создаем рабочую директорию
WORKDIR /app

# Создаем директории для выходных файлов
RUN mkdir -p /app/output /app/temp && \
    chmod -R 777 /app

# Копируем код FastAPI handler
COPY server.py /app/server.py

# Открываем порты
EXPOSE 8081

# Создаем скрипт запуска
RUN echo '#!/bin/bash\n\
set -e\n\
export PADDLEX_HOME=/home/paddleocr/.paddlex\n\
export HF_HOME=/home/paddleocr/.cache/huggingface\n\
cd /app\n\
exec uvicorn server:app --host 0.0.0.0 --port 8081 --log-level info\n\
' > /app/start.sh && chmod +x /app/start.sh

CMD ["/app/start.sh"]
```

### Рекомендация #2: Использовать базовый образ PaddlePaddle 3.0.0 + ручная установка

**Образ:**
```dockerfile
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddle:3.0.0-gpu-cuda12.6-cudnn9.5-trt10.5
```

**Преимущества:**
- ✅ Полный контроль версий
- ✅ Последняя стабильная версия PaddlePaddle
- ✅ Можно настроить dynamic graph mode
- ✅ Меньше несовместимостей

**Недостатки:**
- ⚠️ Требует установки PaddleOCR-VL вручную
- ⚠️ Больше шагов сборки

**Изменения в Dockerfile:**
```dockerfile
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddle:3.0.0-gpu-cuda12.6-cudnn9.5-trt10.5

# Устанавливаем PaddleOCR-VL
RUN python3 -m pip install --no-cache-dir paddleocr

# Предзагружаем модели (когда есть интернет)
RUN python3 << 'PYTHON_SCRIPT'
from paddleocr import PaddleOCRVL
print("Предзагрузка моделей PaddleOCR-VL...")
ocr = PaddleOCRVL()
print("✅ Модели предзагружены")
PYTHON_SCRIPT

# Остальные шаги...
```

## 🔧 Решение проблемы "static graph mode"

### Вариант 1: Отключить static graph mode в коде

В `server.py` при инициализации PaddleOCR-VL:

```python
import paddle
paddle.set_flags({'FLAGS_eager_delete_tensor_gb': 0})
# Отключаем static graph mode
paddle.enable_static()  # НЕ использовать
# Вместо этого используем dynamic graph (по умолчанию)
```

### Вариант 2: Использовать переменные окружения

В `start.sh`:

```bash
export FLAGS_enable_eager_mode=1
export FLAGS_eager_delete_tensor_gb=0
```

### Вариант 3: Обновить версию PaddlePaddle

Старые версии могут использовать static graph по умолчанию.
Обновление до PaddlePaddle 3.0.0 должно решить проблему.

## 📋 Финальная рекомендация

### ✅ Рекомендуемый подход: Комбинированный

**Использовать:**
1. Базовый образ: `paddleocr-vl:latest-gpu-sm120-offline`
2. Если проблема сохраняется, попробовать базовый `paddle:3.0.0-gpu-cuda12.6` с ручной установкой
3. Добавить переменные окружения для отключения static graph mode

**Приоритет:**
1. **Первый вариант:** Попробовать `paddleocr-vl:latest-gpu-sm120-offline` с добавлением переменных окружения для dynamic graph
2. **Второй вариант:** Если не поможет, перейти на `paddle:3.0.0-gpu-cuda12.6` с ручной установкой PaddleOCR-VL

## 🔍 Дополнительные исследования

### Проверить доступные теги:
```bash
# Проверить доступные версии
curl https://ccr-2vdh3abv-pub.cnc.bj.baidubce.com/v2/paddlepaddle/paddleocr-vl/tags/list
```

### Проверить документацию:
- Официальная документация PaddleOCR-VL
- Changelog версий PaddlePaddle
- Issues на GitHub связанные с static graph mode

---

**Статус:** Требуется тестирование с рекомендованными образами

