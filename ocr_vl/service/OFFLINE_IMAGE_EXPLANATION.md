# Объяснение офлайн-образа PaddleOCR-VL

**Дата:** 05.12.2025

## 📖 Что такое офлайн-образ?

### Определение

**Офлайн-образ** (offline image) — это Docker образ, который содержит **все необходимые модели предзагруженными** внутри образа, что позволяет работать **без доступа к интернету** в runtime.

### В контексте PaddleOCR-VL

Офлайн-образ для PaddleOCR-VL — это специальная версия официального образа, которая включает:

1. **Предзагруженные модели:**
   - PP-DocLayoutV2 (layout detection)
   - VLM recognition модели
   - Все компоненты pipeline
   - Все веса моделей сохранены в образе

2. **Все зависимости:**
   - PaddlePaddle
   - PaddleOCR
   - Все необходимые библиотеки

3. **Готовность к работе:**
   - Не требует загрузки моделей при запуске
   - Работает полностью автономно
   - Модели уже "запечены" в образе

## ✅ Преимущества офлайн-образа

### 1. Автономность
- ✅ **Работает без интернета** - модели уже внутри образа
- ✅ Не требует доступа к репозиториям моделей (HuggingFace, ModelScope, AIStudio, BOS)
- ✅ Быстрый старт - нет задержек на загрузку моделей

### 2. Надежность
- ✅ **Нет ошибок** "No available model hosting platforms detected"
- ✅ Гарантированная доступность моделей
- ✅ Стабильная работа в изолированных сетях
- ✅ Предсказуемое поведение

### 3. Производительность
- ✅ Нет задержек на загрузку моделей при первом запросе
- ✅ Модели оптимизированы для образа
- ✅ Быстрая инициализация (модели уже на диске)

## ⚠️ Ограничения офлайн-образа

### 1. Размер образа
- ⚠️ Образ **значительно больше** (включает все модели)
- ⚠️ Примерно **25-30GB** (вместо 20GB)
- ⚠️ Медленнее скачивание и загрузка в registry

### 2. Обновление моделей
- ⚠️ **Требуется пересборка образа** для обновления моделей
- ⚠️ Нельзя обновить модели "на лету" без пересборки
- ⚠️ Нужно пересобирать весь образ при выходе новых версий моделей

### 3. Гибкость
- ⚠️ **Фиксированный набор моделей** - нельзя легко добавить другие
- ⚠️ Все модели, которые есть в образе - это всё, что доступно
- ⚠️ Нельзя динамически загружать новые модели

### 4. Доступность
- ⚠️ Может быть не всегда доступен в официальных репозиториях
- ⚠️ Может требовать специальной регистрации или доступа
- ⚠️ Нужно проверять наличие офлайн-версии для нужной версии

## 🔌 Доступность HTTP API

### ✅ ДА! Офлайн-образ полностью поддерживает HTTP API!

**Важно:** Офлайн-образ — это **базовый образ с моделями**, на который можно **добавить** наш FastAPI сервер.

### Как это работает:

#### 1. Базовый офлайн-образ (с моделями)

```dockerfile
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest-gpu-sm120-offline
```

**Что в нем:**
- ✅ Все модели предзагружены
- ✅ PaddleOCR-VL установлен
- ✅ Все зависимости есть

#### 2. Добавляем наш FastAPI сервер

```dockerfile
# Устанавливаем FastAPI зависимости
RUN pip install fastapi uvicorn python-multipart requests Pillow boto3 pdf2image

# Копируем наш FastAPI handler
COPY server.py /app/server.py

# Открываем порт
EXPOSE 8081

# Запускаем FastAPI сервер
CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8081"]
```

#### 3. Результат

```
Офлайн-образ (с моделями) + FastAPI = Полноценный HTTP API сервис
```

### Все эндпоинты работают так же:

1. **POST /ocr** — OCR обработка
2. **GET /health** — Health check
3. **GET /** — Информация о сервисе
4. **GET /docs** — Swagger UI
5. **GET /redoc** — ReDoc документация

**Разница:** Модели уже загружены, нет задержек на инициализацию!

## 🔄 Сравнение подходов

### Текущий подход (наш образ версии 1.2.0):

```
Базовый образ 
  → Установка зависимостей 
  → Предзагрузка моделей (может не сработать)
  → FastAPI
     ↓
  Модели могут не загрузиться ❌
```

**Проблема:** Модели пытаются загрузиться при инициализации, но нет доступа к интернету.

### С офлайн-образом:

```
Офлайн-образ (с моделями внутри)
  → Установка FastAPI
  → Копирование server.py
  → Готово! ✅
     ↓
  Все модели уже есть ✅
```

**Преимущество:** Модели гарантированно доступны, никаких загрузок.

## 📊 Сравнительная таблица

| Параметр | Текущий образ | С офлайн-образом |
|----------|---------------|------------------|
| **Размер** | ~20.5GB | ~25-30GB |
| **Модели** | Загружаются при сборке | Уже в образе ✅ |
| **Интернет при runtime** | Требуется | Не требуется ✅ |
| **HTTP API** | ✅ Есть | ✅ Есть |
| **Скорость старта** | Медленнее (загрузка) | Быстрее ✅ |
| **Надежность** | Может быть ошибка | Гарантировано ✅ |
| **Обновление моделей** | Пересборка | Пересборка |

## 🎯 Что включает наш FastAPI сервер

### Эндпоинты (не зависят от типа образа):

1. **POST /ocr** — основной эндпоинт для OCR обработки
   - Поддержка Base64, URL, multipart/form-data
   - Конвертация PDF в Markdown
   
2. **GET /health** — проверка здоровья сервиса
   - Статус PaddleOCR-VL
   - Статус S3 хранилища
   
3. **GET /** — информация о сервисе
   - Версия
   - Список endpoints
   - Ссылки на документацию

4. **GET /docs** — Swagger UI для тестирования
   - Интерактивное тестирование API
   - Загрузка файлов через браузер

5. **GET /redoc** — ReDoc документация
   - Альтернативная документация API

### Функциональность:

- ✅ Обработка изображений через PaddleOCR-VL
- ✅ Сохранение результатов локально
- ✅ Загрузка в Cloud.ru Object Storage (опционально)
- ✅ Конвертация PDF в Markdown
- ✅ Все работает одинаково с любым базовым образом

## ✅ Итоговый вывод

### Офлайн-образ + наш FastAPI = Идеальное решение!

**Преимущества:**
- ✅ Все модели гарантированно предзагружены
- ✅ Работает без интернета в runtime
- ✅ Тот же HTTP API, что и сейчас
- ✅ Нет ошибок при инициализации
- ✅ Быстрый старт без задержек

**Ограничения:**
- ⚠️ Больший размер образа (25-30GB вместо 20GB)
- ⚠️ Сложнее обновлять модели (нужна пересборка)
- ⚠️ Меньше гибкости (фиксированный набор моделей)

**Вывод:** Для production использования с изолированной сетью (как Cloud.ru ML Inference) офлайн-образ - оптимальное решение!

---

**Рекомендация:** Использовать офлайн-образ как базовый для нашего FastAPI сервиса, чтобы гарантировать стабильную работу без интернета.
