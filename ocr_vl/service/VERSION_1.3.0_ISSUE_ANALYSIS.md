# Анализ проблемы версии 1.3.0

**Дата:** 05.12.2025  
**Версия:** 1.3.0

## 🔍 Проблема

### Симптомы:

1. ✅ Сервис запускается успешно
2. ✅ Health check работает
3. ✅ FastAPI доступен
4. ❌ При попытке обработки OCR: **502 Bad Gateway**

### Логи:

```
✅ Starting FastAPI handler on port 8081...
✅ Using offline image with pre-loaded models
⚠️  No model hoster is available! Please check your network connection...
✅ PaddleOCRVL imported successfully
✅ Application startup complete
```

## 🔍 Анализ

### Что происходит:

1. **Сервис запускается:**
   - FastAPI handler работает
   - PaddleOCRVL импортирован

2. **При первом OCR запросе:**
   - Происходит ленивая инициализация PaddleOCR-VL
   - Возникает ошибка (502)
   - Сервис не отвечает

### Возможные причины:

1. **Модели не доступны:**
   - Офлайн-образ не содержит модели в нужном формате
   - Модели находятся в других директориях
   - Требуется дополнительная настройка

2. **Ошибка инициализации:**
   - PaddleOCR-VL падает при попытке загрузки моделей
   - Нет доступа к локальным моделям
   - Неправильная конфигурация

3. **Таймаут:**
   - Инициализация занимает слишком много времени
   - Прокси/балансировщик прерывает запрос

## 💡 Решения

### Вариант 1: Проверить наличие моделей в образе

Проверить, действительно ли модели есть в офлайн-образе:

```bash
docker run --rm --entrypoint /bin/bash \
  paddleocr-vl-service:1.3.0 \
  -c "find / -name '*PP-DocLayoutV2*' -o -name '*model*' 2>/dev/null | head -20"
```

### Вариант 2: Проверить логи при инициализации

Включить подробное логирование при инициализации PaddleOCR-VL.

### Вариант 3: Использовать другой офлайн-образ

Попробовать другой вариант офлайн-образа или вернуться к ручной предзагрузке моделей.

### Вариант 4: Увеличить timeout

Увеличить timeout для запросов в настройках ML Inference.

## 📋 Рекомендации

1. **Проверить логи контейнера:**
   - Посмотреть полные логи при попытке обработки
   - Найти конкретную ошибку инициализации

2. **Проверить наличие моделей:**
   - Убедиться, что модели действительно в образе
   - Проверить их расположение

3. **Тестирование:**
   - Попробовать локально запустить и протестировать
   - Проверить инициализацию PaddleOCR-VL

---

**Статус:** ⚠️ Требуется диагностика проблемы с инициализацией моделей

