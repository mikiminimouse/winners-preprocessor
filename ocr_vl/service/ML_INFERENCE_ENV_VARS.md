# Переменные окружения для ML Inference

## ✅ Обязательные переменные (для работы S3 сохранения)

Для сервиса `paddleocr-vl-service` нужно указать следующие переменные окружения:

```bash
# Cloud.ru S3 конфигурация (для сохранения результатов OCR)
CLOUDRU_S3_ENDPOINT=https://s3.cloud.ru
CLOUDRU_S3_BUCKET=bucket-winners223
CLOUDRU_S3_ACCESS_KEY=502f76f0-9017-493d-bda4-9e1bb278da84:ce94860ccc8780b2bc5f00f31459d24e
CLOUDRU_S3_SECRET_KEY=759469c88d6e450b584e2487c5174770
```

## ❌ НЕ нужны (не используются в paddleocr-vl-service)

Следующие переменные **НЕ требуются** для этого сервиса, так как он работает напрямую с PaddleOCR-VL, а не через внешний VLM API:

```bash
# Эти переменные НЕ используются:
USE_CLOUDRU_VLM=false                    # Не используется
CLOUDRU_API_URL=...                      # Не используется
CLOUDRU_API_KEY=...                      # Не используется
CLOUDRU_MODEL=...                        # Не используется

# Эти переменные тоже НЕ используются (не нужны для boto3):
USE_CLOUDRU_S3=false                     # Не используется (сервис проверяет наличие credentials)
CLOUDRU_S3_TENANT_ID=...                 # Не используется напрямую
CLOUDRU_S3_KEY_ID=...                    # Не используется напрямую
CLOUDRU_S3_REGION=...                    # Не используется (boto3 определяет автоматически)
```

## 📋 Формат для ML Inference UI

При настройке ML Inference в Cloud.ru консоли добавьте следующие переменные окружения:

### Вариант 1: Минимальная конфигурация (только S3)

```
CLOUDRU_S3_ENDPOINT=https://s3.cloud.ru
CLOUDRU_S3_BUCKET=bucket-winners223
CLOUDRU_S3_ACCESS_KEY=502f76f0-9017-493d-bda4-9e1bb278da84:ce94860ccc8780b2bc5f00f31459d24e
CLOUDRU_S3_SECRET_KEY=759469c88d6e450b584e2487c5174770
```

### Вариант 2: Без S3 (только локальное сохранение)

Если не нужно сохранять результаты в S3, можно **не указывать** переменные окружения вообще. Сервис будет работать, но результаты будут сохраняться только локально в контейнере.

## 🔍 Как это работает

Сервис проверяет наличие всех 4 обязательных переменных для S3:
- Если все 4 переменные присутствуют → результаты сохраняются и локально, и в S3
- Если хотя бы одна отсутствует → результаты сохраняются только локально

## ⚠️ Важно

1. **ACCESS_KEY формат:** Должен содержать `tenant_id:key_id` (как в вашем примере)
2. **Все 4 переменные обязательны** для работы S3 сохранения
3. **Переменные для VLM API не нужны** - сервис не использует внешний VLM API

## 📝 Пример полной конфигурации ML Inference

```
Docker Image: docling-granite-258m.cr.cloud.ru/paddleocr-vl-service:latest
Port: 8081
Health Check: /health

Environment Variables:
  CLOUDRU_S3_ENDPOINT=https://s3.cloud.ru
  CLOUDRU_S3_BUCKET=bucket-winners223
  CLOUDRU_S3_ACCESS_KEY=502f76f0-9017-493d-bda4-9e1bb278da84:ce94860ccc8780b2bc5f00f31459d24e
  CLOUDRU_S3_SECRET_KEY=759469c88d6e450b584e2487c5174770

Resources:
  CPU: 4-8 cores
  Memory: 16-32 GB
  GPU: 1x NVIDIA (CUDA 12.6+)
```

