# Краткая сводка: Переменные окружения для Cloud.ru

**Версия:** 1.3.6  
**Дата:** 05.12.2025

## 🎯 Главный вывод

**Для версий 1.3.0+ (включая текущую 1.3.6):**

✅ **Переменные для Docker run НЕ ТРЕБУЮТСЯ** (кроме опциональных S3)  
✅ Все внутренние переменные устанавливаются **автоматически**  
✅ Для базовой работы достаточно только образа Docker

---

## 📦 Базовый образ

```
ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest-gpu-sm120-offline
```

**Особенности:**
- Модели предзагружены
- Не требует интернета в runtime
- Все зависимости включены

---

## 🔧 Переменные окружения

### ✅ Опциональные (только для S3 сохранения)

```
CLOUDRU_S3_ENDPOINT = https://s3.cloud.ru
CLOUDRU_S3_BUCKET = bucket-winners223
CLOUDRU_S3_ACCESS_KEY = your-access-key
CLOUDRU_S3_SECRET_KEY = your-secret-key
```

**Поведение:**
- Если все 4 заданы → результаты в S3 + локально
- Если не заданы → результаты только локально

### ❌ НЕ требуются (устанавливаются автоматически)

- `PADDLEX_HOME=/home/paddleocr/.paddlex`
- `HF_HOME=/home/paddleocr/.cache/huggingface`
- `FLAGS_enable_eager_mode=1`
- `FLAGS_eager_delete_tensor_gb=0`
- `FLAGS_use_mkldnn=0`

### ❌ НЕ используются

- Все переменные VLM API
- Дополнительные S3 переменные

---

## 📊 Сравнение версий

| Версия | Базовый образ | Переменные для Docker run |
|--------|--------------|---------------------------|
| 1.0.8-1.2.0 | `paddlex-genai-vllm-server` | ⚠️ Устаревшие |
| **1.3.0+** | `paddleocr-vl:latest-gpu-sm120-offline` | ✅ Только S3 (опционально) |

---

## 🚀 Быстрый старт

### В веб-консоли Cloud.ru ML Inference:

1. **Docker Image:**
   ```
   docling-granite-258m.cr.cloud.ru/paddleocr-vl-service:1.3.6
   ```

2. **Environment Variables (опционально):**
   ```
   CLOUDRU_S3_ENDPOINT = https://s3.cloud.ru
   CLOUDRU_S3_BUCKET = bucket-winners223
   CLOUDRU_S3_ACCESS_KEY = your-access-key
   CLOUDRU_S3_SECRET_KEY = your-secret-key
   ```

3. **Порт:**
   ```
   8081
   ```

4. **Готово!** ✅

---

## 📚 Подробная документация

- `ENVIRONMENT_VARIABLES_ANALYSIS.md` - Полный анализ
- `ENV_VARS_QUICK_REFERENCE.md` - Быстрая справка

---

**Итог:** Для версий 1.3.0+ переменные для Docker run **НЕ требуются** (кроме опциональных S3)!

