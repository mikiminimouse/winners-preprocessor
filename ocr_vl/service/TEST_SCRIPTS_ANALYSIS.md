# Анализ тестовых скриптов для PaddleOCR-VL-0.9B

**Дата:** 06.12.2025  
**Версия:** 1.3.6

## 📋 Найденные тестовые скрипты

### 1. test_full_functionality.py ✅ (Комплексный)

**Назначение:** Полное тестирование всех функций сервиса

**Тесты:**
1. Health Check
2. OCR Base64
3. OCR Multipart
4. PDF to MD (Base64)
5. Documentation endpoints

**URL:**
```
https://1f59f38e-12c1-4199-8638-7169d18fc993.modelrun.inference.cloud.ru
```

**Особенности:**
- ✅ Комплексное тестирование
- ⚠️ Использует Base64 для PDF (может вызвать 502)
- ✅ Использует Multipart для тестовых изображений
- ✅ Сохраняет результаты в JSON

**Использование:**
```bash
python3 test_full_functionality.py [путь_к_pdf]
```

---

### 2. test_pdf_to_md.py ✅ (PDF конвертация)

**Назначение:** Тестирование конвертации PDF в MD

**Тесты:**
1. Health Check
2. Конвертация PDF в изображения
3. Отправка страниц на OCR (Base64)
4. Получение результатов

**URL:**
```
https://1f59f38e-12c1-4199-8638-7169d18fc993.modelrun.inference.cloud.ru
```

**Особенности:**
- ⚠️ Использует Base64 (может вызвать 502)
- ✅ Простой и понятный
- ✅ Обрабатывает все страницы PDF

**Использование:**
```bash
python3 test_pdf_to_md.py
```

**Проблема:** Base64 может вызвать 502 ошибку для больших изображений

---

### 3. test_pdf_conversion.py ✅ (Альтернативный)

**Назначение:** Тестирование конвертации PDF через ML Inference

**URL:**
```
https://c5b0e67c-1426-48e5-b2cd-c86c0acdb5c3.modelrun.inference.cloud.ru
```

**Особенности:**
- ⚠️ Использует Base64
- ✅ Обрабатывает несколько файлов
- ✅ Сохраняет результаты

**Использование:**
```bash
python3 test_pdf_conversion.py
```

---

### 4. test_paddleocr_comprehensive.py ✅ (НОВЫЙ - Рекомендуемый)

**Назначение:** Комплексное тестирование с использованием Multipart

**Тесты:**
1. Health Check
2. OCR Multipart (рекомендуемый способ)
3. PDF to MD через Multipart
4. Service Info

**URL:** Настраивается через переменную окружения

**Особенности:**
- ✅ Использует Multipart вместо Base64
- ✅ Избегает проблем с 502
- ✅ Тестирует PaddleOCR-VL-0.9B
- ✅ Современный подход

**Использование:**
```bash
# С автопоиском PDF
python3 test_paddleocr_comprehensive.py

# С указанием PDF файла
python3 test_paddleocr_comprehensive.py /path/to/file.pdf
```

---

## 🔍 Проблемы в существующих скриптах

### Проблема 1: Использование Base64

**Файлы:**
- `test_full_functionality.py` - использует Base64 для PDF
- `test_pdf_to_md.py` - использует Base64
- `test_pdf_conversion.py` - использует Base64

**Проблема:**
- Base64 увеличивает размер данных на ~33%
- Большие изображения могут вызвать 502 (EOF)
- Прокси/балансировщик может разрывать соединение

**Решение:**
- Использовать Multipart upload
- Новый скрипт `test_paddleocr_comprehensive.py` использует Multipart

---

### Проблема 2: Разные URL

**Найдено два разных URL:**

1. **URL 1:** `https://1f59f38e-12c1-4199-8638-7169d18fc993.modelrun.inference.cloud.ru`
   - Используется в: `test_full_functionality.py`, `test_pdf_to_md.py`
   - Статус: ⚠️ Возвращает 404 (возможно не развернут)

2. **URL 2:** `https://c5b0e67c-1426-48e5-b2cd-c86c0acdb5c3.modelrun.inference.cloud.ru`
   - Используется в: `test_pdf_conversion.py`
   - Статус: ⚠️ Не проверен

**Решение:**
- Использовать переменную окружения `ML_INFERENCE_URL`
- Проверить актуальный URL в Cloud.ru Console

---

## ✅ Рекомендации

### Для тестирования PaddleOCR-VL-0.9B:

1. **Использовать Multipart upload:**
   - Избегает проблем с 502
   - Более надежный способ
   - Подходит для больших файлов

2. **Использовать новый скрипт:**
   ```bash
   python3 test_paddleocr_comprehensive.py
   ```

3. **Проверить URL сервиса:**
   ```bash
   # Установить правильный URL
   export ML_INFERENCE_URL="https://your-endpoint.modelrun.inference.cloud.ru"
   
   # Запустить тесты
   python3 test_paddleocr_comprehensive.py
   ```

---

## 📊 Что тестируется

### Компоненты PaddleOCR-VL-0.9B:

1. **PP-DocLayoutV2**
   - Layout detection
   - Определение структуры документа
   - Тестируется автоматически при OCR обработке

2. **PaddleOCR-VL-0.9B**
   - Text recognition
   - Распознавание текста
   - Генерация Markdown/JSON
   - Тестируется при обработке изображений

### Процесс тестирования:

```
Тест → Создание/загрузка изображения → Отправка на OCR → 
→ PaddleOCR-VL обработка → Получение результатов → Проверка
```

---

## 🚀 План тестирования

### Шаг 1: Проверка доступности сервиса

```bash
curl https://your-endpoint.modelrun.inference.cloud.ru/health \
  -H "Authorization: Bearer YOUR_API_KEY"
```

**Ожидаемый результат:**
```json
{
  "status": "healthy",
  "paddleocr": "ready",
  "s3_storage": "configured"
}
```

### Шаг 2: Тест OCR обработки

```bash
# Используя новый скрипт
python3 test_paddleocr_comprehensive.py
```

**Что проверяется:**
- ✅ Создание тестового изображения
- ✅ Отправка через Multipart
- ✅ Обработка через PaddleOCR-VL-0.9B
- ✅ Получение результатов

### Шаг 3: Тест PDF to MD

```bash
# С автопоиском PDF
python3 test_paddleocr_comprehensive.py

# Или с указанием файла
python3 test_paddleocr_comprehensive.py /path/to/test.pdf
```

**Что проверяется:**
- ✅ Конвертация PDF в изображения
- ✅ Обработка каждой страницы
- ✅ Получение результатов в Markdown
- ✅ Производительность обработки

---

## 📄 Результаты тестирования

Все результаты сохраняются в:
```
paddle_docker_servise/test_results/test_report_paddleocr_YYYYMMDD_HHMMSS.json
```

**Структура отчета:**
```json
{
  "test_run": {
    "timestamp": "...",
    "base_url": "...",
    "service_version": "1.3.6",
    "model": "PaddleOCR-VL-0.9B"
  },
  "summary": {
    "total": 4,
    "passed": 2,
    "failed": 2,
    "duration_seconds": 123.45
  },
  "results": [...],
  "errors": [...]
}
```

---

## 🔧 Обновление скриптов

### Рекомендуемые изменения:

1. **Использовать Multipart вместо Base64:**
   - Более надежный способ
   - Избегает проблем с 502

2. **Использовать переменные окружения:**
   ```python
   BASE_URL = os.getenv("ML_INFERENCE_URL", "default-url")
   ```

3. **Улучшить обработку ошибок:**
   - Детальное логирование
   - Retry механизм
   - Таймауты

---

## ⚠️ Текущий статус

**Сервис недоступен (404):**
- URL: `https://1f59f38e-12c1-4199-8638-7169d18fc993.modelrun.inference.cloud.ru`
- Статус: Возвращает 404
- Причина: Возможно не развернут или URL изменился

**Рекомендации:**
1. Проверить актуальный URL в Cloud.ru Console
2. Убедиться, что сервис развернут
3. Проверить настройки Health Check

---

**Версия документа:** 1.0  
**Последнее обновление:** 06.12.2025



