# Версия 1.3.5 - Попытка решения static graph mode в Python коде

**Дата:** 05.12.2025  
**Версия:** 1.3.5

## 🔍 Проблема

Версия 1.3.4 все еще имеет проблему:
```
"int(Tensor) is not supported in static graph mode"
```

Переменные окружения в `start.sh` не решили проблему.

## 💡 Решение в версии 1.3.5

### Изменения:

1. **Настройка PaddlePaddle ДО импорта PaddleOCR-VL:**
   ```python
   import paddle
   paddle.set_flags({
       'FLAGS_eager_delete_tensor_gb': 0,
       'FLAGS_use_mkldnn': False,
   })
   ```

2. **Дополнительная настройка перед инициализацией:**
   ```python
   if hasattr(paddle, 'disable_static'):
       paddle.disable_static()
   ```

### Логика:

- Флаги устанавливаются в Python коде ДО импорта PaddleOCR-VL
- Это должно гарантировать использование dynamic graph mode
- Комбинируется с переменными окружения для максимальной совместимости

## ⚠️ Альтернативное решение

Если версия 1.3.5 не решит проблему, рекомендуется:

### Вариант: Использовать базовый образ PaddlePaddle 3.0.0

**Базовый образ:**
```dockerfile
FROM ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddle:3.0.0-gpu-cuda12.6-cudnn9.5-trt10.5
```

**Преимущества:**
- Последняя стабильная версия PaddlePaddle
- Полный контроль версий
- Возможно, уже решена проблема со static graph mode

**Недостатки:**
- Требует ручной установки PaddleOCR-VL
- Больше шагов сборки

## 📋 Статус

- ✅ Изменения внесены в код
- ✅ Образ собран
- ⏳ Требуется тестирование на Cloud.ru

---

**Статус:** Требуется тестирование

