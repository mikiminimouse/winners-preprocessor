# Полный анализ PaddleOCR-VL: инфраструктура, работа и проблемы

**Дата:** 06.12.2025  
**Версия:** 1.3.6  
**Статус:** Комплексный анализ

## 📋 Содержание

1. [Анализ логов запуска](#1-анализ-логов-запуска)
2. [Как работает PaddleOCR-VL](#2-как-работает-paddleocr-vl)
3. [Проблемы в процессе извлечения текста](#3-проблемы-в-процессе-извлечения-текста)
4. [Компоненты и модели инфраструктуры](#4-компоненты-и-модели-инфраструктуры)
5. [Логика вызова моделей по версиям](#5-логика-вызова-моделей-по-версиям)
6. [MCP от PaddleX](#6-mcp-от-paddlex)
7. [Подключение через exec на Cloud.ru](#7-подключение-через-exec-на-cloudru)

---

## 1. Анализ логов запуска

### Логи от 06.12.2025 00:49:

```
[00:49:09] Starting FastAPI handler on port 8081...
[00:49:09] Using offline image with pre-loaded models
[00:49:09] PADDLEX_HOME=/home/paddleocr/.paddlex
[00:49:09] Dynamic graph mode enabled (FLAGS_enable_eager_mode=1)
[00:49:11] UserWarning: No ccache found...
[00:50:32] No model hoster is available! Please check your network...
[00:50:33] Started server process [1]
[00:50:33] Application startup complete.
[00:50:33] Uvicorn running on http://127.0.0.1:8081
```

### Что происходит:

1. **00:49:09 - Запуск сервера:**
   - FastAPI handler запускается на порту 8081
   - Используется офлайн-образ с предзагруженными моделями
   - Устанавливаются переменные окружения для моделей

2. **00:49:09 - Настройка PaddlePaddle:**
   - `PADDLEX_HOME=/home/paddleocr/.paddlex` - путь к моделям
   - `FLAGS_enable_eager_mode=1` - включен dynamic graph mode
   - Это решает проблему "int(Tensor) is not supported in static graph mode"

3. **00:49:11 - Предупреждение:**
   - `No ccache found` - это не критично
   - ccache используется для ускорения компиляции, но не обязателен

4. **00:50:32 - Информационное сообщение:**
   - `No model hoster is available` - это НОРМАЛЬНО
   - Офлайн-образ не требует доступа к репозиториям моделей
   - Модели уже находятся в образе

5. **00:50:33 - Сервер готов:**
   - FastAPI запущен и готов к работе
   - PaddleOCR-VL инициализируется в фоне (через 10 секунд)

### Вывод:

✅ **Все работает корректно!**  
- Сервер запустился успешно
- Модели доступны (офлайн-образ)
- Dynamic graph mode включен
- Фоновая инициализация PaddleOCR-VL началась

---

## 2. Как работает PaddleOCR-VL

### 2.1. Общая архитектура

```
┌─────────────────────────────────────────┐
│         FastAPI Handler (8081)          │
│  - Прием HTTP запросов                  │
│  - Обработка Base64/URL/Multipart       │
│  - Сохранение результатов               │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│         PaddleOCR-VL Pipeline           │
│                                         │
│  ┌──────────────┐  ┌──────────────┐   │
│  │ PP-DocLayout │  │ PaddleOCR-VL │   │
│  │     V2       │  │   0.9B VLM   │   │
│  └──────┬───────┘  └──────┬───────┘   │
│         │                 │            │
│         └────────┬────────┘            │
│                  ▼                     │
│         ┌─────────────────┐           │
│         │ Post-processing │           │
│         │  (Markdown/JSON)│           │
│         └─────────────────┘           │
└─────────────────────────────────────────┘
```

### 2.2. Пошаговый процесс обработки изображения

#### Шаг 1: Получение изображения
```python
# FastAPI endpoint /ocr получает:
- image_base64: Base64 строка
- image_url: URL изображения
- file: Multipart файл
```

#### Шаг 2: Сохранение во временный файл
```python
# Все форматы конвертируются в файл:
temp_image_path = TEMP_DIR / f"tmp_{uuid}.jpg"
image.save(temp_image_path)
```

#### Шаг 3: Инициализация PaddleOCR-VL (ленивая)
```python
# При первом запросе:
paddle_ocr = PaddleOCRVL()
# Загружаются модели:
# - PP-DocLayoutV2 (layout detection)
# - PaddleOCR-VL-0.9B (VLM recognition)
```

#### Шаг 4: Обработка изображения
```python
# PaddleOCR-VL обрабатывает изображение:
result = paddle_ocr.predict(temp_image_path)
```

#### Шаг 5: Сохранение результатов
```python
# Результаты сохраняются в двух форматах:
result.save_to_markdown(save_path=md_path)
result.save_to_json(save_path=json_path)
```

### 2.3. Что делает PaddleOCR-VL внутри

1. **Layout Detection (PP-DocLayoutV2):**
   - Определяет структуру документа
   - Находит текстовые блоки, таблицы, изображения
   - Выделяет области для распознавания

2. **VLM Recognition (PaddleOCR-VL-0.9B):**
   - Распознает текст в выделенных областях
   - Использует Vision-Language Model
   - Понимает контекст документа

3. **Post-processing:**
   - Форматирует результаты в Markdown
   - Сохраняет структуру документа
   - Генерирует JSON с координатами

---

## 3. Проблемы в процессе извлечения текста

### 3.1. Известные проблемы

#### Проблема 1: HTTP 502 для Base64 и PDF to MD

**Симптомы:**
- Base64 запросы возвращают 502 (EOF)
- PDF to MD конвертация возвращает 502
- Multipart работает корректно

**Причина:**
- Base64 увеличивает размер данных на ~33%
- Большие изображения могут быть слишком большими
- Прокси/балансировщик разрывает соединение
- Проблема с буферизацией Base64 строки в form-data

**Статус:** ⚠️ Частично решено (Multipart работает)

**Решение:**
- Использовать Multipart для больших файлов
- Для Base64 - только небольшие изображения
- Увеличить таймауты на стороне клиента

#### Проблема 2: SIGTERM при запуске

**Симптомы:**
- Контейнер получает SIGTERM через 37-39 секунд
- Бесконечный цикл перезапуска
- Не успевает инициализироваться

**Причина:**
- Health Check настроен слишком агрессивно
- Инициализация занимает 80-90 секунд
- Health Check убивает контейнер до завершения

**Статус:** ✅ Решение известно

**Решение:**
- Настроить Startup Probe:
  - `failureThreshold: 50`
  - `initialDelaySeconds: 10`
  - `periodSeconds: 10`

#### Проблема 3: Static Graph Mode

**Симптомы:**
- Ошибка: "int(Tensor) is not supported in static graph mode"
- Возникает при обработке OCR запросов

**Причина:**
- PaddlePaddle работает в static graph mode
- PaddleOCR-VL требует dynamic graph mode

**Статус:** ✅ Решено

**Решение:**
- Установлены переменные окружения:
  - `FLAGS_enable_eager_mode=1`
  - `FLAGS_eager_delete_tensor_gb=0`
  - `FLAGS_use_mkldnn=0`

### 3.2. Потенциальные проблемы

#### Проблема 4: Память GPU

**Риск:**
- Модели занимают много памяти GPU
- Большие изображения могут вызвать OOM

**Мониторинг:**
- Проверять использование GPU памяти
- Ограничивать размер обрабатываемых изображений

#### Проблема 5: Производительность

**Риск:**
- Обработка одного изображения может занимать минуты
- Нет параллельной обработки

**Улучшения:**
- Добавить кэширование результатов
- Оптимизировать размер изображений
- Использовать батчинг при необходимости

---

## 4. Компоненты и модели инфраструктуры

### 4.1. Базовые компоненты

#### A. PaddlePaddle Framework

**Что это:**
- Глубокий learning фреймворк от Baidu
- Основа для всех Paddle моделей
- Поддержка GPU (CUDA 12.6+)

**Где находится:**
- Включен в базовый образ
- Версия определяется базовым образом

**Роль:**
- Низкоуровневая работа с тензорами
- Управление GPU памятью
- Выполнение вычислений

#### B. PaddleOCR-VL

**Что это:**
- Vision-Language Model для OCR
- Версия: 0.9B параметров
- Основа: Qwen-VL архитектура

**Где находится:**
- `/home/paddleocr/.paddlex/official_models/PaddleOCR-VL`
- Модель предзагружена в офлайн-образе

**Роль:**
- Распознавание текста
- Понимание структуры документа
- Генерация Markdown/JSON

#### C. PP-DocLayoutV2

**Что это:**
- Модель для анализа layout документа
- Определяет структуру: текст, таблицы, изображения

**Где находится:**
- `/home/paddleocr/.paddlex/official_models/PP-DocLayoutV2`
- Модель предзагружена в офлайн-образе

**Роль:**
- Detection структуры документа
- Выделение областей для OCR
- Определение типов блоков

#### D. PaddleX

**Что это:**
- Высокоуровневая библиотека над PaddlePaddle
- Упрощает работу с моделями
- Управление моделями и их загрузкой

**Где находится:**
- Включен в базовый образ PaddleOCR-VL
- Используется для загрузки моделей

**Роль:**
- Управление моделями
- Загрузка из репозиториев
- Кэширование моделей

### 4.2. Модели в офлайн-образе

#### Модель 1: PP-DocLayoutV2

```
Расположение: /home/paddleocr/.paddlex/official_models/PP-DocLayoutV2
Размер: ~500MB
Назначение: Layout detection
```

#### Модель 2: PaddleOCR-VL-0.9B

```
Расположение: /home/paddleocr/.paddlex/official_models/PaddleOCR-VL
Размер: ~2-3GB
Назначение: Text recognition (VLM)
```

#### Модель 3: HuggingFace Cache

```
Расположение: /home/paddleocr/.cache/huggingface
Назначение: Кэш для трансформеров
```

### 4.3. Что задействовано для OCR

```
Изображение → PP-DocLayoutV2 → PaddleOCR-VL → Markdown/JSON
              (layout)         (recognition)   (output)
```

**Полный pipeline:**

1. **Input:** Изображение (JPG/PNG/PDF страница)
2. **Layout Detection:** PP-DocLayoutV2 анализирует структуру
3. **Region Extraction:** Выделяются области текста
4. **VLM Recognition:** PaddleOCR-VL распознает текст в областях
5. **Post-processing:** Форматирование в Markdown/JSON
6. **Output:** Структурированный текст

---

## 5. Логика вызова моделей по версиям

### Версия 1.0.8-1.0.9 (устаревшие)

**Базовый образ:**
```dockerfile
FROM paddlex-genai-vllm-server:latest
```

**Логика:**

```
1. Запуск vLLM сервера (порт 8080) - версия 1.0.8
2. Инициализация PaddleOCR-VL
3. Попытка подключения к vLLM (не реализована)
4. Использование PaddleOCR-VL напрямую через PaddlePaddle
```

**Модели:**
- Загружаются при первом запросе
- Требуют доступ к интернету
- Могут не загрузиться без сети

### Версия 1.1.0-1.2.0 (экспериментальные)

**Базовый образ:**
```dockerfile
FROM paddlex-genai-vllm-server:latest
```

**Логика:**

```
1. Попытка предзагрузки моделей при сборке
2. Инициализация PaddleOCR-VL
3. Использование PaddleOCR-VL напрямую
```

**Проблемы:**
- Модели не предзагружались корректно
- Все еще требовали интернет

### Версия 1.3.0+ (текущая, рекомендуемая)

**Базовый образ:**
```dockerfile
FROM paddleocr-vl:latest-gpu-sm120-offline
```

**Логика:**

```
Шаг 1: Запуск FastAPI сервера
  └─> Установка переменных окружения
      - PADDLEX_HOME=/home/paddleocr/.paddlex
      - FLAGS_enable_eager_mode=1

Шаг 2: Фоновая инициализация (через 10 сек)
  └─> Создание PaddleOCR-VL объекта
      └─> Загрузка моделей из образа:
          - PP-DocLayoutV2 (из /home/paddleocr/.paddlex)
          - PaddleOCR-VL-0.9B (из /home/paddleocr/.paddlex)

Шаг 3: Обработка запроса
  └─> Получение изображения (Base64/URL/Multipart)
      └─> Сохранение во временный файл
          └─> Вызов paddle_ocr.predict(image_path)
              ├─> Layout Detection (PP-DocLayoutV2)
              ├─> Text Recognition (PaddleOCR-VL-0.9B)
              └─> Post-processing → Markdown/JSON
```

**Модели:**
- ✅ Предзагружены в образе
- ✅ Не требуют интернета
- ✅ Быстрый доступ

### Детальная логика вызова (версия 1.3.6)

```python
# 1. Инициализация (при первом запросе или в фоне)
paddle_ocr = PaddleOCRVL()

# Внутри PaddleOCRVL():
#   - Загружается PP-DocLayoutV2
#   - Загружается PaddleOCR-VL-0.9B
#   - Инициализируются процессоры

# 2. Обработка изображения
result = paddle_ocr.predict(image_path)

# Внутри predict():
#   a. Layout Detection:
#      - PP-DocLayoutV2 анализирует изображение
#      - Определяет структуру: текст, таблицы, изображения
#      - Возвращает координаты регионов
#
#   b. Text Recognition:
#      - Для каждого текстового региона:
#        - PaddleOCR-VL-0.9B распознает текст
#        - Сохраняет координаты
#
#   c. Post-processing:
#      - Форматирует результаты
#      - Сохраняет структуру документа
#      - Генерирует Markdown/JSON

# 3. Сохранение результатов
result.save_to_markdown(path)
result.save_to_json(path)
```

---

## 6. MCP от PaddleX

### 6.1. Что такое MCP?

**MCP (Model Control Protocol)** - протокол для управления моделями в PaddleX.

**Возможности:**
- Управление моделями через API
- Загрузка/выгрузка моделей
- Мониторинг состояния моделей
- Управление конфигурацией

### 6.2. Есть ли MCP в наших контейнерах?

**Ответ:** ❌ **НЕТ**

В текущих версиях контейнеров:
- Используется только базовый PaddleOCR-VL
- MCP не включен
- Модели управляются напрямую через PaddleX API

### 6.3. Как может работать MCP?

Если бы MCP был включен:

```
MCP Server (порт 9000)
  ├─> Управление моделями
  ├─> Загрузка/выгрузка
  ├─> Мониторинг
  └─> Конфигурация

PaddleOCR-VL
  └─> Подключается к MCP для управления моделями
```

**Компоненты:**
- `paddlex-mcp-server` - сервер MCP
- `paddlex-mcp-client` - клиент для подключения
- REST API для управления

### 6.4. Как добавить MCP?

**Требуется:**
1. Установить `paddlex-mcp-server` в Dockerfile
2. Запустить MCP сервер в контейнере
3. Настроить подключение PaddleOCR-VL к MCP
4. Открыть порт для MCP API

**Пример:**
```dockerfile
# В Dockerfile
RUN pip install paddlex-mcp-server

# В start.sh
paddlex-mcp-server --port 9000 &
```

### 6.5. Нужен ли MCP?

**Для текущих задач:** ❌ **НЕ нужен**

**Причины:**
- Модели статичны (предзагружены)
- Нет необходимости менять модели в runtime
- Усложняет архитектуру

**Когда может понадобиться:**
- Динамическая загрузка моделей
- A/B тестирование моделей
- Централизованное управление

---

## 7. Подключение через exec на Cloud.ru

### 7.1. Можно ли подключиться через exec?

**Ответ:** ⚠️ **ЗАВИСИТ ОТ НАСТРОЕК CLOUD.RU**

Cloud.ru ML Inference может ограничивать:
- Прямой доступ к контейнеру
- Выполнение `docker exec`
- SSH доступ

### 7.2. Варианты подключения

#### Вариант 1: Через Cloud.ru Console (если доступно)

1. Зайти в Cloud.ru Console
2. Найти ваш Container App
3. Открыть "Container Shell" или "Exec"
4. Выполнить команды внутри контейнера

#### Вариант 2: Через kubectl (если есть доступ)

```bash
# Получить имя pod
kubectl get pods -n <namespace>

# Подключиться к контейнеру
kubectl exec -it <pod-name> -n <namespace> -- /bin/bash
```

#### Вариант 3: Через API Cloud.ru

Cloud.ru может предоставлять API для:
- Выполнения команд в контейнере
- Получения логов
- Мониторинга

### 7.3. Что можно делать через exec

**Для отладки:**

```bash
# 1. Проверить логи
tail -f /var/log/paddleocr.log
# или
journalctl -u paddleocr

# 2. Проверить процессы
ps aux | grep python
ps aux | grep paddle

# 3. Проверить модели
ls -lh /home/paddleocr/.paddlex/official_models/

# 4. Проверить переменные окружения
env | grep PADDLE
env | grep FLAGS

# 5. Проверить GPU
nvidia-smi

# 6. Проверить память
free -h
df -h
```

**Для управления:**

```bash
# 1. Запустить Python интерактивно
python3
>>> from paddleocr import PaddleOCRVL
>>> ocr = PaddleOCRVL()
>>> result = ocr.predict("test.jpg")

# 2. Проверить доступность моделей
python3 << EOF
from pathlib import Path
print(Path("/home/paddleocr/.paddlex").exists())
EOF

# 3. Проверить версию PaddlePaddle
python3 -c "import paddle; print(paddle.__version__)"

# 4. Проверить версию PaddleOCR
python3 -c "import paddleocr; print(paddleocr.__version__)"
```

### 7.4. Альтернативные способы отладки

#### Способ 1: HTTP Endpoints

```bash
# Health check
curl http://your-service/health

# Получить информацию о сервисе
curl http://your-service/

# Проверить логи через API (если доступно)
curl http://your-service/logs
```

#### Способ 2: Логи Cloud.ru

```bash
# Через веб-консоль Cloud.ru:
# 1. Открыть Container App
# 2. Перейти в "Logs"
# 3. Просматривать логи в реальном времени
```

#### Способ 3: Добавить debug endpoints

```python
# В server.py добавить:
@app.get("/debug/info")
async def debug_info():
    import sys
    return {
        "python_version": sys.version,
        "paddle_version": paddle.__version__,
        "models_path": os.environ.get("PADDLEX_HOME"),
        "gpu_available": paddle.device.is_compiled_with_cuda(),
    }

@app.get("/debug/models")
async def debug_models():
    from pathlib import Path
    models_path = Path("/home/paddleocr/.paddlex/official_models")
    return {
        "models": [p.name for p in models_path.iterdir() if p.is_dir()]
    }
```

### 7.5. Рекомендации

**Для отладки:**
1. ✅ Использовать логи Cloud.ru (проще всего)
2. ✅ Добавить debug endpoints в FastAPI
3. ⚠️ Попробовать exec через Cloud.ru Console (если доступно)

**Для управления компонентами:**
1. ✅ Использовать HTTP API (REST endpoints)
2. ⚠️ Exec через Cloud.ru (если есть доступ)
3. ❌ Прямой docker exec (обычно недоступен)

---

## 📚 Дополнительная информация

### Полезные команды для отладки

```bash
# Проверить статус PaddleOCR
curl http://your-service/health

# Проверить версию
curl http://your-service/

# Проверить модели (если есть debug endpoint)
curl http://your-service/debug/models

# Проверить логи через kubectl
kubectl logs <pod-name> -n <namespace>

# Проверить логи через Cloud.ru Console
# (в веб-интерфейсе)
```

### Структура файлов в контейнере

```
/app/
├── server.py              # FastAPI приложение
├── start.sh              # Скрипт запуска
├── output/               # Результаты OCR
├── temp/                 # Временные файлы
└── models/               # (не используется, модели в .paddlex)

/home/paddleocr/
├── .paddlex/
│   └── official_models/
│       ├── PP-DocLayoutV2/
│       └── PaddleOCR-VL/
└── .cache/
    └── huggingface/
```

---

**Версия документа:** 1.0  
**Последнее обновление:** 06.12.2025

