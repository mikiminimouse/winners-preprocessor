# Анализ причин ошибок интеграции SmolDocling

## Выводы тестирования

### ✅ Подтвержденные факты:

1. **SmolDocling работает только с изображениями страниц**
   - Документация HuggingFace подтверждает: модель принимает только изображения (PNG/JPG)
   - Прямая отправка PDF файлов не поддерживается (тесты показали возврат base64 или пустых ответов)

2. **Проблемы не связаны с размером изображений**
   - Уменьшение с 2.8MB до 0.95MB не помогло
   - Проблема в серверной обработке, не в клиентских данных

3. **Сервер SmolDocling на Cloud.ru имеет критические проблемы**
   - Internal Server Error (500) на первом же изображении
   - Connection refused после первого запроса
   - Проксирование на localhost:8080 вместо внешнего API

### 🔍 Коренные причины ошибок:

#### 1. **Серверная инфраструктура Cloud.ru**
```
"EngineCore encountered an issue. See stack trace (above) for the root cause."
```
- Внутренняя ошибка обработки изображений на стороне SmolDocling
- Сервер не может обработать даже оптимизированные изображения

#### 2. **Проблемы с масштабированием**
- После обработки первого файла сервер становится недоступен
- Недостаточные ресурсы или утечки памяти
- Нет возможности последовательной обработки

#### 3. **Неправильная конфигурация прокси**
```
dial tcp 127.0.0.1:8080: connect: connection refused
```
- Health check и API calls проксируются на localhost вместо внешнего endpoint
- Инфраструктурная проблема Cloud.ru

#### 4. **Несоответствие документации**
- Официальная документация SmolDocling показывает работу только с изображениями
- Cloud.ru реализация имеет дополнительные ограничения

## Техническое подтверждение

### Тест с изображениями:
- ✅ Конвертация PDF → PNG работает
- ✅ Оптимизация изображений работает
- ✅ API аутентификация работает
- ❌ Серверная обработка изображений падает

### Тест с PDF напрямую:
- ❌ Модель не понимает PDF формат
- ❌ Возвращает base64 строку или пустые ответы
- ✅ API канал работает (нет connection refused)

## Рекомендации по исправлению

### Для немедленного решения:
1. **Использовать локальную установку SmolDocling**
   ```bash
   pip install docling
   pip install transformers
   ```

2. **Вернуться к Qwen3-VL-8B** как к рабочему решению
   - Модель стабильна и протестирована
   - Имеет аналогичный функционал

### Для долгосрочного решения:
1. **Сообщить о проблемах** команде IBM/Docling
2. **Запросить исправление** инфраструктуры Cloud.ru
3. **Рассмотреть альтернативные модели**:
   - GPT-4V (OpenAI)
   - Claude-3 (Anthropic)
   - Другие VLLM модели

## Заключение

**Проблемы интеграции SmolDocling вызваны не клиентским кодом, а серверными ограничениями Cloud.ru:**

- 🔴 **Инфраструктурные проблемы**: неправильное проксирование, недостаточные ресурсы
- 🔴 **Серверные баги**: Internal Server Error при обработке изображений
- 🔴 **Нестабильность**: падение сервера после каждого запроса

**Техническая интеграция выполнена правильно**, но **инфраструктура Cloud.ru не готова** к использованию SmolDocling в продакшене.

**Рекомендация**: перейти на локальное развертывание SmolDocling или использовать альтернативные модели до исправления проблем Cloud.ru.
